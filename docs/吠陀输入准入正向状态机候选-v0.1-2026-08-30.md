# 吠陀输入准入必要条件零态候选 v0.1（2026-08-30）

> 文件名保留本轮早期工作名以避免制造第二份漂移文档；它不是语义声明。规范含义以本标题、正文和绑定 JSON 为准：本工件不是正向状态机。

## 结论先行

本文件说明 `content/system-admission/vedic-input-admission-readiness-candidate.v0.1.0.json` 所定义的吠陀输入准入必要条件零态候选。它只把未来资格评估不可缺少的条件做成可机读、fail-closed 的零态结构；它不是正向 transition evaluator，不定义正向 receipt schema，不机械接受任何正向迁移，也不声称这些条件单独构成充分条件。

- 候选身份：`vedic-input-admission-readiness-candidate-v0.1.0`
- 记录类型：`vedic-input-admission-readiness-candidate`
- schema：`vedic-input-admission-readiness-candidate/v0.1.0`
- 状态：`thirteen_requirement_conjunctive_readiness_conditions_defined_zero_resolved_zero_admission_effect`
- 当前效果：`activeAdmissionEffect = none`
- 当前进度：13 条要求已定义，`0/13` resolved；8 条合取条件已定义，`0/8` satisfied
- 输入实例：0；接受输入：0；产品输入拒绝回执：0
- 吠陀产品身份：`research-only / not-integrated / not-authoritative`

项目级上下文继续固定为 `legacy-v13 / targetSchema 13 / migrationId null`。该上下文只描述当前项目发布治理，不被吠陀产品身份继承，也不把八字 v1.7 的工程、内容、专家或权利结论外推到吠陀体系。

## 为何在 C、D 或其他体系中选择吠陀切片

对话中曾有“先转入 B”的安排，但用户随后明确改为“先推进 C、D 或其他体系工作”；最新指令覆盖先前 B 的排期。本切片因此在被允许的 C、D 或其他体系范围内选择“其他体系”中的吠陀输入层。选择吠陀不是要抢先宣布其可用，而是当前吠陀材料已有独立产品边界、13 条输入要求、来源/权利要求、双专家计划和隔离工程草案，却缺少一份把分散前提明确列为合取必要条件、同时诚实记录 evaluator 与法律角色空缺的零态候选。

继续增加固定样例或展示性运行，只会继续证明局部工程能力，不能回答“未来资格评估有哪些不可缺少的前提”。因此本候选优先补齐的是必要条件清单与零态投影，而不是实现正向状态机。即使 8 条条件将来全部成立，也仍需额外的 versioned transition/receipt contract 才能判断资格；目前所有未决值、来源、权利、专家、owner、validator、epoch、产品、发布和公开授权继续保持红灯。

这项工作不改变 C、D 的独立路线，也不把一个体系的输入规则、事实、来源或专家意见复用于另一个体系。

## 候选本体与完整性身份

候选文件当前身份如下：

- 路径：`content/system-admission/vedic-input-admission-readiness-candidate.v0.1.0.json`
- 文件字节数：`35325`
- 原始文件 SHA-256：`7f42750a2d11a64bc9ad2cdbef5838aa7081d60fd314c44647079eae426f19dd`
- `candidateDigest`：`688a786525d8d8c988be2d517d31cca23113d788cadc3168cae5bbd71a6a1efb`
- digest domain：`hakimi/vedic-input-admission-readiness-candidate/v0.1.0`
- canonicalization：`sorted_object_keys_compact_json_finite_numbers_aliases_expanded_active_cycles_rejected_v1`

`candidateDigest` 的输入是：删除候选对象自身的 `candidateDigest` 字段后，对对象键排序并生成紧凑 canonical JSON，再计算 `SHA-256(domain + NUL + canonical JSON)`。复用的对象 canonicalizer 会把非循环共享 alias 按值展开，拒绝当前递归链上的 active cycle；因此不再用不准确的 `no_alias` 描述。该 digest 只用于候选内部的 domain-separated 完整性标识，不是数字签名，不提供签名者身份，也不建立真实性、权威性或发布安全性。

## 13 条要求与 26 个语义 invariant

13 行次序固定；每行的 `requiredSemanticInvariantIds` 是精确 allowlist，不能增删、别名化或跨行替换。

| 顺序 | requirementId | requiredSemanticInvariantIds |
| ---: | --- | --- |
| 1 | `civil_calendar_and_date` | `civil_date_validity`, `calendar_identity_version` |
| 2 | `local_wall_time_and_precision` | `canonical_wall_time`, `precision_vocabulary` |
| 3 | `birth_time_uncertainty_interval_or_candidates` | `uncertainty_representation`, `interval_ordering`, `candidate_semantics` |
| 4 | `place_coordinates_and_precision` | `coordinate_reference_system`, `coordinate_precision_semantics`, `location_privacy_boundary` |
| 5 | `iana_time_zone_and_tzdb_identity` | `iana_zone_identity`, `tzdb_version_identity`, `coverage_update_policy` |
| 6 | `dst_gap_overlap_resolution` | `dst_gap_overlap_classification`, `dst_resolution_policy` |
| 7 | `utc_conversion_and_time_scale` | `utc_conversion_consistency`, `time_scale_identity` |
| 8 | `ephemeris_identity_version_and_coverage` | `ephemeris_identity_version_digest`, `ephemeris_coverage` |
| 9 | `sidereal_zodiac_declaration` | `sidereal_zodiac_identity_version` |
| 10 | `ayanamsa_identity_and_version` | `ayanamsa_identity_version` |
| 11 | `rahu_ketu_mode` | `node_mode_definition_version` |
| 12 | `bhava_house_definition` | `bhava_definition_version` |
| 13 | `birth_time_perturbation_candidates_and_transition_points` | `perturbation_candidate_semantics`, `transition_ordering`, `perturbation_policy_identity` |

合计为 13 行、26 个 invariant ID。当前每行都保持：

- `currentState = draft_shape_defined_value_unselected_required_unbound`
- owner decision、source binding、rights evidence、expert opinion、engineering validator receipt 的计数全部为 0
- owner accepted、source frozen/verified、rights closure、expert review、engineering validation、mutation epoch protection 和 admission eligibility 的投影全部为 `false`
- 不携带 `selectedValue`，也不携带任何伪装成实际证据的 refs

因此当前是 `requirementsDefined = 13`、`requirementsResolved = 0`、`requirementsSelected = 0`、`admissionEligibleRequirements = 0`，即 `0/13`，不是“13 条已通过”。

## 8 条合取必要条件：必要但不充分

`conjunctiveReadinessConditions` 的模型类型是 `conjunctive_fail_closed_necessary_conditions_only`。下列 8 条都是未来资格评估的必要条件，必须同时为真，且必须绑定同一个冻结的 13 条要求 packet 与同一个 admission epoch；它们不是充分条件：

| 顺序 | conditionId | 权限/责任主体 | 当前 |
| ---: | --- | --- | --- |
| 1 | `all_thirteen_owner_selections_and_scope_decisions_accepted` | `product_owner` | `false` |
| 2 | `all_thirteen_source_or_first_party_provenance_bindings_frozen_verified` | `source_provenance_review` | `false` |
| 3 | `all_thirteen_work_version_carrier_rights_and_distribution_dispositions_complete` | `source_rights_and_legal_review` | `false` |
| 4 | `two_independent_domain_opinions_cover_same_frozen_thirteen_requirement_packet` | `two_independent_vedic_domain_experts` | `false` |
| 5 | `two_independent_source_rights_reviews_and_legal_authority_disposition_complete` | `independent_source_rights_review_and_legal_authority` | `false` |
| 6 | `schema_specific_structural_and_semantic_validator_receipts_complete` | `engineering_reproducibility_review` | `false` |
| 7 | `mutation_epoch_atomic_snapshot_aba_and_private_input_lifecycle_receipts_complete` | `engineering_data_lifecycle_review` | `false` |
| 8 | `owner_supersession_formal_parent_and_central_registry_projection_complete` | `product_owner_and_formal_admission_governance` | `false` |

当前 `necessaryConditionsRequired = 8`、`necessaryConditionsSatisfied = 0`、`admissionGatesSatisfied = 0`，即 `0/8`。部分完成不能使 gate 为真；`conditionSkippingAllowed = false`；不得自动晋级、不得多数表决、求平均或由生成模型选择值、选择意见赢家。未决或有歧义的证据处置是 `not_ready_no_product_receipt`。

候选明确固定：`conditionsAreNecessaryNotSufficient = true`、`allConditionsRequiredForFutureEligibility = true`、`allConditionsAloneConferEligibility = false`、`positiveTransitionEvaluatorImplemented = false`、`positiveReceiptSchemasDefined = false`、`positiveTransitionReceiptsAccepted = 0`、`futurePositiveTransitionsMechanicallyAccepted = false`、`additionalVersionedTransitionAndReceiptContractRequired = true`。因此即使未来 8 条全部满足，也不能由本候选得出“已具资格”，更不能得出正式体系准入、产品可用或发布授权。

## Node 与隔离浏览器 Worker 只算工程前置

### Node 固定四 probe

已绑定的 Node child 只记录四个固定、无人数据的 diagnostic probe：

- `diagnosticProbeExecutions = 4`
- `acceptedInputs = 0`
- `inputInstances = 0`
- `productInputRejectionReceipts = 0`
- `structuralValidatorComplete = false`
- `inputRejectionCapabilityEstablished = false`
- `countsTowardAdmission = 0`

这四个 probe 不能代表任意候选输入，不能代表产品输入拒绝链，也不能建立完整结构 validator 或语义 validator。

验证 CLI 对 Node launch state 的边界也只是 visible-only：实际启动时可以拒绝仍可见的 `NODE_OPTIONS`、`NODE_PATH` 和 `process.execArgv`，但这不证明启动前没有执行过随后被擦除痕迹的 preload/loader。`erasedPreloadExcluded = false`，已加载模块字节、Node loader、launcher、runtime intrinsic 的身份与完整性也没有建立。可见 launch state 干净不能晋级为可信运行时来源证明。

### 隔离浏览器 Worker

浏览器侧只绑定隔离、私有、非产品 package 的当前 endpoint 快照：18 个 source endpoints、6 个 build endpoints、1 个说明文档 endpoint 和 1 个工程 registry endpoint。候选没有证明历史浏览器执行，也没有为吠陀产品 surface 建立 browser runtime evidence；输入实例、产品拒绝回执和生产 export 都是 0，`countsTowardAdmission = 0`。

Node 与浏览器两组材料只能证明工程前置结构的存在，不能替代 owner、来源、权利、专家、validator、epoch、产品或发布证据。

## 34 个 upstream endpoint identity

候选以“当前 endpoint snapshot 指向候选”的单向绑定记录 34 个上游身份：

| 类别 | 数量 | 内容 |
| --- | ---: | --- |
| 治理与准入上下文 | 7 | 独立产品 ADR、输入 schema draft、13 条要求 baseline、来源与三层权利要求、双专家计划、历史产品化 parent 要求、version-aware observation candidate |
| Node 固定 probe child | 1 | `vedic-input-structural-rejection-execution-evidence.v1.json` |
| 浏览器工程 source endpoints | 18 | package/config/README、browser app、e2e、server、protocol/preflight/worker/client 及其测试和样式源文件 |
| 浏览器工程 build endpoints | 6 | 当前隔离 build 的 HTML、JS、source map 与 CSS 输出 |
| 浏览器工程说明文档 | 1 | 隔离浏览器 Worker 草案说明 |
| 工程 registry | 1 | system contract draft registry |
| **合计** | **34** | 每个 endpoint 都在候选中固定 `path + bytes + sha256`；带语义 digest 的治理记录同时固定其 digest 字段名和值 |

34 个 endpoint 的规范性逐项身份位于候选的 `upstreamBindings` 中；本文不复制出第二份可漂移的 hash 清单。绑定方向为 `exact_current_endpoint_snapshots_to_candidate_only`：上游没有 backlink，本候选也不改写上游。

静态候选的自我投影保持保守：`candidateHashAndParseUseSameReadBuffer = false`、`upstreamHashAndSemanticInspectionUseSameReadBuffer = false`、`heldFileHandleReads = false`。这些 `false` 表示静态 JSON 本身不证明某次 loader 执行已经发生，不是否认 loader 实现具备局部读取保护。

当前 loader 实现若被实际调用，会对每个文件分别打开并持有 handle，核对打开前后 endpoint/目录链，对同一 held-handle buffer 计算 raw hash，并让需要语义解析的 endpoint 从该同一 buffer 解析与重算 digest。这能形成“逐文件 individually held-handle”的非原子 endpoint-set observation；34 个文件以批次分别读取，不是 simultaneous 或 cross-file atomic snapshot，也没有 parent/children atomic snapshot。`mutationEpochAvailable = false`，没有批次 mutation epoch、interval mutation exclusion 或 ABA exclusion。因此逐文件读取保护与 34 个当前 hash 匹配都不能被提升为原子闭包、epoch 证据或 admission receipt。

## 角色与证据分账

各账必须由对应责任主体独立形成，工程记录不能替代非工程判断：

1. `product_owner`：13 项取值、范围和采用决定。
2. `source_provenance_review`：来源或第一方定义的可靠底本、版本、正文和 locator 冻结。
3. `source_rights_and_legal_review`：作品层、版本/编辑层、载体层的权利材料与分发处置审查；source-rights reviewer 无权签署最终 rights/legal conclusion。
4. `two_independent_vedic_domain_experts`：身份、资质、独立性及对同一 frozen packet 的两份意见；分歧并列保存；domain expert 无权签署 rights/legal conclusion。
5. condition 5 需要两份相互独立的 source-rights review，再由与二者分离的独立 `legal_authority` 作法律权限处置；这些角色不能合并为一个自签主体。
6. `engineering_reproducibility_review`：schema-specific 结构与语义 validator 回执。
7. `engineering_data_lifecycle_review`：mutation epoch、atomic snapshot、ABA 排除和私有输入生命周期回执。
8. `product_owner_and_formal_admission_governance`：supersession、formal parent 与 central registry 投影。

工程证据、内容真值、专家真值、来源与权利法律结论、产品运行时证据、发布就绪和公开授权继续分账。生成模型无权填补空席、模拟签字、选择冲突意见赢家或把一种证据转换成另一种权威。

### 独立法律角色仍为空缺

`roleSeparationBoundary` 把此前容易混淆的法律权限缺口单列：

- `domainExpertMaySignRightsLegalConclusion = false`
- `sourceRightsReviewerMaySignRightsLegalConclusion = false`
- `independentLegalAuthority.roleDefinition = absent`
- `independentLegalAuthority.seatCount = 0`
- `independentLegalAuthority.eligibilitySchemaDefined = false`
- `independentLegalAuthority.signingSchemaDefined = false`
- `independentLegalAuthority.authenticitySchemaDefined = false`
- `conditionFiveBlocked = true`
- `rightsLegalConclusionEstablished = false`

因此 condition 5 当前不仅是“尚未提供两份 review”，还因独立 legal-authority 的角色、席位、准入资格、签署和真实性 schema 全部缺失而被阻断。domain expert 或 source-rights reviewer 的意见无论数量多少，都不能填补这个签署权限缺口。

## Privacy 边界

候选明确保持以下隐私投影：

- `arbitraryCandidateInputAuthorized = false`
- `candidateInstancesIncluded = 0`
- `personalDataProcessedByThisCandidate = false`
- `candidateDigestIsAnonymous = false`
- `candidateDigestSafeToPublish = false`
- `fixedProbeReceiptsSafeOnlyBecauseNoPersonData = true`
- `stableDigestForArbitraryCandidateForbiddenByGovernance = true`
- `stableDigestMechanicallyBlockedForArbitraryCandidate = false`

固定 probe 的安全性仅来自其不含个人数据，不能推广为任意出生数据可处理、可稳定哈希或可公开。对任意 candidate 建立稳定 digest 目前是治理禁止，不是 hash API 的机械禁令：底层 hash 能力仍存在，尚无通用输入层机械阻断回执。因此 `stableDigestMechanicallyBlockedForArbitraryCandidate = false` 必须保持；治理禁止不能被冒充为已实现的技术强制。候选 digest 也不得被描述成匿名化证明或发布许可。

## Same packet、same epoch 与不可跳步

所有 owner、source、rights、expert、validator、data lifecycle 和 supersession 证据必须同时绑定：

- 同一个 frozen 13-requirement packet；以及
- 同一个 admission epoch。

当前 `currentFrozenPacketDigest = null`，`currentAdmissionEpochReceipt = null`。候选本身没有 mutation epoch，也没有排除跨文件批次读取期间的修改或 ABA。任何跨 packet、跨版本、跨 epoch 拼接的“证据全集”都不能满足合取必要条件；`conditionSkippingAllowed = false`。由于 positive evaluator 和 receipt schema 均不存在，本候选根本不机械接受正向迁移；未决或歧义证据只能保持 `not_ready_no_product_receipt`。

## Version 与 supersession 边界

候选没有获得 owner 接受，也没有改动正式 parent 或 central registry：

- `candidateIsFormalParent = false`
- `supersedesBaseline = false`
- `ownerDecision = null`
- `ownerAcceptanceVerified = false`
- `formalParentUpdated = false`
- `centralRegistryUpdated = false`
- `supersessionReceipt = null`

因此本文件和候选 JSON 都不能被当作新的正式 parent、baseline、registry current record 或吠陀 release identity。后续即使补齐局部证据，也必须经过同一 packet/epoch 下的 owner supersession 与正式治理步骤，不能隐式覆盖历史 parent。

## 本候选不建立的内容

本候选明确不建立：

- owner 已选择或接受的吠陀输入政策值；
- 已准入的吠陀输入契约、产品输入实例或任意输入处理权限；
- 正向 transition evaluator、正向 receipt schema 或被机械接受的正向 transition receipt；
- 完整结构 validator、语义 validator 或产品拒绝链；
- 历法、时区、DST、UTC、time scale、星历的正确性；
- sidereal zodiac、ayanamsa、node、bhava、出生时刻扰动政策的事实或领域真值；
- 来源正文、exact quote、locator 或可靠冻结 binding；
- 作品层、版本/编辑层、载体层许可或法律结论；
- 现实专家身份、资质、独立性、意见或专家真值；
- 吠陀产品 surface 的浏览器运行时证据；
- mutation epoch、跨文件原子快照、interval mutation 或 ABA 排除；
- parent supersession、central registry 更新或吠陀 release identity；
- 正式体系准入、发布就绪、部署/回滚确认、公开部署授权、公开发布授权或高风险表达授权；
- `legacy-v13 / targetSchema 13` 或八字权威向吠陀体系的继承。

当前权威投影继续全部为 `false`，包括 `formalAdmissionAuthorized`、`releaseEvidenceComplete`、`releaseReady`、`publicDeploymentAuthorized`、`publicReleaseAuthorized`、`expertClaimsAuthorized` 和 `highRiskClaimsAuthorized`。候选 digest 正确、endpoint hash 匹配或局部工程检查通过，都不会改变这些值。

## 验证（root 于 2026-08-30 当前工作区执行）

工作目录固定为 `C:\Users\Administrator\Documents\哈基米算命`。本节只记录本候选及其固定加载器的工程验证，不扩大到全仓、完整应用或公开发布。

### 定向命令

- `node --check scripts/vedic-input-admission-readiness-candidate-lib.mjs`：exit `0`
- `node --check scripts/verify-vedic-input-admission-readiness-candidate.mjs`：exit `0`
- `node --check scripts/verify-vedic-input-admission-readiness-candidate.test.mjs`：exit `0`
- `node scripts/verify-vedic-input-admission-readiness-candidate.mjs`：exit `0`
- `node --test scripts/verify-vedic-input-admission-readiness-candidate.test.mjs`：`19/19` pass，`0` fail，`0` skipped

19 个测试覆盖：固定路径与私有品牌、严格 UTF-8/BOM/escaped duplicate key/真实尺寸上限、13 行与 26 个 invariant、必要非充分与法律角色空缺、全红自重签攻击、hostile object/Proxy/revoked Proxy/accessor/custom prototype/cycle/passive alias、candidate 与 upstream 的 missing/link/empty/oversize、Windows junction、held-handle 期间 truncate/grow/same-size replace、仍可见的 Node launch state，以及已擦除 preload 仍不可被排除的非主张边界。

### 当前身份与闭包结果

- 候选：`35325` bytes
- raw SHA-256：`7f42750a2d11a64bc9ad2cdbef5838aa7081d60fd314c44647079eae426f19dd`
- `candidateDigest`：`688a786525d8d8c988be2d517d31cca23113d788cadc3168cae5bbd71a6a1efb`
- 独立按 `domain + NUL + sorted-key compact JSON` 重算：与候选 digest 一致
- 顶层字段：精确 `23`
- requirement rows：`13`；唯一 invariant IDs：`26`
- 合取必要条件：`8`；当前满足：`0`
- upstream endpoints：`34/34` 当前 bytes/SHA-256 匹配且路径唯一；其中 `7` 个带 semantic digest 的 JSON endpoint（6 个治理上下文 + 1 个 Node fixed-probe evidence child）由与 raw hash 相同的 held-handle buffer 解析并重算
- CLI 当前只签发 endpoint-set 非原子机械观察品牌；`candidateEndpointMechanicallyObservedUnderUnverifiedRuntime = true`
- CLI 同时明确：`erasedPreloadExcluded = false`，module/loader/launcher/runtime intrinsic 身份未验证，simultaneous closure、跨文件 atomic snapshot、mutation epoch、interval mutation exclusion 与 ABA exclusion 全部为 `false`

### 通用边界检查

`npm.cmd run check:system-contract-draft-boundaries` 当前 exit `1`。输出没有本轮 readiness 新文件的 finding；它仍只列出既有问题：受限文件被明确跳过、`bazi-expert-review-packet-lib.mjs` 的既有未注册依赖，以及三个既有测试文件的 non-literal dynamic import。该结果不能登记为通用边界通过。

本轮没有运行全仓 typecheck 或默认 Web build，没有读取受限文件，也没有重新执行完整应用、PWA/Service Worker、Chrome/Edge、公开主机或部署/回滚矩阵。先前隔离浏览器 Worker 只作为 34 个 endpoint 中的工程前置身份被绑定，不是本轮新的浏览器运行证据。

上述通过只进入本候选的工程验证账；它不进入内容真值、专家真值、来源权利法律结论、产品运行时证据、发布就绪或公开发布授权账。
