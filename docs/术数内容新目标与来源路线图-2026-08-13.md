# 术数内容新目标与来源路线图

> 日期：2026-08-13
>
> 适用范围：八字主 Web、隔离紫微 4218、隔离西洋 4219
>
> 目标：让排盘后的内容更直接、更有上下文、更容易由用户／专家纠正，同时不把竞品文案、古籍定位或工程摘要冒充真值

## 1. 用户目标的产品化表达

用户希望的不是“再多放一些百科文字”，而是排盘后立即回答：

1. **盘里实际有什么**：十神、神煞、星曜、宫位、相位和组合事实；
2. **它具体落在哪里**：年／月／日／时柱，本宫／对宫／两组三合，天体的星座／宫位／相位；
3. **在这个位置关注什么**：直接说主题、可用端、受阻端和矛盾；
4. **什么条件会改变结论**：身强身弱、同柱关系、三方四正、四化、宫主星、定位星链、其他相位；
5. **当前能不能下结论**：事实、候选、已审、未审、冲突或不可判断必须分开。

“直言不讳”在本项目中的标准是：**不使用空泛套话，但必须把依据、成立条件、反例和未知项一起直说**；它不等于“注定”“一定发财”“必有疾病”等无法负责的确定事件。

## 2. 统一内容模型

三体系以后都按相同的四层输出，不强行混用规则：

| 层 | 用户看到什么 | 工程要求 |
| --- | --- | --- |
| 事实层 | 位置、关系、亮度／角距／orb、命中规则 | 来自当前已验真盘面；不可由内容文案反推或补猜 |
| 直读候选 | “这个位置主要观察什么” | 来源绑定、自写短文、禁止复制竞品长文；结果字段为空 |
| 条件合参 | 资源端、压力端、相互牵制、反例 | 保留冲突，不按项目数量直接加权，不跨体系推导 |
| 审稿状态 | 未审、批准、退修、拒绝及审稿归属 | 绑定当前快照；无签名／身份核验时不得自动激活 |

每条可显示主张以后统一登记：

`claimId / claimType / sourceType / exactLocator / traditionScope / factInputs / applicabilityConditions / counterexamples / prohibitedOutcomeClaims / reviewStatus / expertTruthClaimed / scientificValidityClaimed`。

## 3. 当前已经具备的内容基线

| 体系 | 当前覆盖 | 当前最重要缺口 |
| --- | --- | --- |
| 八字 v0.18 | v0.17 的 69 项目录与当前盘命中包；新增 8 来源、12 个精确 binding、12 条旺衰 claim，以及按月令→透干→首藏→余藏展开的当前盘证据叙事；定向门与双浏览器 2/2 已收口 | 旧 69 项目录的非旺衰来源仍未全部获得同等级逐句 locator，十神“偏有利／需警惕”也不能冒充专家吉凶；未重跑整仓、Schema 16 长矩阵或 Android |
| 紫微 v0.13（定向工程门已收口） | v0.12 的 14 主星×12 宫=168、4 本命四化×12 宫=48、三方四正四段式、12 核心辅煞×12 宫=144；新增当前整盘 12 reviews／标准 48 occurrences 动态审稿合同；adapter 2/30、三个 typecheck、108-module build、应用内 Browser 与 Edge／Chrome 2/2 已通过 | 旧 4218 长故障矩阵、整仓、Firefox、Android 与专家验收未重跑；不能把 v0.12 历史数字当作 v0.13 结果 |
| 西洋 v0.7 | 43 个基础基元；命主星、宫主星、定位链、四轴距离、10 张逐星综合、日月上升命主星首读；动态候选数为 `50 + 实际主要相位数` | 只提到“尊贵”概念，尚无七传统星体本质尊贵事实表与当前盘 occurrence |

三体系现有所有候选仍不能直接称为专家真值。默认生产只包含八字主 Web；紫微 4218 和西洋 4219 仍是隔离草案。

## 4. 执行路线

### P0：八字 v0.17（历史基线）

已完成：

- 纠正旧 4/2/1 文字漂移，统一为月令 4、透干 2、首位藏干 2、其余藏干 1；
- 建立当前盘实际命中复核包；
- Web 支持显式准备、下载、具名反馈只读预检；
- 篡改、错盘、切 Revision 与异步迟到失败关闭；
- Edge + Chrome 2/2，完整浏览器存储前后相等；默认身份仍为 legacy-v13。

详见 `八字内容-v0.17-旺衰政策单一事实源与当前盘命中复核包-2026-08-13.md`。

### P1：八字 v0.18——来源—主张账与旺衰证据叙事层（已定向收口）

当前源码已经形成独立 claim registry、逐项证据叙事、Web 只读账本，并把其摘要绑定进 v0.18 当前盘复核协议。本轮新执行 v0.18 定向 Vitest **4 files / 27 tests PASS**、默认身份定向 **3 files / 23 tests PASS**、根 typecheck、草案隔离 verifier、**2058 modules** E2E build，以及 Edge + Chrome **2/2 PASS**；没有借用 v0.17 数字。

已形成的最小范围：

1. 把当前盘旺衰判断展开为月令、四柱透干、四柱首位藏干、其余藏干的逐项证据账；
2. 显示每项的五行关系、权重、支持／消耗方向和月主气重复计权影响；
3. 为每句叙事绑定 `claimId`、流派、来源 locator、条件与反例；
4. 只生成“为什么当前候选落入本档、哪些输入会令它跨档”的说明；
5. 与 v0.17 当前盘包共用快照绑定，但不自动采用审稿结果；
6. 未知时辰只用三柱，所有时柱相关事实明确 withheld。

通过线已在本轮定向范围内关闭：同一事实与政策确定性；跨阈值边界可复演；来源／主张错配失败关闭；用户可定位每句话来自哪条事实；完整浏览器存储前后相等；跨 Revision 失效关闭；无个人吉凶和事件结果。应用内 Browser 在最新壳桌面与 390×844 显示 12 claims / 12 unique bindings、月主气 `4 + 2 = 6`，四个共享 SHA 一致，overflow／overlay／console warning-error 均为 0；E2E build 为 `bc2c28f15407`，默认身份仍是 legacy-v13/schema13。完整证据、截图摘要和版权边界见 `八字内容-v0.18-来源主张账与旺衰证据叙事-2026-08-13.md`。

### P2：紫微 v0.13——核心十二辅煞 × 三方四正 occurrence 动态审稿包（定向工程门已收口）

建议合同：`ziwei.core_minor_star.sanfang_occurrence_review/0.1`。

每条真实 occurrence 只绑定：

- 精确星键、标签与 Hakimi 事实类别；
- `self / opposite(+6) / trine(+4) / trine(-4)`；
- 宫位、地支、实际亮度与本命生年四化；
- 基础候选 ID、落宫候选 ID；
- 当前规则快照、事实快照、有序 occurrence 与来源摘要；
- “天空／地空”等命名冲突状态。

禁止按六吉／六煞、星数、亮度或四化加总；不借对宫补空宫；禄存、天马、天空、其他 minor 与全部 auxiliary 仍只显示事实、候选为 `null`。`orientation / eventOutcome / result` 继续为空。

当前源码把范围收紧为一份 `current_chart_all_twelve_sanfang_groups` 整盘包：12 个目标组；标准盘十二个核心事实各进入四组，共 48 occurrence。固定顺序为 `self → opposite(+6) → trine(+4) → trine(-4)`。五来源只复用既有核心星曜 2、宫位 2 与三方几何 1，没有新增星义语义来源；切宫只过滤显示，编辑、重算、重开或失败会使旧包失效。模板排除直接身份与原始输入，但派生四宫／星曜／亮度／四化仍属敏感事实，SHA-256 不是加密。v0.13 与旧静态反馈面板隔离，formal／scoring／outcome 均保持 `false / null`。本轮定向 Vitest **2 files / 30 tests**、三个 typecheck、隔离边界、默认身份 **3 files / 18 tests**、4218 build **108 modules transformed**、应用内 Browser 和 Edge／Chrome 独立场景 **2/2** 均通过；完整存储快照、390px 响应式及两浏览器同字节截图已核对。详细证据与未扩大的长门边界见 `紫微内容-v0.13-核心十二辅煞三方四正命中复核包与证据-2026-08-14.md`。

### P3：西洋 v0.8——七传统星体本质尊贵 occurrence 事实包

建议合同：`western.essential_dignity_occurrence_review/0.1`。

首版只做：

- 七颗传统可见星体；
- 热带黄道；恒星黄道请求明确 unsupported；
- 入庙、擢升、失势、落陷四种星座级 occurrence；
- 天体、精确经度、星座、黄道模式、来源表版本、表摘要、当前盘事实摘要和有序 occurrence 摘要绑定；
- 传统／现代守护星表不混合；
- 不做尊贵点数、强弱排名、互容、吉凶或事件。

三分、界、面要等专家选择昼夜规则、三分主体系和 Ptolemaic／Egyptian terms；角宫、顺逆、速度、日光关系属于偶然尊贵，应另立后续事实层，不能混成一个“星体强弱分”。本目标也不要求把 Astronomy Engine 换成 Swiss Ephemeris。

### P4：条件化内容版本（依赖现实审稿）

只有 P1～P3 的来源账和反馈包稳定后，才逐批推进：

- 八字：十神 × 柱位 × 当前旺衰条件；神煞 × 柱位 × 同柱十神，但不让旺衰直接决定神煞吉凶；
- 紫微：主星／核心辅煞 × 宫位 × 三方四正 × 本命四化；
- 西洋：天体 × 星座 × 宫位 × 主要相位 × 本质／偶然状态。

每一批都必须由用户或专家给出流派、成立条件、反例和具体退修意见；“全部 approve”本身仍不构成正式发布授权。

## 5. 八字来源路线

| 来源 | 用途 | 不可声称 |
| --- | --- | --- |
| [周易天地八字排盘入口](https://www.zhouyi.cc/bazi/sm/) | 观察阳／阴历、时间、地点、真太阳时、姓名／性别等输入 IA | 本轮未提交动态表单，不能声称实测其结果页或算法 |
| [周易天地日主旺衰文章](https://m.zhouyi.cc/bazi/sm/43679.html) | 观察“得时／得势／得地—生扶或克泄”的内容语法 | 页面自身承认量化法不严谨；不能证明 4/2/2/1 或复制其文案 |
| [十神落四柱示例](https://m.zhouyi.cc/bazi/sm/21128.html) | 观察十神 × 年月日时的内容颗粒度 | 现代编辑文案不是原典、共识或确定事件 |
| [驿马规则示例](https://m.zhouyi.cc/bazi/sm/14460.html) | 暴露年支／日支查法等流派冲突，促使公式显式版本化 | 不能静默扩张当前已实现基准 |
| [知己排盘](https://zhijipaipan.com/) | 竞品功能层级与排盘后阅读顺序 | 未验证计算、内容权利或准确性 |
| [《滴天髓阐微》oldid 2600158](https://zh.wikisource.org/w/index.php?title=%E6%BB%B4%E5%A4%A9%E9%AB%93%E9%97%A1%E5%BE%AE&oldid=2600158) | `通神论 > 十五、月令；十七、衰旺`：月令重要且须合看全局的传统语境 | 固定社区转录 revision 不等于影印校勘；不支持 4/2/2/1、阈值或科学有效性 |
| [《三命通会》卷五 oldid 2706483](https://zh.wikisource.org/w/index.php?title=%E4%B8%89%E5%91%BD%E9%80%9A%E6%9C%83/%E5%8D%B7%E4%BA%94&oldid=2706483) | `卷五 > 论古人立印食官财名义`：十神相对生克语境 | 不支持固定吉凶、当前支持／需求权重或个人事件 |
| [《三命通会》四库本卷十 oldid 761703](https://zh.wikisource.org/w/index.php?title=%E4%B8%89%E5%91%BD%E9%80%9A%E6%9C%83_(%E5%9B%9B%E5%BA%AB%E5%85%A8%E6%9B%B8%E6%9C%AC)/%E5%8D%B710&oldid=761703) | `卷十 > 看命口诀；玉井奥诀 > 物须提豁方明轻重`：月支、其余柱、透藏和配置合看 | 社区转录不等于四库影印逐字核验；不支持固定首藏主气或数字权重 |
| [《渊海子平》oldid 2593607](https://zh.wikisource.org/w/index.php?title=%E6%B7%B5%E6%B5%B7%E5%AD%90%E5%B9%B3&oldid=2593607) | `论天干地支暗藏总诀；又地支藏遁歌` 的藏干列示候选 | 页面明确不完整且来源未核；locator 为 pending，相关 claim 必须 withheld，不能声称首位必为主气 |
| [《子平真诠评注》CText chapter 974137](https://ctext.org/wiki.pl?chapter=974137&if=gb&remap=gb) | 第九、十三、十四、十八、十九篇仅用于挂起成败救应、气候、破格／成格复核门 | revision 与原文／评注层未冻结，只链接和定位，不支持算法、喜忌、个案结论或正文再分发 |

4/2/2/1、五档阈值和六个敏感性场景的精确来源只能是本仓 `strength-policy.ts` 与 `strength-sensitivity-review.ts`；外部古籍只提供传统问题域和历史语境，不能为项目参数“镀金”。竞品内容按现代版权对待：只学习信息结构和问题拆分，不复制长段文字、表格、图片或自动批量抓取。

作品层、数字载体层与转录准确性必须分开：古籍可能属于公版，不代表 Wikisource／CText 的数字文本自动可任意复制。Wikisource 转录复用须逐页核对适用许可、署名、相同方式共享与修改说明；CText 当前只外链和人工定位。v0.18 的 locator `contentSha256` 仍为 `null`，所以 pinned `oldid` 也不能冒充影印校勘摘要。

## 6. 紫微来源路线

| 来源 | 用途 | 不可声称／权利边界 |
| --- | --- | --- |
| [sǹg-miā 算好命](https://sng-mia.com/ziwei) | “固定排盘事实 → 白话报告”的两阶段 IA | 未提交表单，不确认算法或结果页；不复制产品文案 |
| [紫宸天机排盘](https://www.yinchen.tw/zh-TW/www.ziwei-lens.tw/chart) | 主星／六吉六煞／杂曜、四化、运限的视觉分层 | 颜色和标签不是吉凶真值；不复刻布局或文案 |
| [react-iztro](https://github.com/SylarLong/react-iztro) | 开源 UI 的宫位、星曜、四化与三方线机制参考 | MIT 只覆盖仓库代码，不授权外部内容、截图或品牌 |
| [锁定 iztro 提交](https://github.com/SylarLong/iztro/tree/9d39f1743bf31c2b3c635c9b9556215d9c90ee2c) | 当前 2.5.8 工程行为和不同流派配置边界 | 锁定提交不等于唯一正确流派；浮动主干不能替代基线 |
| [iztro 三方四正基础](https://docs.iztro.com/learn/basis) | 现代“本宫＋对宫＋两组三合”的术语交叉说明 | 现代材料不是古籍底本或专家签署 |
| [《紫微斗数全书》卷一](https://zh.wikisource.org/wiki/%E7%B4%AB%E5%BE%AE%E6%96%97%E6%95%B8%E5%85%A8%E6%9B%B8/%E5%8D%B7%E4%B8%80) | 主星、辅煞、四化、庙陷与制化 locator | Wikisource 转录 CC BY-SA；高风险古代断语不直接呈现 |
| [《紫微斗数全书》卷二](https://zh.wikisource.org/wiki/%E7%B4%AB%E5%BE%AE%E6%96%97%E6%95%B8%E5%85%A8%E6%9B%B8/%E5%8D%B7%E4%BA%8C) | 十二宫安星、十干四化和三方四正口诀 locator | 需处理异体字、错字和版本分歧 |
| [日本国立公文书馆明刊本影像](https://www.digital.archives.go.jp/file/1078787) | 核对转录、四化口诀、星名及天空／地空版本 | 底本载文不证明流派唯一或预测真值；记录册次／叶码 |

## 7. 西洋来源路线

| 来源 | 用途 | 不可声称／许可边界 |
| --- | --- | --- |
| [Astro.com Natal Chart Wheel](https://www.astro.com/faq/fq_fh_owtype_e.htm) | 观察星盘轮、天体、宫位、相位线／表与元素分布 IA | 默认 Placidus/orb 是产品选择，不是统一标准；不复制报告 |
| [Astro-Seek Birth Chart](https://horoscopes.astro-seek.com/birth-chart-horoscope-online) | 观察宫制、相位、orb、传统／恒星黄道扩展设置 | 未提交动态表单，不确认结果文案或计算差分；不批量采集 |
| [Astrodienst House Ruler](https://www.astro.com/astrowiki/en/House_Ruler) | 宫头守护星 → 宫主星落座／落宫／定位关系术语 | 不裁决传统／现代冲突，也不提供结果权重 |
| [Astrodienst Angular Planet](https://www.astro.com/astrowiki/en/Angular_Planet) | 靠近 ASC/DSC/MC/IC 的上下文术语 | 没有统一合轴 orb，不能自动等价为“强” |
| [Skyscript Dignities](https://www.skyscript.co.uk/glossary/dignities/) | 区分本质尊贵与偶然尊贵，规划事实字段 | CC BY-NC-SA；商业使用前须许可审查，不搬运文字／表格 |
| [《Tetrabiblos》目录与卷一](https://sacred-texts.com/astro/ptb/ptb02.htm) | 相位、守护、三分、擢升、界、面等传统 locator | 单一译本不代表后世全部传统；需核对版本与公版状态 |
| [Astronomy Engine 官方仓库](https://github.com/cosinekitty/astronomy) | 当前同类引擎的天体位置／坐标工程能力与精度范围 | 天文位置精度不等于占星内容有效；继续锁项目 2.1.19 身份 |
| [JPL Horizons 官方文档](https://ssd-api.jpl.nasa.gov/doc/horizons.html) | 未来官方向量字节和坐标／中心差分的权威工程参考 | 当前仍未取得本项目冻结查询的官方响应字节；不伪造通过 |
| [Carlson 1985 双盲研究](https://doi.org/10.1038/318419a0) | 科学有效性边界和产品免责声明 | 不能被占星内容条目当作语义支持来源；只承担研究边界角色 |

Swiss Ephemeris 可作为以后独立数值差分候选，但其 AGPL／商业双许可和引擎迁移成本必须先评估；本内容路线不授权替换当前 Astronomy Engine。

## 8. 用户／专家如何参与

推荐协作方式不是在聊天里一次性给“最终答案”，而是每批 10～30 条：

1. 系统导出当前盘复核包；
2. 用户／专家填写流派、条件化方向、理由、成立条件、反例和补充来源；
3. 系统只读预检当前盘绑定和字段完整性；
4. Codex 把通过预检的意见整理成**代码变更提案**，逐条对照来源、禁词和测试；
5. 现实身份／资质、内容权利和发布授权另行核验；
6. 未获授权时永远停在候选层，不写正式规则。

用户当前最有价值的输入是：

- 八字采用哪一套旺衰／取用传统，是否接受月令与月支首位藏干分别计权；
- 五个神煞各采用年支、日支或并列异法中的哪一种；
- 紫微采用的四化／亮度／借星流派及天空／地空口径；
- 西洋本质尊贵表采用的传统版本，以及以后是否需要日夜、三分主、界和面。

## 9. 不随内容扩展而解除的边界

- 默认发布保持 `legacy-v13 / targetSchema 13 / migrationId null`；
- v15/v16 不因内容增加而晋级；
- 不绕过 mutation epoch 或以 raw IndexedDB 写入伪造状态；
- 紫微／西洋不自动进入正式入口；
- 竞品、古籍、开源库、哈希、单测和浏览器通过都不等于专家真值；
- 医疗、寿命、灾祸、法律、投资、婚姻必然性等高风险结果不得由候选自动生成；
- 历史证据与每轮新证据继续分账；
- 未获用户授权不暂存、不提交、不发布。

## 10. 2026-08-24 来源审阅可见化增量

外部项目审计所吸收的“来源优先、事实与解释分层、独立材料并列保存、输出前忠实性门”已经开始落到主 Web，但本次只完成注册证据主题的本机只读查看，不改变第 2 节的内容层级：

1. 资料库只在 `evidence_subject` 目标下按需加载“本机主题来源审阅”；默认不读库，命盘侧只提供主题级跳转，不把通用主题来源冒充当前 Case/Revision/字段值的直接证据。
2. 首次三表原子只读捕获不显示；第二次只读事务必须复现同一 storage/packet/source-set 摘要，只有第二份最小投影进入页面。页面恢复可见或本页知识写入/索引重读开始会撤下结果，且明确不是 mutation epoch 或持续订阅。
3. candidate/rejected 只显示计数；只有 citation 状态已复核的 exact quote 可作为有界纯文本预览。权利双记录状态不等于两名真人身份，工程再分发门也必须按 bundled origin、作品/版本、总状态和双记录字段完整闭合后才显示。
4. 任何未来字段、访问器、危险属性键、资源超限、混合大小写深链正规化风险或摘要漂移都失败关闭；当前组件没有语义排名、分歧裁定、内容胜者、Provider 外发、公开导出或正式激活能力。

本增量的合并定向证据为 **6 files / 81 tests**，其中审阅组件 **14 tests**；组件范围临时 strict TypeScript 检查和 release governance 通过。没有运行全仓 typecheck、默认 build 或真实浏览器，应用内浏览器此前对本地地址的阻断仍未被 jsdom 替代。发布身份继续是 `legacy-v13 / targetSchema 13 / migrationId null`，`publicDeploymentAuthorized=false`、`expertClaimsAuthorized=false`。

下一步不是自动生成更多“断语”，而是先为真实人工工作流设计独立的观察输入、身份记录与并列保存合同；若要讨论当前命盘适用性，必须在 reviewer 实际查看前冻结 Case/Revision/fact/rule 上下文。完整术数内容仍需来源与许可治理、专家审定和跨体系各自独立成文。

## 11. 2026-08-24 context-bound 观察侧车增量

上一节要求的“观察前先冻结上下文”已形成第一版工程合同，但只完成空白本地文件与只读预检，不是正式人工审定系统：

1. 每份模板精确绑定 `legacy-v13 / 13 / null`、Case、Revision、注册字段、fact/rule projection、规则 Profile/规则包、来源工作集、packet/source-set 与全部 verified citation IDs；任何摘要或引用集合变化都失败关闭。
2. 每条 citation 只记录并列的 context-bound 观察，不产生评分、胜者、共识或自动激活。填写观察后必须给出自述 reviewer、时间、方法和传统范围，但现实身份、人工行为真实性、术数真值和科学有效性仍一律未核验。
3. Web 每次准备或预检都重新执行两遍仓储读取并复现页面已显示的 context digest；模板以敏感工件进入人工交付门，预检只向 React 返回计数、自述标签和摘要，不返回理由、条件、反例、URL、字段值、来源正文或生辰资料。
4. 页面生命周期、locator/subject 或知识状态变化会撤下观察结果；Storage、Case、Revision、规则包和 citation 均零写入，mutation epoch 未复核且未绕过，DeepSeek/Provider 外发、公开导出、正式激活和自动晋级保持关闭。

最终受影响回归为 **4 files / 56 tests**，临时 scoped TypeScript 检查和隔离浏览器生产依赖图打包通过。应用内浏览器使用真实 Schema 13 隔离库和真实门面完成桌面、`390×844` 窄屏、敏感模板门与两条观察预检，console 无 error/warn；这不是完整应用启动、PWA、Service Worker、迁移、公开主机或发布证据。下一步是受控逐项编辑与明确保存动作、隐私化本机持久化/撤销/导出/删除、两份独立观察和身份核验；在这些门完成前，当前侧车不能被称为真实专家审定、内容权威或公开发布授权。

## 12. 2026-08-24 纯内存逐项观察编辑器增量

第 11 节的“受控逐项编辑”已经完成一个不扩 Schema 的叶级版本，但“正式保存与审定”仍未开始：

1. reviewer/session 与每条 citation 的状态、理由、条件、反例、关联项和补充 HTTPS URL 只存在于编辑器叶组件；父面板只获得 `dirty / stale / total / observed`。本应用代码不主动持久化或传输这些字段，浏览器、操作系统、输入法和扩展仍属于应用控制之外的隐私边界。
2. 正常文件必须先通过本地字段上限，再执行新的上下文预检；回执还要精确复核 profile、binding、reviewer、计数、完整 boundary 和确切文件摘要。任何额外 authority 字段、错误摘要、观察者错配、单 URL 超 2,000 字符或文件超 512 KiB 都在交付前失败关闭。
3. 上下文失效会立即冻结而不清空脏稿；独立的未预检恢复文件保留原始输入，不能作为当前观察采用。站内导航、外链、页面离开及原生 Back/Forward 均有脏稿保护；异常 history 恢复会牺牲被拒绝物理 entry 的内容以维持 URL/UI/草稿一致，因此不宣称完整浏览历史保真。
4. 最终受影响回归为 **9 files / 114 tests**，scoped strict TypeScript 检查通过；相关生产依赖图不落盘打包为 **28 outputs / 1,913 inputs / 5,335,904 bytes**。隔离叶级 Browser 完成桌面、同源 `390×844` iframe、敏感交付、陈旧恢复和原生 Back/Forward 验证，干净会话无 console warning/error；这不是完整应用、真实仓储、PWA、Service Worker、迁移、跨浏览器或发布验证。
5. 默认身份继续是 `legacy-v13 / targetSchema 13 / migrationId null`，mutation epoch 未复核且未绕过；Storage/Case/Revision/规则包/citation 零写入。工程证据不构成真人身份、专家资格、术数或科学真值、许可结论、正式内容权威或公开发布授权。

下一步从“编辑 UI”转为独立的正式记录治理：明确保存意图、隐私化本机持久化、撤销/导出/删除、身份材料离线核验、至少两份独立观察并列保存，以及不自动选赢家的分歧处理。完整术数内容路线仍需来源与许可治理、专家审定和跨体系各自独立产品化。

## 13. 2026-08-25 双观察生命周期敏感文件增量

第 12 节所需的“两份观察并列”现已扩展到确定性 lifecycle sidecar，但仍没有开始正式保存或专家审定：

1. 两份完整观察在同一 fresh 冻结上下文上预检并按记录摘要规范排序；独立 inspection 只证明 sidecar 结构完整，始终不允许据此直接比较。只有再次匹配调用方提供的冻结 context snapshot 时，才开放该快照下的机械枚举比较；仓储来源、存储新鲜度和 return 时当前性仍未证明。
2. lifecycle recorded/withheld 事件不使用时间或随机数。本机撤下只是一条未核验 sidecar 断言，保留原记录字节，不证明 reviewer 撤回权、物理删除、旧副本召回或全局终态；多文件只能经严格前缀 reconcile，分叉事件失败关闭。serializer 不能接受普通伪造对象或 accessor 绕过严格 inspection。
3. Web 只有在用户显式点击“并列并准备敏感生命周期文件”后才执行 fresh 两遍只读仓储读取、生成并复核 sidecar。公开组件只接收 binding 与 citation 摘要，完整 canonical text 与 Blob 只停留在私有叶组件；父组件与普通比较投影都不获得自由文本、身份材料、ledger/event ID 或文件字节。同一公开组件的 locator、四个 binding 摘要或有序 citation ID/title 任一变化，都会同步重建私有叶并使旧 raw、结果、迟到任务和页面会话 Blob 失效。文件名、说明和 `blocked_sensitive` 固定，系统分享关闭；“页面会话内已准备”不表示物理 RAM 驻留、已保存或已交付。
4. 最终合并定向证据为 **6 files / 84 tests**，包层与 Web 两个 scoped strict TypeScript 检查通过；其中补有结构深度 48/49 相邻阈值、全部上下文身份维度、pending compare/prepare、既有 Blob 清除、缓存 lazy 焦点和真实 `React.lazy` Promise rejection 关闭路径。最后一次隔离 Vite 构建直接包含真实 production 叶、门面与 package 依赖图，为 **1,911 modules / 10 outputs**，仅有 Vite 选项弃用和大 chunk 警告；它不连接正式仓储，也不是默认 Web build。应用内浏览器完成桌面与 `390×844` 显式准备、普通比较、关闭焦点恢复、移动溢出和控制台检查；dialog `scrollWidth=clientWidth=358`、标题零外边距且位于 dialog 内、页面无横向溢出、分享按钮和 DOM 中自由文本 sentinel 均为 0、console warning/error 为 0。浏览器使用确定性隔离端口和 package inspection stub，不是完整应用、真实仓储、PWA、跨浏览器或发布证据；真实生命周期与门面闭合来自自动化合同测试。
5. 默认身份继续是 `legacy-v13 / targetSchema 13 / migrationId null`，没有新增 Schema/formal store，mutation epoch 未复核且未绕过。全仓 typecheck、默认 build 和发布治理本轮未重跑；工程结果不构成真人身份/独立性、撤回权威、专家资格、术数或科学真值、许可结论、正式内容权威或公开发布授权。

下一步才是应用内明确保存意图、隐私化持久化、跨副本 reconcile、撤销/导出/物理删除与旧副本不可召回提示、离线身份和撤下权威核验。完整术数内容仍需来源/许可治理、专家审定和八字、紫微、西洋、吠陀等体系分别产品化；不能通过访问对方后台、复制提示词或搬运内容来替代这些工作。

## 14. 2026-08-25 lifecycle 多副本重开、合并、撤下与回读增量

第 13 节的页面会话 lifecycle sidecar 现在可以由用户显式重新选择并机械复原，但仍没有进入应用正式存储：

1. Web 门面先对 1～16 份、单份最多 2 MiB 的文本执行严格 inspection 与同 ledger reconcile，再进行一次新的两遍上下文仓储读取。未撤下结果只有在 fresh context、记录集合和全部 binding 摘要精确一致时才返回机械比较；已撤下结果必须同样复核这些摘要，但永远返回 `comparison=null`。结果只说明确切输入数、规范输出摘要及该摘要是否与至少一份输入相同，不使用文件时间、选择顺序、多数或“最新版本”语义。
2. 本机撤下只接受五种有限理由并生成确定性 terminal successor；理由不同可以占用同一事件槽，但事件摘要必须不同，跨副本出现这种分叉时以 `EVENT_FORK_DETECTED` 整体失败关闭。回读只证明重新选择的文件能复现预期 canonical sidecar 摘要，不证明下载字节逐字节相同、文件已持久化、旧副本已召回或现实撤回权成立。
3. 新工作台只在用户显式打开后 lazy 挂载。原始输入、canonical 内容和 Blob 保持在私有叶组件；父组件只传冻结 binding 与 citation ID/title，且 locator、四个 binding 摘要、有序 citation、页面生命周期、关闭或卸载任一变化都会撤下旧会话与迟到结果。交付固定为 `blocked_sensitive`，系统分享关闭；工作台业务逻辑不调用 Storage、Web Storage 或数据网络接口，但首次 lazy 挂载仍可能像普通应用代码一样请求同源静态 chunk，不能把它表述为整页物理零网络。
4. 本轮合并定向验证为 **6 files / 95 tests**，scoped strict TypeScript 检查、`check:web-storage-import-boundary` 与 `check:release-governance` 均通过。隔离 Vite 生产依赖图为 **1,933 modules / 12 outputs**，只有既有 directive／大 chunk 警告；它不是默认 Web build。应用内浏览器使用真实工作台和真实本机交付对话框、确定性 facade/platform 隔离端口，完成桌面导入两副本、fresh-context reconcile、有限理由撤下、交付回执、重新选择摘要回读，以及 `390×844` 布局和会话清除。撤下态为 `data-comparison-ready=false`，原始 sentinel 与分享按钮均为 0，工作台 `scrollWidth=clientWidth=349`，页面无横向溢出，console warning/error 为 0；这些浏览器证据没有连接真实仓储。
5. 默认身份继续是 `legacy-v13 / targetSchema 13 / migrationId null`，治理输出仍为 `publicDeploymentAuthorized=false / expertClaimsAuthorized=false`。本轮没有正式 store、自动保存、跨刷新重开、真实 Schema 13 附件往返、完整应用 typecheck/default build、PWA/Service Worker、跨浏览器或公开主机验证；mutation epoch 未复核且未绕过。工程闭环不构成真人身份、专家资格、撤下权威、来源许可、术数/科学真值、正式内容权威或公开发布授权。

下一步应把“保存、隐私告知、按摘要重开、撤下 successor、逐副本 CAS 删除、原字节导出、完整备份往返和旧副本不可召回提示”作为一个不可拆分的 `LocalAttachment` 垂直切片；在这组治理能力一起完成前，不增加一个会把敏感观察静默写入本机库的孤立按钮。

## 15. 2026-08-25 lifecycle LocalAttachment 显式治理闭环增量

第 14 节要求的垂直切片现在已作为专用敏感 `LocalAttachment` 用途整体落地，但它仍不是正式专家记录库：

1. Storage 新增 0～16 个 exact unlinked source 的一次性创建与完整身份 CAS 删除。创建入口在首次异步操作前对输入、字节和来源身份做 descriptor-safe 捕获；只接受本 realm 的普通 `Uint8Array`，先用内部槽检查拒绝 Proxy，再用捕获的原生构造器与 `set` 做不读取 caller `constructor`／`Symbol.species` 的复制。全部 source 在同一附件写事务内二次核对完整元数据、hash 与 `link=null`；dedupe 未命中时才在 `add` 前原子执行同 MIME 行数、encoded characters、exact-purpose 数量和 matched declared bytes 四门。容量错误是有限 typed code，事务会整体回滚；旧 `createAttachmentOnce` 语义不受专用 purpose 容量门影响。
2. Web adapter 固定专用 MIME 与 description，返回不可变回执并精确区分“已有相同原字节”与“新增记录”。只有真实 Storage typed 容量错误能作为 prewrite/noncommit 处理；伪造同码或未知写错误保持 reconciliation required。目录、重开、原字节读取与删除都使用 exact identity；保存回执同时证明容量准入发生在原子事务中、是否真实新增以及 source 是否在创建时再次 CAS，不能由普通错误对象伪造。
3. 私有 lazy 工作台不在 mount 自动读库。目录只显示未验真的有限元数据；用户必须显式读取、选 1～16 份、fresh-context 重开后，才可对该页面会话中的 exact copies 导出或删除。保存要求五项确认；删除要求四项确认和精确短语 `删除此本机副本`。跨叶 singleton coordinator 持有 writing、未认领完成回执和 reconciliation-required 状态，父级关闭／重挂不能消除未知写结果；typed prewrite 拒绝只在已证明没有存储 mutation 时撤销锁。通用附件页同时保留该 MIME/description，不能借普通上传、下载或删除入口绕过专用治理。
4. 完整备份集成使用两个随机 Schema 13 数据库做真实 snapshot → envelope/serialize/preflight → prepare/apply restore 往返；附件 ID、时间、完整身份、`contentBase64`、原字节和 hash 精确恢复，恢复后可经 adapter 目录与 fresh-context 重开再次验真。备份形成后删除源库附件不会改写既有 snapshot/envelope/serialized bytes。这里同时意味着专用附件会进入未加密明文完整备份；删除当前 origin 的记录/数据库字节不能召回旧备份、旧导出、其他 profile/device 或 OS 残留，也不证明底层介质物理擦除。
5. 最终主线实跑分为 Storage **19 files / 248 tests** 与 Web **7 files / 96 tests**，全部通过；生产切片和隔离入口 scoped strict TypeScript 均通过，`check:web-storage-import-boundary` 与 `check:release-governance` 通过。隔离 Vite production 依赖图为 **1,920 modules / 20 bundle outputs**，另有仅用于窄屏测量的静态壳；只有既有 Vite 选项弃用和大 chunk 警告。它不是默认 Web build。
6. 应用内 Browser 在随机 targetSchema 13 库完成桌面真实闭环：内存文件复核 → 显式保存 → 原子目录刷新 → stored reopen → 生成并保存本机撤下后继 → 两副本重开 → exact 原字节交付到内存端口 → 两次完整身份 CAS 删除 → 新目录确认 **0**。testing reopen 为文件 1 次、存储 2 次；交付捕获 14,923 bytes 且 SHA-256 与原附件一致。桌面 `clientWidth=scrollWidth=1265`。独立 `390×844` 静态壳执行文件选择与 fresh reread reconciliation 后，文档 `clientWidth=scrollWidth=375`、main `clientWidth=scrollWidth=359`；两个私密 sentinel、分享按钮和当前 preview console warning/error 都为 0。窄屏没有重复保存／删除全流程，不能冒充第二次完整闭环。

默认身份继续是 `legacy-v13 / targetSchema 13 / migrationId null`，没有新增 Schema/formal store，mutation epoch 未复核且未绕过。全仓 typecheck 与默认 build 仍因既有禁读模块阻塞而未运行；临时 harness、构建产物、窄屏壳、随机数据库和预览服务器均已清理。以上只证明本轮敏感工件治理的工程闭环，不证明 reviewer 现实身份/独立性或撤回权威、来源许可、专家资格、术数/科学真值、正式内容权威、发布就绪或公开发布授权。

## 16. 2026-08-25 报告逐句工程忠实度路线增量

在内容忠实度、离线身份材料绑定和跨刷新 operation intent 三条候选路线中，本轮先选择内容忠实度：后两条在 `legacy-v13` 且不新增正式 store 的约束下只能形成结构材料绑定或非权威 reconciliation hint，不能被包装成现实身份、独立性或持久操作权威。

1. 单盘报告格式提升到 `1.5.0`，新增不可由调用方注入的 `interpretationEvidence`。它把同一冻结 Revision 的规范 Envelope 投影成 7 个家族、27 条逐句账，并保留 fact/rule/source/stability/conflict、来源定位覆盖、缺失证据与工程理由；完整模式 12 条最小 binding，匿名模式零 binding/payload，DST 未决零句失败关闭。
2. Web 只消费严格报告模型，正文与规范渲染器精确匹配；withheld 正文永不进入 DOM 或 Markdown。任何枚举、顺序、计数、摘要、匿名泄漏或边界不符都会整份报告失败关闭；这只保证工程投影未被自由改写，不保证叙事正确。
3. 实际 Browser 像素验证暴露并修复 `content-visibility:auto` 导致的长图离屏遗漏。PNG 专用克隆强制展开内容并采用紧凑三列逐句账，最终为 `1080 × 14357`；干净完整/匿名/Markdown/打印会话无 warning/error，系统 Chrome `390×844` 无横向溢出且零 Web Storage/IndexedDB。
4. 定向回归为 **3 files / 104 tests**，scoped strict TypeScript、离线 lockfile dry-run、依赖审计、release governance 与隔离 build 通过。治理仍是 `legacy-v13 / targetSchema 13 / migrationId null / publicDeploymentAuthorized false / expertClaimsAuthorized false`，mutation epoch 未绕过；全仓 typecheck、默认 build 和完整 PWA 仍未执行。

下一阶段的内容优先级是：为当前 12 条 binding 补精确 locator、固定作品/版本与权利状态；让至少两份经离线身份材料核验的真人观察继续并列保存；任何分歧保持显式，不多数表决、不平均、不自动选赢家。紫微、西洋、吠陀等体系继续各自独立成文，不能把八字工程叙事跨体系复用为权威结论。

## 17. 2026-08-25 报告来源资格快照路线增量

第 16 节的 12 条 binding 已进入 `1.6.0` 机械来源资格快照，但只完成工程治理的第一段：

1. 完整模式列出 8 个唯一来源与 12 条 binding；注册表当前有 10 个 locator 通过结构复核、7 个固定版本。locator 资格、结构化 citation、分发权利和来源正文分别计量，当前后三者均为 0；不得把 oldid、URL、版本固定、SHA-256 或测试通过解释成许可、逐字核校或内容真值。
2. 匿名模式只保留 8／12／10／7 逻辑计数，不含来源定位或 binding 明细。包层以整份报告 exact-instance 登记防止 clone／移植绕过，Web 以有界自有 passive snapshot 防止同身份校验后修改；二者均失败关闭，但不宣称可隔离任意恶意同 realm Proxy。
3. 定向 **6 files / 140 tests**、scoped TypeScript、5 项治理检查和隔离 Vite build（181 modules）通过。应用内 Browser 完成完整、匿名、Markdown 和长 PNG；PNG-only 克隆展开默认收起的本地摘要，14,785 CSS 高度按画布约束输出 `1196 × 16384`，47／47 个语句、来源和 binding 区域均被绘制。摘要区为 37.6875 CSS px、15 种采样颜色；移除摘要的对照图高 14,716，同区 1,482 个采样字节不同。交付文案将 1080 明确为 CSS 布局宽度。补充 `390px` Edge 无横向溢出和运行期 error，最终全新 Browser 标签页无 warning/error。该隔离证据不包含完整应用启动、正式仓储、PWA／Service Worker、公开主机，也不验证内容语义。
4. 默认身份继续是 `legacy-v13 / targetSchema 13 / migrationId null`，mutation epoch 未复核且未绕过；`publicDeploymentAuthorized=false / expertClaimsAuthorized=false`。全仓 typecheck 与默认 build 仍未执行。

下一阶段不是把“公开 GitHub 项目已充分吸收”写进结论，而是逐条补齐 12 条 binding 的结构化 citation、许可／权利依据和可核校来源正文，再由至少两份经离线身份材料核验且彼此独立的真人观察并列审阅。Renhuai、dzcmemory、Sudo、Vedic 与 Horosa 当前只形成 clean-room 工程模式吸收；紫微、西洋、吠陀等体系仍需各自的来源治理、专家审定与独立产品化，任何分歧不得多数表决、平均或自动选赢家。

## 18. 2026-08-25 单盘报告 v1.7 引用／权利机械准入阶段

第 17 节所列的下一段工程合同已推进到 `1.7.0`：核心机械准入已落盘并完成定向复核。它没有把外部正文自动装入报告，也没有把 Citation 或 SourceRights 记录提升为内容、身份或法律真值。

1. 12 条 binding 与显式 `evidenceSubjectId` 一一闭合；只有活动的 `interpretive_claim` 且 `requiredForV1=false` 的注册主题可进入本报告合同。Citation target 必须精确绑定当前 Revision 的 `chart_field` 和该 subject，新增、删除、交换或伪造 subject 都失败关闭。
2. `KnowledgeDocument` 的实际内容 SHA-256、章节范围、quote、locator 和 Citation 声明必须精确闭合。报告内 `C#` 依规范引用顺序生成，`D#` 按文档首次出现连续编号；同一 `D#` 必须复现完整来源与权利投影而不只比较 `documentId/contentHash`，摘要只按唯一目标、文档和权利记录计数。
3. 每条 binding 的机械状态区分无可用记录、candidate、rejected、verified-private 和 verified-redistributable。redistributable 只有在精确文档版本、受限枚举、双条独立 reviewer 记录与分发字段共同满足合同时成立；blocked、未知、跨文档或被篡改的记录不能升级。
4. `sourceIdentityStatus` 固定为 `not_assessed`，`citationTargetEstablishesSourceIdentity=false`，准入账不复制来源正文。完整报告以 `evaluationStatus=evaluated` 标记实际已执行的机械评估；DST 扣留解释时固定为 `not_evaluated_interpretation_withheld` 且计数全零。匿名报告只保留固定 redacted 边界，不泄露动态准入状态、subject、文档或权利细节。
5. `1.7.0` 有独立冻结 golden（SHA-256 `aba357d98a99271cc9a9b2727ad1c7c516424c94b7cf34f765cb38b0e9a74b29`）；本轮合并定向回归为 **10 files / 269 tests**，scoped strict TypeScript、三项治理边界、离线 lockfile dry-run、隔离 Vite build 及应用内真实浏览器 full／anonymous／`390×844` 通过。全仓 typecheck、默认 build、完整应用、PWA、跨浏览器和公开主机未验证，不能由这些定向证据外推。

这一阶段关闭的是“记录能否与当前 binding、冻结文档和权利记录机械相接”的工程问题，不是“该命理主张是否正确”或“该材料是否依法可用”的终局判断。12 条 binding 若要进入正式内容路线，仍须逐条取得并冻结可靠底本／版本、明确作品层与载体层许可、核校 exact quote，并保留不能再分发时的 private／link-only 边界；公开仓库许可证不能替代古籍、讲义、转录层、数据集、模型、字体和依赖的单项治理。

专家路线仍须验证现实身份、资质和彼此独立性，由至少两份并列意见处理语义适用性与冲突，不以自动计数、多数表决、平均或生成模型选赢家。跨体系产品化仍须把八字、紫微、西洋和吠陀各自的输入、事实层、规则版本、来源、许可、专家审定与高风险边界独立建设；不得把八字 v1.7 的机械合同或公开仓库的工程模式外推为其他体系的内容权威。

发布身份保持 `legacy-v13 / targetSchema 13 / migrationId null`，mutation epoch 未绕过。以上工程合同不构成术数／科学真值、真人专家审定、来源身份确认、权利法律结论、正式内容权威、发布就绪或公开发布授权。

## 19. 2026-08-29 PR10B／PR10C 概念范围对账增量

第二轮深审的后九个概念不能直接作为当前九条 binding 继续推进。深审原始清单与推荐分批还给出了两套冲突顺序，账本分别保留 `originalTopicOrder` 与 `recommendedBatchOrder`，后续执行采用推荐分批序。claim registry 当前 `sourceBindings` 精确为 7 条工程候选、4 条历史文本候选和 1 条 review gate；九个旧 topic 与 current binding ID 精确命中为 0。本轮新增独立、可重建的 scope reconciliation 账本和 verifier，防止旧审计分解与当前 12 条范围混账。

1. PR10B 的季节生产、季节制约、季节收藏、根气表和透干表在锁定 scoped 工件中均没有专属 current binding 或完整规则表。season production/control 除无季节参数的通用 relation 外，整体管线还存在月令输入、月柱位置和工程加权；这些 core／policy binding 仍只是非等价近邻。experimental 藏干事实与可见天干出现同样不能变成表值来源。相邻历史证据已精确到 quote candidate ID 并保持 `link_only / not_established`，仍不能替代表内逐值来源。
2. PR10C 的两项位置衰减仍有概念歧义。当前权重函数只接收 factor group 与藏干列表索引，不接收年／月／日／时柱位，不能把 hidden-stem index weighting 改称 pillar-position decay。
3. 两项 score topic 只映射到现有七条工程候选 binding，不新增平行 identity；固定 `project_engineering_heuristic_candidate_projection / observational_engineering_candidate_only / formalScoringAllowed=false / freezeEffect=none`。当前锁定的 scoped 工件中没有一等 score-meaning 字段，仓库范围不存在性未建立。
4. topic→binding 与相邻来源的选择是 repository-authored 范围判断，不是机器自动建立的语义等价，也未绑定用户附件原字节或获得领域专家批准。机器只验证预设投影、精确 12 条注册 inventory 与 scoped 当前字节一致。新账本定向 `11/11`、C 合并切片 `57/57` 测试通过；scoped 规则表观察数 `0/5`、仓库范围不存在性未建立、逐值来源 bundle `0`、新建平行 binding `0`、冻结 `0/12`。它未接入正式 lifecycle、八字 domain manifest 或四体系 registry，也没有重签既有中央闭包。

后续 PR10B 必须先由人确认表模型、流派与适用范围，再逐值取得来源或明确第一方派生 provenance，并补齐作品／版本／载体三层权利和两份独立真人意见。后续 PR10C 必须先定义“位置”和 score 的产品语义，再决定是否扩展现有 binding；不能从旧深审 topic 名机械生成算法或权威。

详见 [八字 PR10B／PR10C 概念与当前 Binding 对账](./八字PR10B-PR10C概念与当前Binding对账-v1-2026-08-29.md)。默认发布治理继续固定为 `legacy-v13 / targetSchema 13 / migrationId null`，Schema 13 不伪造 mutation epoch，`publicDeploymentAuthorized=false / expertClaimsAuthorized=false`。本增量只建立工程范围真值，不构成浏览器／运行时证据、内容真值、专家真值、权利法律判断、发布就绪或公开发布授权。

## 20. 2026-08-29 PR10C 七条工程 Binding 值级缺口增量

在第 19 节完成旧 topic 与当前 binding 范围对账后，本轮继续拆解现有七条工程候选本身，而不启动 PR10B 表模型或制造新 binding。独立账本从当前 producer／consumer 与 lockfile 重建精确 33 个值级候选：core 8、factor inclusion 4、direction 4、weights 3、month duplication 4、thresholds 4、sensitivity 6。它们全部是 repository-authored 的 pre-freeze 缺口单元；当前 binding inventory 与所评估 binding EvidenceSubject registry 中均未观察到这些 ID 的正式登记，而全仓 registry inventory 未评估。全部候选保持 `valueProvenanceFrozen=false / bindingFreezeEligible=false / freezeEffect=none`，当前仍为 `0/33` 可冻结、`0/12` binding 已冻结。

机械检查暴露出六项不能被整文件摘要替代的问题：`factorInclusion` 与 `monthMainDuplication` 在 scoped factor／sensitivity source 中没有具名 AST 引用，但不建立动态或别名数据流不存在性；`hour_range` 在交互面板与报告／本地 AI／evidence envelope 之间存在 includeHour 冲突；三个 legacy sourceRef 与当前 binding ID 没有精确命中，本门也未建立可审查语义 crosswalk，而全仓 crosswalk inventory 未评估；12 个正式 EvidenceSubject 的 algorithm／field／rule-profile locator 仍为空；`lunar-typescript@1.8.6` 只能闭合 lockfile metadata，本门未核验已安装包或 `ZHI_HIDE_GAN`／`SHI_SHEN` 表字节、来源或权利；专家候选包仍因 `bazi-core` artifact lock 漂移失败。本轮没有为这些冲突选择赢家、重签专家包或把 package metadata 提升成传统内容与分发权利。

新 verifier `11/11`，与前三私有 Exact Quote、来源候选、三层权利、冻结准备和 PR10B／PR10C 对账合并为 `6 files / 68 tests`，全部通过；五个 C 账本 CLI 与 release governance 也通过。直接文件各自的 hash 与 parse／AST／稳定可执行语法检查使用同一 Buffer；除专门绑定的 includeHour、authority null 与 literal table 外，不建立一般控制流、数据流或运行时等价。嵌套 verifier 会重读，跨文件不是原子快照；Schema 13 没有 mutation epoch，仍不排除区间 mutation 或 ABA。本门没有建立全仓 mutation set，也不机械评估正式 lifecycle 或中央 manifest 是否在其他工作中发生变化。

下一步应先由产品与领域负责人明确 `hour_range`、两处声明的配置地位和 legacy sourceRef 迁移策略，再分别取得上游表值 identity、来源／派生 rationale、作品／版本／载体权利和至少两份独立真人意见。只有这些闭合后，才可讨论正式 EvidenceSubject locator 与 binding freeze；不能让模型从当前 33 项自动生成 PR10B 传统规则表。

详见 [八字七条工程 Binding 值级 Subject 缺口账](./八字七条工程Binding值级Subject缺口账-v1-2026-08-29.md)。默认身份保持 `legacy-v13 / targetSchema 13 / migrationId null`，`publicDeploymentAuthorized=false / expertClaimsAuthorized=false`。本增量不构成浏览器／运行时证据、内容真值、专家真值、权利法律判断、发布就绪或公开发布授权。

## 21. 2026-08-29 D0 八字 v1.7 机器 Manifest 漂移归属决策增量

D 阶段不能在当前字节上直接填写真人意见或冻结 RC。保存的八字 manifest 自身摘要仍为 `60711da42ebc71e1f23ec300dffd5ed44f418c9bcc83ee394bf106f7354dc932`，但按当前 manifest builder 以相同 `createdAt` 重建的 expected preview 已随 Phase C／D 工程补强变为 `be637b1e37ae92a9fd047af28d0117334405a39408dc470c766b7ddb8ab96954`。该 preview 没有写回 domain manifest，也不是新的权威机器身份。

独立 D0 账本当前精确冻结 4 个组件／7 个组件－文件差异条目（6 个唯一物理路径）：`bazi-core/src/index.ts` 属于 `fact_contract`；live source auditor 与 readiness trust-chain 文件属于 `source_bundle`；随包 audit、根 `package.json` 与同一 readiness 文件属于 `rights_bundle`；专家候选包 verifier 属于 `high_risk_policy`。七项的变化作者、所有者归属、语义接纳和重绑定决定均保持未建立；定向修复记录不自动转化为 manifest 重绑定授权。`bazi-core` 另使专家候选包形成一处 artifact drift，两个现实专家席位仍全部为空，`expert_review_bundle` 仍为 `absent/0`。

新 verifier 要求保存 manifest 当前精确以 `MANIFEST_MISMATCH` 失败、专家候选包精确以 `ARTIFACT_DRIFT` 失败；若出现额外漂移、摘要变化、伪造 owner decision、重签许可、RC 晋级或旧 manifest 被替换，D0 自身失败关闭。D0 tests 为 `12/12`；账本摘要为 `5b82b545a2c5b0cd5714342c1bdaca742f4de44e196cc32817e8310926850d0f`。专家候选包 verifier 以同一 artifact Buffer 完成锁摘要与 ledger parse；D1 为仓外原始意见建立 held-handle／同 Buffer seal 与 opaque context，D2 再为 identity dossier 建立双摘要 opaque context，并把 caller workspaceRoot 固定到 verifier 模块项目根。独立审计复现并修复假根 P2 后，零实例、七类 intake、两类仓外端点与 bundle graph 定向 tests `47/47`；self-digest、seal 与 dossier SHA 仍不建立真实性、first-seen 或 custody。正式 packet 未重签，仍红。D0 的 hash／parse 只对保存 manifest 与专家包各自复用同一 Buffer；nested builder/verifier 会重读，跨文件不是原子快照，Schema 13 无 mutation epoch，区间 mutation／ABA 仍未排除。

详见 [八字 v1.7 机器 Manifest 漂移归属决策账](./八字v1.7机器Manifest漂移归属决策账-v1-2026-08-29.md)。默认发布治理仍为 `legacy-v13 / targetSchema 13 / migrationId null`，两项公开／专家授权为 false。本增量没有改生产语义、重签 manifest／专家包、接入正式 lifecycle 或建立真人专家、内容、权利、浏览器、发布就绪和公开授权证据。跨体系比较的自声明 frozen-facts 缺口继续另立红账，未由本增量解决。

## 22. 2026-08-29 D 阶段现实专家零实例 intake 增量

现实专家工作当前只推进到零实例合同：固定 gap 账精确绑定八字 `single-chart-report@1.7.0` 的批准 packet/readiness、四题、十项独立性因素和七类候选 record。通用 bundle 引用清单不得单独通过；专用入口必须提供并交叉验证两份 identity、一个 pairwise assessment、两份 original opinion、两份 seal、完整四题 inventory、条件性 reconciliation 和 supplements。所有成功预检仍固定真实性、独立性、expert gate、expert truth、release readiness 与两项授权为 false。

当前 gap 属于 `high_risk_policy` 的传递绑定，不是 `expert_review_bundle`；现实实例为 0，candidate feedback 与 release closure collection readiness 均为 false。正式 packet 仍因 `bazi-core` 漂移而红，不能开始现实 intake，也不能把 synthetic 测试图当作真人意见。详见 [阶段 D 现实专家独立工件零实例合同与准入红账](./阶段D现实专家独立工件零实例合同与准入红账-v1-2026-08-29.md)。

## 22. 2026-08-29 E1／E2 其他体系工程事实回执与跨体系 v0.3

旧跨体系 v0.2 允许调用方虚构事实、规则身份和来源后自行重算 `contentSha256`，故 `factsFrozen=true` 只能表示载荷自洽，不能表示 producer 事实已冻结；成功对象还曾与调用方别名。本轮没有把这个缺口交给内容或专家门，而是在工程层建立独立回放闭包。

紫微和西洋分别新增体系自有 projector，并进入各自独立 manifest 的 `fact_contract`。离线 Node 证据工具使用固定合成输入直接运行当前 iztro 2.5.8 fresh-worker 和 Astronomy Engine 2.1.19 diagnostic producer，再以固定 projector 重算 7／5 个窄事实，绑定当前 manifest、`execution_rules / input_policy / fact_contract`、producer output、projector source 和 source requirement ledger。生成 registry digest 为 `27630244f1ec8bc90a3a1ad5ebd3b1fa97d0a1d09d497f747d88a27d1872e263`；紫微 receipt `22d981cd…`、manifest `fe646d35…`、来源冻结 `0/27`，西洋 receipt `9042d7bd…`、manifest `68590eb7…`、来源冻结 `0/28`。两者都保持 `engineering_replay_verified_not_admitted`，西洋也继续 `diagnostic_only / successReceiptIssued=false`。

跨体系 contract 升级为 v0.3，只接受 bundled registry 的精确事实、规则、工程依据与 receipt reference。descriptor-safe 有界快照会在零调用下拒绝 getter，成功值脱离输入并递归冻结；伪造事实即使重算 envelope／receipt／registry 摘要、复制另一体系 receipt 或提权仍失败。无评分、无加权、无多数表决、无模型仲裁、无自动人物合并和无概念等价推断继续固定；观察仍是人工未审定，不产生赢家。

四体系没有因此合账：八字仍因 D0 保存 manifest 漂移而排除；保存的四体系 registry 当前 verifier 首错为八字 `MANIFEST_MISMATCH`，机器回执还逐项冻结紫微 manifest、西洋 manifest、Release Evidence Schema 和 v0.3 contract 共 4 个额外陈旧锁，中央 registry 本轮未重建或重签，首错不是唯一漂移。本段保留 E1／E2 当时的吠陀点时状态：已有独立 input／fact／rule 三套 draft 与 requirements，父账计 3 个 standalone draft；正式 input／fact／rule contract 均未 admitted，fact value／producer／projector、rule evaluator／versioned ruleset、产品 surface 仍 absent，当时 `bindingRequired=null`；其当前 replacement 见第 29 节。工程回执 registry 为 `fail_closed_partial_two_of_four`，正式跨体系比较未授权。Schema 13 无 mutation epoch，跨文件和双体系 replay 非原子，区间 mutation／ABA 未排除。

历史 E 切片的回执对抗曾为 `9/9`，但不能继续冒充当前工作区。本次重跑 cross-system engineering receipts 为 `6/9`，三个正向闭包测试以 `REGISTRY_MISMATCH` 失败；system admission registry 为 `1/9`，八字 domain manifest 为 `2/3`，首错均为当前八字 `MANIFEST_MISMATCH`。紫微／西洋独立 manifests 为 `5/5`，release governance `193/193`，D0 `12/12`。中央 registry 与回执没有重签，这些红数是当前失败关闭证据。v0.3 浏览器未运行，旧 v0.2 8/8 不复用。详见 [跨体系工程事实重放回执与 v0.3 准入边界](./跨体系工程事实重放回执与v0.3准入边界-v1-2026-08-29.md)。这些工程结果不建立内容真值、专家真值、权利法律判断、发布就绪或公开授权。

## 23. 2026-08-29 C 阶段来源／权利信任链与随包预读门

当前 readiness 不再只消费来源／权利候选账的局部 gate 字段：它从同一批 held-handle、有界 Buffer 解析候选账，先运行完整来源 verifier 和完整三层权利 verifier，再消费 Binding 投影，并直接以这些 Buffer 生成 basis bytes／SHA-256。basis 与 persisted readiness ledger 均拒绝最终文件 symlink／hardlink，最多读取 `maxBytes + 1`，读取端点发生变化即失败关闭。跨文件仍不是原子快照，Schema 13 无 mutation epoch，区间 mutation 与 ABA 不排除。

随包 build gate 在任何正文读取前先完成严格 manifest、路径、作品／版本／载体权利和完整库存准入；逐级目录链拒绝 junction，正文拒绝 hardlink，以 held handle 有界读取并用同一 Buffer 做实际正文哈希，全部正文完成后重扫库存。当前 v2 manifest 仍为空，没有新增正文。`SourceCarrierRecord` 是既有正式合同；C-M1 另把 carrier → 项目原始副本 → 固定 UTF-8 归一化 → KnowledgeDocument hash 写成零实例技术物化要求，但不是第四个权利层，也没有创建正式记录。

当前机械值仍为 4 个来源候选、5 个载体观察层、7 个归一化候选、1 个非专家 exact-glyph 观察候选、正式 `SourceRightsRecord=0 / SourceCarrierRecord=0 / materialization=0`、可再分发来源 0、Binding `0/12`、现实专家 `0/2`。C-M0 私有材料与既有 C 切片为 `54/54`；C-M1 为 `23/23`，合计 `77/77`。随包 audit／knowledge-core另为 `2 files / 28 tests`，scoped strict TypeScript 通过。详见 [阶段 C 来源／权利信任链与随包材料预读门](./阶段C来源权利信任链与随包材料预读门-v1-2026-08-29.md)与 [项目物化副本／SourceCarrierRecord 分层合同](./阶段C项目物化副本与SourceCarrierRecord分层合同-v1-2026-08-29.md)。本轮未联网、未运行完整浏览器、默认构建或全仓 typecheck，不建立内容、专家、许可法律、发布就绪或公开授权证据。

## 24. 2026-08-29 D1 仓外原始意见端点与 Opaque Context

D1 为未来有权接入的两份仓外原始意见建立端点门：显式 `privateRoot` 只相对调用方提供的 `workspaceRoot` 判定仓外；路径任意位置的 ADS、hardlink、symlink、junction、特殊端点与目录链替换失败关闭。原始 SHA-256、严格 JSON parse 与 seal 字段核对复用同一 held-handle Buffer。成功只返回深冻结脱敏 context，完整 opinion／seal 留在模块私有 WeakMap；伪造、spread／structuredClone／JSON clone、重复、Proxy、accessor 和稀疏数组均不能进入 bundle-from-context。

完整 bundle graph 仍重新核对 session、review purpose、两席、pairwise independence、四题、分歧／reconciliation、supplement DAG 与形成时序。定向 tests `38/38`，但现实实例仍为 `0/2`；机械绑定不证明作者、真实性、first-seen、custody、身份、资质、彼此独立性、专家真值或权限。详见 [阶段 D 仓外原始意见文件端点与 Opaque Context](./阶段D仓外原始意见文件端点与OpaqueContext-v1-2026-08-29.md)。

D1 的 `38/38` 是当时字节身份。D2 已增加 identity dossier 双摘要 context，并把 workspaceRoot 绑定固定项目根；当前完整定向测试为 `47/47`。详见 [阶段 D 仓外身份 Dossier 端点与 Opaque Identity Context](./阶段D仓外身份Dossier端点与OpaqueIdentityContext-v1-2026-08-29.md)。

## 25. 2026-08-29 吠陀独立产品化零实例要求账 runtime/bundle 点时

本节保留来源／权利 requirements-only child 接入前的点时身份。吠陀父产品化账当时精确绑定独立 input／fact／rule 三套 draft 与 requirements，并新增绑定 [独立运行时与体积量化提案回执](./吠陀独立运行时与体积量化提案回执-v1-2026-08-29.md)，形成八层单向 DAG，所有 child 均无 parent backlink。该点时状态为 `input_fact_rule_contract_drafts_and_runtime_bundle_proposal_present_research_only_not_admitted`；`productArtifactsPresent=3` 仍只计三份 standalone schema 草案，三份 requirements 与 runtime/bundle proposal 都是治理证据，不计产品工件。product surface、fact value／producer／projector、rule evaluator／versioned ruleset、domain manifest 与 source ledger 仍 absent。

七项 re-review 在该点时为 2/7 complete：第一项只在结构材料层为 `complete_structural_drafts_present_research_only_not_admitted`，第二项只表示 runtime/bundle proposal 的治理材料存在；`rereviewTriggered=false`。提案枚举两个互斥候选 option、18 项尚未批准的 ceiling 与结构化 tradeoff，`observedMeasurementCount=0`；运行时仍未选择、未实现、未运行、未量测、未验证，`reviewsComplete=false`，安全／隐私／可复现性／许可／法律复核均未完成。五项 dependency 只列待决前置条件，不回答代码或数据许可、商业使用或再分发问题；涉及重新评审第 5、6 项的内容仅为 interface plan，不使独立存储／备份／恢复／回滚设计或独立浏览器门／Release Evidence 设计完成。

八类 gate 仍为 0/8 satisfied：input、deterministic facts 与 versioned ruleset 仍分别停在草案、零实例／零 producer、零规则集／零 evaluator 边界，其余正式 gate 也未通过。bindingRequired=null、bindingFrozenVerified=0、experts=0/2 与所有 authority／release false 不变。父账没有接中央 registry、根 package/lifecycle、UI 或 domain manifest，也不继承 legacy-v13／Schema 13 或八字权威；项目默认仍是 `legacy-v13 / targetSchema 13 / migrationId null`，吠陀自身 `releaseIdentity/targetSchema/migrationId` 继续为 null。

该点时 proposal 独立定向 48/48、八层合计 240/240，默认隔离与显式 `--test-isolation=none --test-concurrency=1` 两种模式均通过；parent 定向为 26/26。独立审计最终 P1/P2=0，发现的 `__proto__` 键与 complete coverage 两个 P2 已修复。CLI 绿色字段仍只是局部机械闭包，不是产品准入信号；这些测试不建立浏览器、默认 build、实际 runtime、任何产品 receipt、来源／权利、专家、发布或 mutation epoch、跨文件原子、interval mutation、ABA 证据。详见 [吠陀独立产品化零实例要求账](./吠陀独立产品化零实例要求账-v1-2026-08-29.md)与 [吠陀独立运行时与体积量化提案回执](./吠陀独立运行时与体积量化提案回执-v1-2026-08-29.md)。

## 26. 2026-08-29 吠陀独立输入合同未决要求

吠陀现有独立 JSON Schema 草案 hakimi.vedic.input/0.1-draft：contractVersion 加 13 项要求一一覆盖，禁止 default／examples／外部 ref，且不含任何现实、样例或合成人物输入。input requirements 13 项均为 draft_shape_defined_value_unselected，每项只绑定草案 property pointer；requirementsDraftCovered=13 但 requirementsResolved=0，selected/default 与实例／来源／权利／专家 refs 仍为空。accepted、rejected、fact receipt 和 success receipt 都为 0，其中 rejected 0 不表示执行能力。

input requirements 只绑定 ADR 与 input draft，不反向绑定父账。input draft 21/21、input requirements 33/33，合计 54/54。独立复核发现的 builder 可变旧摘要、canonical getter/-0、CLI 泛化 ok，以及 raw `Uint8Array` parser 读取可覆写 `length` 的 P2 均已修复；当前 raw 边界零 trap 拒绝 Proxy，使用内部 typed-array 槽，拒绝 SharedArrayBuffer／Resizable ArrayBuffer／detached view，并复制私有快照后解析。最后复验无剩余 P1/P2。该闭包未进入中央 registry 或 build，也不建立吠陀事实值、来源、许可、专家真值、正式产品或发布权限。详见 [吠陀独立输入合同未决要求与零实例回执](./吠陀独立输入合同未决要求与零实例回执-v1-2026-08-29.md)。

## 27. 2026-08-29 吠陀独立事实合同未决要求

吠陀现有独立 JSON Schema 草案 hakimi.vedic.facts/0.1-draft，覆盖 observation_instant_fact、graha_position_facts、lagna_fact 与 bhava_facts 四个 fact family。四族 coverage 单列为 non_requirement_fact_family_coverage_summary；fact requirements 是 12 项 scoped open prerequisite inventory，requirementsDraftCovered=12、requirementsResolved=0、requirementsUniverseClosed=false，不能把 4 族重复计入 12 项，也不能把当前 inventory 外推成完整事实要求宇宙。fact/value/producer/projector instances 与 failure/fact/success receipts 全部为 0，状态为 fact_contract_draft_present_zero_instances_values_producer_projector_absent_not_admitted。

fact draft 精确绑定 ADR、input draft 与 input requirements；fact requirements 再绑定这三项与 fact draft，且不反向绑定 parent。当前 fact draft 24/24、fact requirements 28/28，合计 52/52；绿色字段仅为 factContractDraftClosureVerified 与 factRequirementsClosureVerified。独立审计复现并修复 raw `Uint8Array` parser 读取可覆写 `length` 导致子类 getter 执行与 `maxBytes` 绕过的 P2，最终定向复核未见剩余 P1/P2。该机械闭包不建立事实值、计算能力、内容真值、来源／权利、专家真值、中央准入、发布就绪或公开授权，也不证明跨文件原子、interval mutation、ABA 或 mutation epoch。详见 [吠陀独立事实合同未决要求与零实例回执](./吠陀独立事实合同未决要求与零实例回执-v1-2026-08-29.md)。

## 28. 2026-08-29 吠陀独立规则合同未决要求

吠陀现有独立 JSON Schema 草案 hakimi.vedic.rule-contract/0.1-draft，只定义 ruleDefinitionEnvelope 与 rulesetEnvelope 的结构身份位。它要求 school、适用范围、冲突、反例、来源、版本、摘要、事实依赖、权利、专家／高风险与实现／失败通道等 13 项 prerequisite 均显式出现，但当前 13/13 blocked、0 resolved、requirementsUniverseClosed=false。D1、D9、D10、Vimshottari、Chara Karaka、Shadbala 与 Ashtakavarga 只在 excludedUnreviewedCandidateMethodIds 中各出现一次，不是已选方法或权威内容。

rule draft 精确绑定 ADR 及此前 input／fact 四层；rule requirements 再绑定 rule draft，且不反向绑定 parent 或中央 registry。rule candidate／definition／instance、ruleset／evaluator／implementation 与 failure／rule／success receipt 全部为 0，全部 authority 与 release 位为 false。当前 rule draft 26/26、rule requirements 34/34，合计 60/60；默认隔离与显式同进程串行均通过。绿色字段仅为 ruleContractDraftClosureVerified 与 ruleRequirementsClosureVerified；它不建立规则正文、内容真值、来源／权利、专家真值、可执行规则集、准入或发布权限，也不证明跨文件原子、interval mutation、ABA 或 mutation epoch。详见 [吠陀独立规则合同未决要求与零实例回执](./吠陀独立规则合同未决要求与零实例回执-v1-2026-08-29.md)。

## 29. 2026-08-29 吠陀独立来源 Binding 与三层权利点时 replacement（专家计划接入前）

当前 [独立来源 Binding 与三层权利要求账](./吠陀独立来源Binding与三层权利要求账-v1-2026-08-29.md)作为与 runtime proposal 独立的 sibling，由 [父产品化聚合账](./吠陀独立产品化零实例要求账-v1-2026-08-29.md)单向绑定；child 不反向绑定 runtime、parent 或中央 registry。父账当前状态为 `input_fact_rule_contract_drafts_runtime_bundle_proposal_and_source_rights_requirements_only_inventory_present_open_universe_zero_bindings_research_only_not_admitted`。当前 scoped minimum 精确为 input 13 + fact 12 + rule 13 = 38，故 `bindingRequirementsInventoryDefined=true / bindingRequired=38`；requirements universe 保持 open，冻结 binding 为 0。runtime proposal 的五项 dependency 是另一组待决运行时前置条件，不包含在 38 项来源／权利最低库存中，也不得重复计数。

来源／权利 re-review 第 3 项仍为 partial，整体保持 2/7 complete，`rereviewTriggered=false`；八类正式 gate 为 0/8，`productArtifactsPresent=3` 仍只计 input／fact／rule 三份 standalone draft。来源正文、exact quote、locator、冻结 binding、作品／版本／载体三层权利证据、许可结论、现实专家与法律判断仍未建立；内容真值、浏览器、默认 build、实际 runtime、Release Evidence、发布就绪与公开授权全部为红。项目默认继续为 `legacy-v13 / targetSchema 13 / migrationId null`；吠陀自身 `releaseIdentity/targetSchema/migrationId` 为 `null/null/null`，不得继承默认 v13 或八字权威。

当前机械结果为 child 103/103、parent 37/37、九个吠陀文件合计 354/354，默认隔离与显式 `--test-isolation=none --test-concurrency=1` 两种模式均通过。三项已修复 P2 分别是 closure 攻击精确错误码路由、三层权利 version 术语统一、raw bytes 解析前私有快照复制；最终剩余 P1/P2=0。该机械闭包没有 mutation epoch，不证明跨文件原子快照，不排除 interval mutation 或 ABA；中央 registry 未重建或重签，第 22、25 节的 expected-red、broader aggregate 与旧计数继续保留其历史点时含义。

## 30. 2026-08-29 吠陀两席现实独立专家审阅材料契约当前 replacement

当前 [两席现实独立专家可核验审阅计划](./吠陀两席现实独立专家可核验审阅计划-v1-2026-08-29.md)只单向绑定 ADR、input／fact／rule 六件与 source-rights child；它不绑定 runtime、parent 或中央 registry，[父产品化聚合账](./吠陀独立产品化零实例要求账-v1-2026-08-29.md)随后单向投影该 child。计划精确覆盖 38 subjects／八问同题集，定义两席 Vedic-specific 资格、五维 scope-to-role mapping、两席外部核验、主证据与独立佐证不双计、十因素独立性、same-upstream 分账、private dossier／original opinion custody、同包同题集、分歧并列、三类 append-only lifecycle 与 16 项启动门。

材料契约完成只把 re-review item 4 提升为 complete，父账为 3/7、`rereviewTriggered=false`；现实专家仍 0/2，身份／资质／scope fit／独立性／审阅包／意见／review 实例均为 0，启动门 0/16，八类正式 gate 0/8。`planCoverageMeaning` 明确不建立 expert gate、内容／专家真值或权利法律结论；`productArtifactsPresent=3` 仍只计 input／fact／rule 三份 standalone draft。项目默认继续为 `legacy-v13 / targetSchema 13 / migrationId null`，吠陀自身仍为 `null/null/null`；中央 registry 未重建或重签，浏览器、默认 build、完整应用、PWA、公开主机、Release Evidence、部署／回滚和公开发布授权均未建立。

当前 child 84/84、parent 39/39；十个吠陀 test files 在默认隔离与显式同进程串行两种模式均为 440/440、0 fail／skip。早先九文件 354/354 与 parent 37/37 只保留为点时证据。当前逐文件 held-handle／same-buffer／端点重验仍没有 mutation epoch，不证明跨文件原子快照，不排除 interval mutation 或 ABA。

## 31. 2026-08-29 吠陀运行时依赖公开许可观察当前 replacement

当前新增 [运行时依赖公开一手许可观察 evidence child](./吠陀运行时依赖公开一手许可观察回执-v1-2026-08-29.md)，记录五个公开端点的十次 HTTP 200 读取；四组立即二读稳定，一组 Astrodienst 动态页面不稳定。五类候选依赖只覆盖四类，runtime proposal 的 refs 精确为 `1/3/3/1/0`，loopback 仍为 0。证据采用 link-only policy，不保存来源正文；公开可访问、响应 hash、未签 Professional 合同模板或 WHATWG 平台规范都不等于真实性、精确版本、许可完成、合同生效、再分发授权或浏览器实现通过。

child 只绑定 ADR 且无 proposal／parent／registry 回链；runtime proposal 保持原七节点线性链，并以正交单向 closure 聚合 child。父账只消费更新后的 runtime projection，不把 child 计作 sibling 产品材料、来源 binding 或中央登记。当前 parent 仍为 `productArtifactsPresent=3`、38/0 binding、open universe、3/7 materials、`rereviewTriggered=false`、0/8 gates、0/2 experts；runtime 未选择、未实现、未测量，所有 build／runtime／browser／success receipts 为 0。

机械验证当前为 evidence child 25/25、runtime 53/53、parent 40/40；11 个吠陀 test files 在默认与 `--test-concurrency=1` 两种模式均为 471/471。此前 440/440、354/354、240/240 均降为历史点时。八字 manifest 2/3、中央 registry 1/9、cross-system receipts 6/9 继续按既有身份漂移预期红；release governance 193/193 通过，中央登记未重建或重签。

本阶段只推进“真实公开读取回执→问题矩阵”的工程闭包，没有完成 runtime／分发路线决定、SBOM／NOTICE、精确 binary／data files、作品／版本／载体权利依据、现实法律复核、内容或专家真值、浏览器／完整应用／PWA、Release Evidence、部署与回滚。默认仍为 `legacy-v13 / targetSchema 13 / migrationId null`；吠陀自身为 `null/null/null`，两项公开授权继续 false。逐文件证据没有 mutation epoch，也不证明跨文件原子、区间无 mutation 或 ABA 排除。

## 32. 2026-08-29 紫微 HKO 公农历边界来源候选增量

当前新增 [紫微 HKO 民用公农历边界来源证据准入](./紫微HKO民用公农历边界来源证据准入-v1-2026-08-29.md)：六个既有 2023—2028 年度响应通过独立 child parser 重放为 2,192 日、74 个边界对与 5 个跨年 seam，并精确映射 `ziwei.engineering.official-calendar-differential`。它只支持 `calendar_resolution` 的工程候选，不支持节气、早晚子、四柱、真太阳时、紫微规则或解释真值。

紫微 requirements 当前只有 1 条 partial candidate／27 required，仍为 `0/27` frozen；exact quote、三层权利、专家意见、来源真实性与法律结论均未建立。紫微 source bundle 仍 incomplete、manifest 仍 draft／Schema null；西洋仍零 candidate。中央 registry 与跨体系 receipts 没有改签并继续 exit 1 失败关闭。两组 22/22（含 candidate identity 复用与 basis 同缓冲断言）、HKO adapter 4/4、定向 boundary fixture 3/3 和 release governance 193/193 只是局部机械证据，不建立浏览器、完整应用、PWA、发布或公开授权，也不证明 mutation epoch、跨文件原子、interval mutation 或 ABA 排除。

## 33. 2026-08-29 D 阶段现实专家公开候选预筛增量

当前新增 [阶段 D 八字现实专家公开候选预筛](./阶段D八字现实专家公开候选预筛-2026-08-29.md)，把四个公开候选线索按方法、领域、理论与实践／表达方向登记。所有对象仍是 `uncontacted_public_candidate_lead`：没有联系、同意、身份／资质／四题 scope、两两独立性或意见核验，正式专家仍为 `0/2`，也没有权威 JSON 或 reviewer slot。

公开论文、著作、课程、协会和机构页面只能支持候选发现，不能自动证明现实身份、专业资格、适合本项目四个旺衰问题或彼此独立。正式外联、私人 dossier、同意、席位与原始意见接入均须另行授权并等待 12 条 Binding、来源与权利前置门真实闭合；两份意见必须并列，分歧不得多数表决、平均或由生成模型自动选赢家。

## 34. 2026-08-29 西洋 JPL Horizons 来源候选真实性缺口

当前 [西洋 JPL Horizons 来源候选真实性缺口审计](./西洋JPL-Horizons来源候选真实性缺口审计-2026-08-29.md)升级为 `0.2-draft`，但不新增 source candidate。旧相同起止时间查询会收到 HTTP 200 + `Bad dates`；当前改用 21 参数显式 `TLIST` 单时刻合同，并以一次不落盘 PowerShell live body 通过新 verifier，Node 直连同期超时。本地仍无保存的 raw body／独立 retrieval envelope；demo 以 `HORIZONS_RESPONSE_CANDIDATE_INVALID / exit 1` 失败关闭，西洋继续为 `0/28 candidate`、`0/28 frozen`。

新 verifier 已绑定 canonical full URL、规范检索时间、exact byte snapshot、API／target／center／time／unit／calendar／correction／format／EOP／frame header 全序、固定 JDUT 与唯一两行表；direct constructor、orchestrator、response/report brand、deep-freeze 及 raw／record／result／payload digest 均保持 failed-closed。2 files／16 tests 在默认与显式串行模式均通过，scoped TypeScript 通过。这只证明 candidate-only mechanics 与当前格式兼容：提交者仍能重算无密钥 SHA，schema parse 不是 producer attestation。取得允许保存且可独立复核的 authoritative body 前不得生成 candidate；将来即使有单一 Sun／地心／单时刻快照，也只能登记 partial、unbound 工程候选。

API fair-use 不是三层权利或再分发许可结论，工程差分不是占星内容真值。中央 registry／跨体系 receipts 不重签；默认 v13、全部发布／专家授权 false 和 mutation／atomic／ABA 边界均不变。

## 35. 2026-08-29 C／D 独立候选机器账增量

Stage C 新增 [八字政策权重逐值证据候选](./阶段C八字政策权重逐值证据候选-v1-2026-08-29.md)，只覆盖 `binding:policy:weights` 的 values／dispatch／guard-and-consumers 三个 value-subject，并从当前 held source buffers 重建 `4/2/2/1` 与三个直接 consumer call shape。candidate digest 为 `208ae39797d048b2fc869392528a3cf7d1858de232a8691bc0296366d6363088`；工程理由、来源／权利、现实专家和冻结资格继续为 0／false，父账与 manifest 不接入，12 条 Binding 仍 `0/12`。

Stage D 新增 [八字现实专家公开候选预筛机器账](./阶段D八字现实专家公开候选预筛机器账-v1-2026-08-29.md)，把 4 个未联络公开线索映射为 11 个 link-only 观察／6 个去重上游组，并固定四题 scope、10 因素、6 pair 的全未核验状态。ledger digest 为 `ba9878bfcf266d5f34c46cf0808b3bbc382ddaa8464b36bf1a63d8c77bdf6d21`；正式席位、身份／资质／scope／独立性、同意、dossier、原始意见和专家真值均不存在，正式专家仍 `0/2`。任何外联仍需用户另行明确授权。

C／D 定向两文件在默认与显式同进程串行模式均为 44/44，CLIs 成功；新增对抗关闭合法摘要字段注入、超限／非 exact-native byte 输入、固定 v1 basis 字节漂移、候选整体及父账重签提权（含 scoped authority／candidate／distribution、value-subject metadata／coverage 与完整 parent backlink 晋级）、错误 guard、死 consumer call 与 Windows ADS 路径。release governance 193/193；正式专家包、中央 admission、cross-system receipts 三个 CLI 均保持 exit 1 的失败关闭状态且未重签。该增量不建立全仓 typecheck、默认 build、浏览器／PWA、完整应用、来源权利／法律结论、专家审定、Release Evidence、部署／回滚或公开授权；默认 v13 与 mutation／atomic／ABA 边界不变。

## 36. 2026-08-29 D 公开证据跟进 child current replacement

[八字现实专家公开证据跟进 child](./阶段D八字现实专家公开证据跟进-child-v1-2026-08-29.md)在不修改父预筛账的前提下，单向跟进 `bazi-public-lead-002/003`：2 个候选、3 条无需登录的当日公开观察、保守去重后的 2 个来源族。父账仍是 `4/11/6` 与 digest `ba9878bfcf266d5f34c46cf0808b3bbc382ddaa8464b36bf1a63d8c77bdf6d21`。协会研究档案只提供邻近主题信号，exact project question 全部未评估；公开机构名册与 DOI 元数据同属一个保守发布族，DOI 只是 locator，不另算独立事实上游，也不得据此合并现实人物或核验当前角色／资质。

该 child 只保存 URL、最小操作员摘要与响应 bytes/hash 身份；不保存正文、PDF、摘要、关键词、exact quote、UTC capture timestamp、redirect chain、capture receipt、私人资料或意见。分发固定 `link_only`，作品／版本／载体／存储／再分发权、法律审查和公开仓／build 纳入授权均未建立。没有候选外联、身份／资质／scope／独立性核验、同意、席位或原始意见，专家门仍 `0/2`。

冻结身份为 child raw `11989 / 1961f1e9118f24eba8ea2a7c782afa6e3748ed56e3a4de2c4bca3bf5f481bce3`、digest `3cbe882b03294119b75a3ba8e13bfcf60202529029aff0ea3821bf232f7619f9`、basis `5517 / 8ac95f9ea52cdf3aef7db2cc96f12a7fd780b00ea06d3b139d5deb8b8a0a982e`。新门 29/29；与父账、D0、D1/D2 合并后默认／显式同进程串行均 110/110、skip 0；release governance 193/193，独立红队未见该切片剩余 P1/P2。正式 expert packet、八字 manifest、中央 registry、cross-system receipts 继续按既有漂移 expected-red，未重签。

该 replacement 只扩充候选发现的工程证据，不建立网络来源认证、内容真值、专家真值、权利法律结论、发布就绪或公开授权；也没有运行全仓 typecheck、默认 build、完整应用、浏览器／PWA／Service Worker、公开主机、Release Evidence、部署或回滚。默认仍为 `legacy-v13 / targetSchema 13 / migrationId null`，`expertClaimsAuthorized=false`、`publicDeploymentAuthorized=false`；无跨文件原子、mutation epoch、interval mutation 或 ABA 排除证明。

## 37. 2026-08-29 C 国家版权局页面标签公开证据观察 child current replacement

[国家版权局页面标签公开证据观察 child](./阶段C中国现行著作权法公开证据观察-child-v1-2026-08-29.md)把两条国家版权局 requested URL、1 个去重页面发布族、6 个法条页面片段 hash、1 个施行日期页面片段 hash 及 4 条候选规则观察链接固定为单向 `link_only` child。所有正向网络／法律措辞均限定为 `operatorRecorded* / pageClaim`；`remoteCaptureMechanicallyVerified=false`、`publisherAuthenticityEstablished=false`、`statuteApplicabilityToCandidateWorksEstablished=false`，页面 host、HTTP/hash 和操作员摘要不等于远程认证、法律权威或法律意见。

child raw 为 `19961 / c0e91874bd0b9e7c99d4db2b79df1e68c483fc1e68ed06700be44d5bf6bc10cf`，digest 为 `f2cbbbf2d34b6f6ee69272805799def74f582f0058de8cfbab3321a84a44146c`，basis 为 `7867 / 737b271ebf526b34e10ad3462657fe3baa1eec4ef2ccf417bdeb098fc2d296c4`。新门 31/31；与来源、权利、C-M1、Binding readiness、release governance 的六文件默认／串行均 281/281，release governance 193/193。正式权利／载体记录、三层 clearance、可再分发来源、Binding、专家、内容／专家真值、发布就绪和公开授权继续为 0／false／`not_established`。

下一条更接近真实来源实例的 C 路线仍是逐候选载体取证，不把本一般规则观察当成 clearance：优先对 `binding:dtt:month-command` 的固定修订与 1947 同版双载体做逐层版本／载体核验；Harvard-Yenching 官方馆藏《三命通会》十二卷可作为卷五／卷十的另一条高质量书目与 IIIF 载体候选，但与当前 Wikisource 四库本不是同版，页图、exact quote、版本校勘和司法辖区权利判断均未进入仓库或正式记录。

本 child 不重签八字 manifest、中央 registry 或 receipts；formal expert packet、八字 manifest、中央 registry 与 cross-system receipts 继续按既有漂移失败关闭。默认仍为 `legacy-v13 / targetSchema 13 / migrationId null`，无完整应用、全仓 typecheck、默认 build、浏览器／PWA、Release Evidence、部署／回滚或 mutation epoch／atomic／ABA 证明。

## 38. 2026-08-29 C DTT 月令固定修订与双载体公开证据 child current replacement

[DTT 月令候选公开证据 child](./阶段C-DTT月令候选公开证据-child-v1-2026-08-29.md)完成了上一节所指的第一步候选特定取证，但没有把它升级成 clearance。范围只含 `binding:dtt:month-command` 的一个月令 quote hash/locator 与两个月令 normalized collation；不投影 rooting quote 或其他 collation。本轮临时下载并重算 SSID PDF 与 CADAL DjVu 的整文件 SHA-256，分别为 `d6a7d1daf6513ce9fb9940ff8331b96804c10cdafbeac00d4d3d0a37229e1803` 与 `1d0a16f528ed3074cdefad33b2d2f9233a47199cf88a8b02001c897e16996e60`，与父候选相等；仓库继续不保存文件、页图、正文或 quote。

固定 filepage 复核显示 SSID 有 `PD-scan`，CADAL 只有 `PD-old`／`CC-PD-Mark`、无 `PD-scan`；因此父来源账对 CADAL 的旧通知概括保持 `unresolved_parent_not_mutated_reconciliation_required`，任何正式晋级在对账前被阻断。Wikisource oldid 正文不字面含 `PD-old`，该通知是捕获时动态模板依赖，oldid 不冻结后续依赖修订。两份文件只是两个不同 byte identities；同一 Commons 上游、同 uploader 标签、MediaInfo 与 PDM 都不能变成两个独立版本见证、权利来源或法律意见。

child raw `31062 / 85788506067b50453545ec786c7a47b0e51a4cf004419cbe8c547f9384c01ae6`，digest `01e61e4f77526270ef83a0d95fbcb165bf307d5efa09c5a0360ff49bf1735993`，basis `10165 / 9d0c09d9add17785f41bfdb56628771c12b091b9b0ef3db7caef558a270140cb`。新门 `42/42`，七文件默认／同进程串行均 `323/323`，release governance `193/193`。正式 Rights／Carrier／KnowledgeDocument 为 `0/0/0`，Binding `0/12`、专家 `0/2`、legal conclusion `not_established`；中央 expected-red 未重签，默认 v13 与 mutation／atomic／ABA 边界不变。

## 39. 2026-08-31 C/E 西洋 IANA tzdb 2026c 来源—权利候选增量

[西洋 IANA tzdb 2026c 来源／权利候选](./阶段C-E-西洋IANA-tzdb-2026c来源权利候选-2026-08-31.md)新增一个单向 evidence child，并由[机械子件与后继候选验证账](./阶段C-E-西洋IANA-tzdb-2026c机械子件与后继候选验证-2026-08-31.md)记录非正式 v1.1.0 successor。范围只覆盖 IANA tzdb 2026c、`moment-timezone@0.6.3` 与本地 packed artifact identity：`western.input.calendar-time-zone-and-dst`、`western.rights.ephemeris-time-data-redistribution` 各有一个 partial candidate，但均为 unbound／unsatisfied。其余 26 subjects 与正式 v1 canonical-exact。

child raw／digest 为 `ee61453078f7e4df39c71ee59dcfc94f75ab37a7cd0d967849e1049d748d1ee1`／`14361c93e29d257080b25c0f3e580345243930acb07da450f938fbf6f02b8465`；successor raw／digest 为 `7f50fd5a7fd09647d1a91fc1c663e30acc56828962b98624aeeb73e985dba0fc`／`254fbc561d3a82a4b9dfaf969b472de7dfcbb9543299b4f69cc9a9944e6994db`。正式 current 仍是 v1，successor 没有 manifest／registry／owner active effect，Binding 保持 `0/28` frozen、exact quotes bound 0、专家 `0/2`。

三份新增定向测试默认／同进程串行均 `56/56`；六个受影响既有治理文件两种模式均 `293/293`。最终 in-memory live audit 只构成一次点时网络观察，不保存 receipt；remote body 零落盘只限该 audit process。detached signature 未验证，IANA 到 packed data 的 transformation provenance、三层权利法律审查、现实专家、完整应用／浏览器／PWA、Release Evidence、部署／回滚均未建立。默认继续为 `legacy-v13 / targetSchema 13 / migrationId null`，两项公开授权继续 false；无跨文件原子、mutation epoch、interval integrity 或 ABA 排除证明。

## 40. 2026-08-31 C/E 西洋 IANA 至 Moment-Timezone 受控重放观察与 v1.2 后继候选增量

[受控重放 observation 说明](./阶段C-E-西洋IANA至MomentTimezone受控重放观察候选-2026-08-31.md)记录一次临时 Windows 探针的两次成功运行：固定 `tzdata2026c`／`tzcode2026c`、Moment-Timezone tag `0.6.3` 的任务与 lock、Zig／Node／npm 身份及两个不同 decoy，均得到相同的 `meta`、`collect`、`unpacked` 与 `packed` 最终字节。observation child raw／digest 为 `41ef81e05f428286be7721529dbb0e911ad408b1d8a583b24abadac5f23f3d76`／`5d3eea3297b7cd024fb53f5a9c9e92d5a396f972d802fede6ced7c58f496b612`。这只是 `operator-recorded / point-in-time / two-run / byte-exact observation candidate`；probe runner、raw receipt 与完整 probe 时间窗均未持久化或补造。

[observation 与 v1.2 验证账](./阶段C-E-西洋IANA至MomentTimezone受控重放观察与v1.2后继候选验证-2026-08-31.md)新增 nonformal v1.2 successor，raw／digest 为 `e9894ce51541d284a58aa59b1d175a121746dbd1d6521b16a700cf64c05c63c1`／`91708bf49785148eede41872717104949fa9725732e661c25a115788a983dd58`。formal v1 是唯一 current；v1.1 与 v1.2 都不是 formal current，active effect 均为 `none`。v1.2 只给既有时区输入与数据再分发两个 partial source candidate 追加 observation，其余 26 subjects 相对 v1.1 canonical-exact；原来源、三条最小 quote 观察与 rights 账不被替换。Binding 继续 `0/28`、observation fully satisfied `0/2`、exact quote bound 0、专家 `0/2`。

四个关联 test files 在默认与显式串行模式均为 `98/98`，两个独立只读对抗审查最终均为 `P1=0 / P2=0`；release governance exit 0 并保持 `legacy-v13 / targetSchema 13 / migrationId null` 与两项公开授权 false。`check:system-contract-draft-boundaries` 当前仍 exit 1：禁读文件继续明确未检查，另有既有／其他范围的动态导入与未登记依赖诊断；本轮新增文件不在清单，红线没有被绕过。

同 cwd quarantine 不是 OS 级目标不可见，静态任务路径审计不是完整 read-set trace，clean transform offline 不是 OS 级网络隔离，Windows host adaptation 也不是跨主机工具位级复现或发布者原构建。逐字节命中不建立签名／key trust、publisher authenticity、作品／版本／载体许可、法律 clearance、内容／专家真值、发布就绪或公开发布授权。本轮没有全仓 typecheck、默认 Web build、完整应用、浏览器／PWA、公开主机、Release Evidence、部署／回滚，也没有跨文件原子、mutation epoch、interval mutation 或 ABA 排除证明；该西洋工程切片不得外推为八字、紫微或吠陀体系权威。

## 41. 2026-08-31 C/E 吠陀 IANA tzdb 2026c 来源候选、独立 civil-time 草案与 v1.1 后继候选增量

[吠陀来源／权利候选](./阶段C-E-吠陀IANA-tzdb-2026c来源权利候选-2026-08-31.md)新增体系专属 evidence child，raw／digest 为 `baa55f5e21f3a91075048c85e5b2a880f0d0e6c62e611a881e7d636b10db58fa`／`9be3d2631d1bcee5ccea174b55884f4f7f5655fed69ecd2adc94ca31cefb1bc0`。五个 IANA endpoint 在同一操作员窗口各双读一致，另有本地 Moment-Timezone 许可首行观察；远端正文、archive、signature 和 receipt 均未落盘，签名／key trust／publisher authenticity／网络隔离／法律结论均未建立。child 不继承西洋品牌、权利或 transformation provenance。

[机械子件与后继验证账](./阶段C-E-吠陀IANA-tzdb-2026c机械子件与v1.1后继候选验证-2026-08-31.md)冻结 nonformal v1.1 successor，raw／digest 为 `9356740cfd960a3137493928978d43d25be432f1bae719dde7ea95c66338c2bd`／`afd65de96a850c1632e8c00b03b6be2ff05a2f8b1dd0a19fbe27d4376ac2bb6b`。38 条要求中 36 条相对 formal v1 canonical-exact，仅 `vedic.input.iana_time_zone_and_tzdb_identity` 与 `vedic.rule.rights_license_and_redistribution_review` 增加 partial candidate；`vedic.input.dst_gap_overlap_resolution` 仍 canonical-exact。formal v1 继续是唯一 current，successor active effect 为 `none`，Binding `0/38`、完整 subject 0、formal exact quote 0、专家 `0/2`。

体系专属 civil-time adapter 位于 `isolated-drafts/vedic-civil-time-input-resolution-draft`，不进入根 workspace 或 lock。它只生成 node-test-only resolution candidate，覆盖 exact Gregorian minute/second、显式 IANA 2026c snapshot、unique、gap reject、overlap reject／earlier／later、声明重算、漂移和 round-trip；完整 13 项输入、正式 acceptance／receipt、canonical IANA／DST／UTC time-scale、来源权利、专家和发布身份均未建立。迁移时发现并修正一个测试／README 旧路径引用，最终 strict TypeScript、adapter `24/24`、`tzdb-core + adapter` 默认／串行各 `38/38`，独立终审 `P1=0 / P2=0 / P3=0`。

根 `package-lock.json` 已精确恢复既有冻结身份 `175812 / 40a02c7c3166f0c9bc03afa9c1d1f342fb7f54efe1abbf7cb3f5cf6ecb482a4d`；西洋四文件默认／串行各 `98/98` 且最终四 CLI 继续通过，没有重封或放宽其 verifier。新增吠陀 child／successor 默认／串行各 `27/27`，21 个吠陀脚本测试文件两种模式均 exit 0，release governance 通过。legacy registry 组合仍因既有八字 `MANIFEST_MISMATCH` 为 `195/203`，system-contract boundary 仍因已知受限文件和其他既有诊断 exit 1；两条红线均未绕过。

默认治理继续是 `legacy-v13 / targetSchema 13 / migrationId null`，吠陀自身 product／schema／migration／release identity 仍为 null，两项公开授权继续 false。本阶段没有建立来源正文、三层权利 clearance、现实专家身份／资质／独立意见、内容或科学真值、浏览器／完整应用／PWA、Release Evidence、部署／回滚、发布就绪或公开发布授权；逐文件 held-handle 与同缓冲复核也不证明 mutation epoch、跨文件原子、interval integrity 或 ABA exclusion。

## 42. 2026-08-31 D/E 西洋现实专家公开证据跟进 child 增量

[西洋现实专家公开证据跟进 child](./阶段D-E-西洋现实专家公开证据跟进-child-v1-2026-08-31.md)把 parent 已有 `western-public-lead-003` 的两条 PAA 观察做成单向 `link_only` follow-up；[独立验证账](./阶段D-E-西洋现实专家公开证据跟进-child验证-2026-08-31.md)记录完整失败与修复历史。child 只保存 URL、时间、HTTP／content-type、长度、hash、final=requested 与 candidate-token boolean，不保存或打印页面正文、exact quote、私人联系方式、dossier 或意见。当前为 1 个既有候选、2 条观察、4 次操作员读取、1 个 `PAA_NCGR` 上游组；身份／资质／六题 scope／独立性／同意／专家意见均未建立，专家仍 `0/2`，Binding 仍 `0/28`。

最终 child raw／digest 为 `32deead24ee4a0e3727000b9ebbcd38069de7cb1957fd4fdc0961049bc5dd5d3`／`32be644f1e4c7b5b3b599dd59e85b0b3ad7c3ce491ad20980ea1a309aea24856`，basis 为 `5d89011599e14aa4aa2a03e7f7e5f556c11c890117f05d174fc6c32118f710d4`。初版曾被真实 P1 判别：动态 `Object.hasOwn` 可使嵌套 authority 保持可变而 WeakSet brand 仍为真；修复后捕获 primordial、消除 `Array[Symbol.species]` 与动态 `RegExp.exec` 缺口。两轮 CLI 终审又依次淘汰了“无 direct-entry 绑定”和“仅凭可伪造 argv 路径绑定”的中间态，最终以 `import.meta.main` 为主门、resolved argv path 为附加门，并用真实 target-path wrapper 回归证明业务 import／write 未被触达。

child 专用默认／串行各 `31/31`，受影响既有闭包各 `89/89`，全部 8 个西洋 test files 默认／串行各 `188/188`，第三次独立终审 `P1=0 / P2=0 / P3=0`。release governance exit 0 并保持 `legacy-v13 / targetSchema 13 / migrationId null` 与两项公开授权 false；offline lockfile dry-run 前后保持 `175812 / 40a02c7c3166f0c9bc03afa9c1d1f342fb7f54efe1abbf7cb3f5cf6ecb482a4d`。system-contract boundary 仍按已知受限文件和其他既有诊断 exit 1，本轮新增文件未进入诊断，红线没有被绕过。

两页稳定双读仍只是操作员记录，不建立 network attestation、publisher authenticity、现实人物 binding、当前角色／资质、权利法律结论或专家真值。本轮没有专家外联、完整应用、全仓 typecheck、默认 Web build、浏览器／PWA、公开主机、Release Evidence、部署／回滚，也没有 mutation epoch、跨文件原子、interval integrity 或 ABA exclusion 证明；该西洋 child 不得外推为八字、紫微或吠陀体系权威。

## 43. 2026-08-31 C 阶段 SourceCarrier readiness v1.1 版本感知后继候选增量

[SourceCarrier readiness 版本感知后继候选说明](./阶段C-SourceCarrier-readiness版本感知后继候选-v1.1-2026-08-31.md)与 [v1.1 candidate](../content/system-admission/bazi-source-carrier-record-readiness.v1.1.0.json)新增一个 candidate-only、单向、无 backlink 的版本感知后继。旧 v1 的五行 carrier gap canonical-exact 保留，唯一新增 ordinal 6 为 SMT-v10 CADAL anchor；当前为 4 个 source／rights families、6 行 carrier observations、SMT-v10 2 行、DTT 2 行。artifact raw identity 为 `18,654 bytes / 8a2ae3fcd2abc01d952159ab78cb98eeba5dd2ab06d205e478abf64574759377`，ledger digest 为 `ca2734777002a2e56d82ec3702f0517668ae68b23b423c2cb31e65164c225531`；说明文档为 `6,339 bytes / ed7abd46e47db965dc1c6fd984a06a944766e1f321416b60f371a257179dacd9`。

正式 `KnowledgeDocument / SourceRightsRecord / SourceCarrierRecord / project-copy materialization=0/0/0/0`，adjudication、已核 reviewer、现实专家、法律结论与 frozen Binding 均未建立，Binding 仍为 `0/12`；same-edition、直接 provenance 与 live verification 均为 false。Binding readiness、C-M1、既有 Bazi Manifest v2 与四体系 Registry v2 均未改写或重绑，v1.1 `activeAdmissionEffect=none`，不能由 successor 存在推导 active/default/manifest/registry adoption、内容或专家真值、权利 clearance、发布就绪或公开授权。

专用测试默认／串行均为 `23/23`，新 candidate、旧 readiness 与 SMT paired supersession 受影响闭包两种模式均为 `90/90`，三个对应 CLI 均通过；遗漏旧载体和合并 DTT 两条载体的重签攻击已分别有显式拒绝回归。默认继续为 `legacy-v13 / targetSchema 13 / migrationId null`；跨文件原子、mutation epoch、interval integrity 与 ABA exclusion 仍未建立。本阶段没有运行全仓 typecheck、默认 Web build、完整应用、浏览器／PWA、联网复核、Release Evidence、部署或回滚，也没有读取受限文件或执行 Git 操作。

## 44. 2026-08-31 D 阶段八字专家隐私与正式 intake 对账后继候选增量

[阶段 D 对账说明](./阶段D-八字专家隐私与正式intake对账后继候选-v1-2026-08-31.md)与 [reconciliation child](../content/system-admission/bazi-expert-privacy-formal-intake-reconciliation.v1.json)新增一个 candidate-only、zero-instance、opaque-only 的单向后继。它只消费 expert intake v1.1 与 authority/privacy precheck v1 两个私有品牌；旧 public identity 8 字段只保留历史 disposition，不再构成未来收集或仓内持久化授权。当前真人 context、dossier、identity、credential、scope、authority、independence、opinion、seal 与 bundle 全部为 0，`personDataPresenceAssessed=false / personDerivedDigestExcluded=false / safeToPublish=false`。

artifact raw／digest 为 `f99612bd68adbe42145b0cc92030f44d54ee91eed574617690de8a4f109c9a17`／`cdf1a4d73a07b99a915e19f24672adbf6a4b42fb98754a0d679b32ec91eca3a2`。专用默认／串行各 `30/30`，与两个父门三文件闭包各 `92/92`；两路终审无剩余 P1／P2并保留列明的自动化覆盖 P3。Manifest v2、observers、Registry v2、formal lifecycle、D0 与 kernel 均无 consumer／backlink；`activeAdmissionEffect=none`。

formal packet 仍以 `INTAKE_GAP_BINDING_DRIFT` expected-red，packet locks `11/12`，专家 `0/2`、Binding `0/12`。CLI 被明确降格为 mechanical observation：hidden preload、Node／loader／launcher identity 与 CLI trusted attestation 均未建立，可见 loader guard 不是安全边界。默认仍为 `legacy-v13 / targetSchema 13 / migrationId null`；内容真值、专家真值、三层权利法律判断、runtime／浏览器证据、release readiness 与公开授权都没有因本 child 提升。

## 45. 2026-08-31 C 阶段 12 Binding readiness v1.8 增量

[v1.8 说明](./阶段C-12Binding-readiness版本感知后继-v1.8-2026-08-31.md)与 [artifact](../content/system-admission/bazi-binding-freeze-requirements.v1.8.0.json)把 source／rights 父位更新为 `1.7.0 / 1.3.0`，并消费 predecessor、paired supersession 与 SourceCarrier readiness v1.1 三个私有品牌。十二行只切换 ordinal 10 的 SMT-v10 candidate；机械观察为 `6/9/8/2`，quote digest 为 6，但正式四类记录仍全 0、Binding `0/12`、现实 reviewer／专家／法律审查均为 0。artifact raw／digest 为 `f612019e19255f47be03a569bb4cf3e61227146a924c4c63a2b75f1b28649ab3`／`6ed301be956d160e06126eed63a7155ab8182adbcbc7e0def2690e8271e50866`，专用默认／串行各 `23/23`，无中央 backlink。

下一步 C 不是继续抬机械计数，而是逐条建立可冻结的可靠底本／版本／正文／exact quote，以及作品层、版本层、载体层权利依据；不能再分发的材料保持 private 或 link-only。当前 v1.8 不满足任一正式 Binding。

## 46. 2026-08-31 D 阶段八字 Manifest v2.1 增量

[v2.1 说明](./阶段D-八字v1.7当前机器身份Manifest-v2.1-2026-08-31.md)与 [artifact](../content/domain-release/bazi.single-chart-report.v1.7.0.manifest.v2.1.0.json)消费 Manifest v2、carrier v1.1 与 privacy／formal-intake reconciliation 三父品牌，只重绑 rights carrier 与 expert preconditions。raw／digest 为 `68794de2ce30c11a8e94333c23a2e115355590ead41eb97f67af6a638e420413`／`f3cc8c91674c49317f93a7362b34e1b8eb887029284e0c9994fdf15ea546bfd2`；专用默认／串行各 `32/32`。红队发现并关闭 test-only unbranded summary P2，最终 P1/P2 为 0。

下一步 D 仍必须由现实自然人专家完成身份、资质、scope 与彼此独立性核验，并至少并列保存两份原始意见；当前 packet locks 仍 `11/12`、首失败 `INTAKE_GAP_BINDING_DRIFT`，专家 `0/2`。不得多数表决、平均或由生成模型选赢家，也不得在未获授权时开始外联或个人信息收集。

## 47. 2026-08-31 E 阶段吠陀独立工程 Manifest v1 增量

[独立 Manifest 说明](./阶段E-吠陀独立工程当前机器身份Manifest-v1-2026-08-31.md)与 [artifact](../content/domain-release/vedic-astrology.engineering-draft.v0.1.0.manifest.v1.json)首次为吠陀固定独立 current-machine identity：`5 closures / 33 references / 33 unique paths`，并通过 full loader 私有品牌纳入最新 source-rights v1.1。artifact raw／digest 为 `a21c5bcafe84fcbb6289af6d3dfdd052acb72c6987ea924a882abd76768bc0d5`／`96c1f8c4062a5d398b7b546ee68fd6bbd51f5a5200f4bdb4b76b994ab25d8685`；v1.1 仍 nonformal／zero-effect，吠陀 `0/38 / 0/2 / 0/8`，自身四项产品身份全 null。专用默认／串行各 `36/36`，E scope 红队最终 P1/P2/P3 为 0；上游历史 residual 没有被冒充关闭。

三条 C／D／E 新测试合并默认／串行均 `91/91`，release governance 通过；旧 formal／中央账继续 expected-red，system-contract boundary 继续只列既有红项。下一步 E 必须分别完成吠陀自己的输入、事实、规则版本、来源、权利、现实专家与高风险表达边界，不能从八字 v1.7、紫微或西洋继承权威。全仓 typecheck、默认 Web build、完整应用、浏览器／PWA、Release Evidence、部署／回滚、内容／专家／法律真值与公开授权仍未建立。

## 48. 2026-08-31 C 阶段生产 Bundled Knowledge Manifest 共享契约 v0.2 增量

[共享契约说明](./阶段C-生产BundledKnowledge-Manifest共享契约-v0.2-2026-08-31.md)关闭首份真实材料进入生产 manifest 前的接口 P2：knowledge-core、Web build audit 与隔离 Stage C candidate 现在共用 `documents/`／Markdown-TXT／规范 path segment／code-unit ordering／format inference 元数据门。Web 的磁盘 inventory、stable read、实际 bytes/hash 仍单独存在；candidate 明示 `productionBodyInventoryAndBytesAudited=false`，没有把结构 fixture 冒充生产正文证据。

Stage C candidate 升为 `0.2.0`，仍固定 `bindingCitationLocatorLinked=false / externalTuplePinVerified=false / mutationEpochCapabilityAvailable=false / admissionAuthorized=false`。默认／串行定向测试各 `47/47`，两条 strict TypeScript 切片与 release governance 通过；红队先后关闭非规范路径、`.txt` dotfile 格式、document format／extension、排序和不可辨识回归问题，最终运行时 P0/P1/P2 为 0。

knowledge-core 生产 identity 的改变使 v1.8 readiness、Bazi Manifest v2／v2.1 full-loader 链按预期失败关闭；旧 artifacts 均不改写，从此只能称历史快照。下一机械步骤是 append-only v1.9／v2.2：旧 source／rights／DTT／SMT／carrier 只作 `brandCurrent=false` 的 raw+semantic historical observations，重绑 current knowledge-core 与 Web audit，并继续保持 Binding／formal records／专家／authority 全红。在后继落盘前不得称 v2.1 为 current。

真正非 synthetic 的 C 下一步仍是 `binding:smt-v10:whole-chart` 的 `strength.yueling_exact_quote / smt-v10-yueling-minimal-v1`：需要工作区外合法持有的 revision 761703 原始 UTF-8 body 和“仅本机私有导入／验证、禁止仓库存储与再分发”的明确授权。D 没有无需真人授权即可继续的真实 tranche；其他体系的下一工程候选可独立评估西洋 civil-time → fact-only 浏览器链，但不得接入宫位／解释或继承八字 authority。

## 49. 2026-09-01 C/D 阶段 readiness v1.9 与八字 Manifest v2.2 增量

[Binding readiness v1.9](../content/system-admission/bazi-binding-freeze-requirements.v1.9.0.json) 已以 append-only 后继恢复当前 knowledge-core raw identity：artifact `45,551 bytes / e20145b5f34b5dc04464e482cda5da990f82e520236679efbf568b322637f797`，ledger digest `42930e63cb0960df969f5fe059617eb96a8ec9bd857c486cd4b33a28d37a8ae1`。十二条 Binding 行保持原样、改动数为 0；v1.8 与十项旧 basis 只作固定历史观察，未消费其失效私有品牌。Binding、正式记录、materialization、reviewer、专家与法律计数均未提升。

[Bazi Manifest v2.2](../content/domain-release/bazi.single-chart-report.v1.7.0.manifest.v2.2.0.json) 以 `23,399 bytes / 6481d5edfa9f9819af035ad9d208ea526fa7b29f12ccd6e072ad31f625e7f86d`、digest `a95e722674c0b09a2d564b4090396fbfc25fdfc2f5f2fff9aedc92aa2468f68e` 成为当前机器身份后继。10 个组件中只重绑 source／rights，34 个引用归并为 28 个当前唯一路径；唯一 current private brand 为 v1.9，v2.1／carrier／privacy 三者均降为历史 raw+self-digest context。knowledge-core 与 Web audit 只固定源码身份，Web call-site、生产正文 inventory／bytes 与本轮 build gate 均为 false；Stage C candidate 不在 active surface。

专用测试默认／串行分别为 `32/32` 与 `25/25`，联合闭包两种模式均 `57/57`；独立终审无剩余 P0–P3。release governance 与 Web storage boundary 通过，lockfile 未变；system-contract boundary 继续只列禁读文件与既有诊断。当前仍为 Binding `0/12`、专家 `0/2`、正式四类记录 `0/0/0/0`，并保持 `legacy-v13 / targetSchema 13 / migrationId null` 与全部 authority／epoch 红门。

下一真实 C 输入没有变化：必须由用户在工作区外提供合法持有的 revision 761703 UTF-8 body，并明确授权仅作本机私有验证。D 仍需用户另行授权现实专家外联与个人信息处理；不得由 v2.2 的机器身份恢复推导专家真值、权利结论、Release Evidence 或公开发布授权。其他体系继续独立，不继承八字 v1.7 authority。

## 50. 2026-09-01 E 阶段西洋 civil-time→UTC fact-only 浏览器链增量

[机械验证说明](./阶段E-西洋civil-time至fact-only浏览器链机械验证-v1-2026-09-01.md) 与 [observation child](../content/system-admission/western-civil-time-fact-browser-observation-candidate.v1.0.0.json) 已落盘。推进的是独立的民用时输入→UTC 工程事实链，不是星盘、宫位、相位、规则或解释内容；协议仍为 `0.1-draft`，西洋自身产品／schema／migration 身份仍全 null，也不继承八字 v1.7 authority。

浏览器证据仅来自 Codex In-app Browser 受控 loopback：四组 synthetic 场景、窄屏布局、表单失败关闭与 console 0 已观察；Chrome、Edge、PWA、Service Worker、正式存储、完整应用及公开主机均未验证。runtime storage probe 不可用，受限文件继续未读，因此只能报告静态 authored-source 0 命中与 incomplete production reachability audit，不能声称运行时零存储或全仓生产不可达。

西洋来源仍为 `Binding 0/28`、现实独立专家 `0/2`、正式准入 `0/8`。可靠底本／版本、来源正文、exact quote、作品／版本／载体三层权利、内容／专家／科学真值、Release Evidence、发布就绪及公开部署／发布授权均未建立。SHA-256 只是一致性摘要，不是签名；UI generation 只防旧 Worker 结果覆盖新输入，不是 mutation epoch，也不证明跨文件原子、interval mutation 或 ABA 排除。

## 51. 2026-09-01 E 阶段吠陀 civil-time→UTC fact-only 浏览器链增量

[机械验证说明](./阶段E-吠陀civil-time至fact-only浏览器链机械验证-v1-2026-09-01.md) 与 [observation child](../content/system-admission/vedic-civil-time-fact-browser-observation-candidate.v1.0.0.json) 已落盘。推进的是吠陀体系独立的民用时输入→UTC engineering facts，不是 sidereal zodiac、ayanamsa、Rahu／Ketu、Bhava、分盘、Dasha、星历、规则或解释；吠陀自身产品／release／schema／migration identity 仍全 null，也不继承八字 v1.7、紫微或西洋 authority。

candidate raw／semantic digest 为 `fdfb6da43215e87e103f428d59d104b43cb3be3b4468880b9417f0baf6167e0a`／`019ee27933a5a63eb2636a3b3618df25b9a365fed0101f88e1e8ddc1bcb68f6d`，绑定 25 个 artifacts。两次独立 OS-temp build 的 12-file envelope byte-exact，build output digest 为 `2037ddbf4a4ccea5b80e597751dd30c51f9e4a99a5fef353531570c8b5f78ccd`。隔离 strict TypeScript、`5 files / 52 tests`、专用 verifier 默认／串行各 `12/12` 和 CLI 均通过；release governance、Web storage boundary、offline lockfile dry-run 通过。中央 system-contract boundary 仍为 `74/75` expected-red，并继续明确跳过受限文件；没有把专项全绿冒充全仓全绿。

浏览器证据仅来自 Codex In-app Browser 受控 loopback：unique、second precision、1900 年秒级 offset、gap／overlap reject、overlap earlier／later、未知时区、输入变化清空、窄屏、CSP 与 console 0 已观察。网络请求计数未探测，存储／cookie 检查受工具边界禁止，Service Worker 未探测；Chrome、Edge、PWA、正式存储、完整应用与公开主机均未验证，不能声称 runtime zero network／storage／cookie／SW activity。

吠陀当前仍为 `Binding 0/38 / expert 0/2 / admission 0/8`。内容／专家／科学真值、三层权利法律判断、Release Evidence、发布就绪与公开部署／发布授权均未建立。UI generation 不是 mutation epoch，也没有跨文件原子、interval integrity 或 ABA exclusion。C 的私有原始材料与 D 的现实专家外联继续等待用户分别授权，本增量没有越权启动。

## 52. 2026-09-01 E 阶段吠陀独立浏览器质量门与 Release Evidence 设计候选增量

[设计候选说明](./吠陀独立浏览器质量门与Release-Evidence设计候选-v0.1-2026-09-01.md) 与 [requirements-only candidate](../content/system-admission/vedic-independent-browser-quality-gate-and-release-evidence-design-candidate.v0.1.0.json) 已落盘。candidate 为 `45,131 bytes`，raw／semantic digest 为 `4f2ecea6cb8d639c23fa3b06528b217319e93d171a7db57f0a5331172a5ae673`／`c66ebecac08cbfad109d7bf11b6d63d7e8468bd075c599f105d4d515cbda768c`。它固定 runtime proposal 的 8 个 exact interface IDs，并为未来 Edge、Chrome、artifact、build、runtime、deployment、release rollback、evidence retention 定义 conjunctive exact-set；当前实现与 accepted receipt 全部为 0，`designMaterialCandidateComplete` 只表示八份 requirements 文本齐备。

独立终审发现并关闭两项 P2：Edge／Chrome 各三次现固定为 fresh desktop、fresh `390x844` 与 retained-profile offline/update transition 三个不可重复 run role；所有 receipt 共同绑定 subject release N+1，transition 另绑 predecessor release N，后者不能替代前者。跨 receipt 还强制同一 target schema、migration、mutation epoch 与 rollback release lineage，并由测试硬编码 receipt→issuer mapping、exact cross fields 与 run matrix。七账也已显式列出逐账全红状态。

既有 `2 build + 1 Codex IAB` 仍只是 operator-supplied engineering baseline：不能计作 Edge／Chrome、artifact／build、production runtime、deployment、rollback 或 retention receipt。网络未探测，storage／cookie inspection 受限，Service Worker 未探测；相应 observed value 保持 null，不能归一化为零活动。两个 retention ceiling `50,000,000 / 100,000,000 bytes` 仍为未测量、未批准计划值；storage requirements-only rollback 也不能替代 release rollback receipt。

候选先顺序重验五个直接上游的 bytes／SHA-256，再消费两个 full-loader 私有品牌，但没有独立重锁当前 35 个去重传递 raw 端点，所以明确保持 `transitiveClosureIndependentlyRawPinned=false / completeCurrentRawClosureClaimed=false`。ADR 与三份 direct JSON 具有各自明确的同 buffer 校验；browser direct snapshot 与 child full-loader 是分离读取，因此 `sameBufferHashParsePerBoundFile=false`。顺序固定路径观察不证明跨文件同时态、mutation epoch、原子快照、interval integrity 或 ABA exclusion。

专用测试在最终稳定快照默认／串行各 `18/18`；独立只读终审 `P0/P1/P2/P3=0/0/0/0`，并以真实 fixed-path 定向复现确认 inherited-setter／ambient-push P1 已关闭。四个受影响既有吠陀测试文件 `169/169`，CLI、release governance 与 Web storage boundary 通过。测试已冻结 post-import intrinsic／继承 setter／ambient push 定向投毒、五路 clean fixture＋同长度 raw drift 精确错误码、bound-path junction、转义／非规范 JSON、CLI `process.execArgv` 与 raw SHA exact pin。formal parent／Registry／Manifest 均未改写，重新审阅仍 `required_absent / 3 of 7`，吠陀继续为 `0/8 / 0/38 / 0/2` 与四项产品身份全 null。默认治理背景继续是 `legacy-v13 / targetSchema 13 / migrationId null`，不被吠陀继承；内容／专家／权利法律、Release Evidence、发布就绪和公开授权均未提升。C 私有原文与 D 真人外联仍等待分别授权。
