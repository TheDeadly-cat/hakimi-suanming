# 阶段 C：C-M1 项目物化版本感知候选 v1（2026-08-30）

## 结论

本切片建立一条独立的 C-M1 v1.1 version-aware、candidate-only、zero-instance capability。它保留历史项目副本物化契约与 API schema，只把历史账的三个版本化 parent slot 精确重绑到 source v1.6、rights v1.2 与 readiness v1.7；三个未变的直接 non-parent basis 必须继续匹配历史 raw identity。

这条候选只建立固定 raw／semantic identity 下的机械账。它不创建 SourceCarrier、SourceRights 或 ProjectCopyMaterialization 实例，不接入 materialization API，不改变默认治理，不激活准入，也不建立内容真值、专家真值、法律结论、运行时证据、发布就绪或公开发布授权。

## 历史账与候选身份

历史 C-M1 账保持不变：

- path：`content/system-admission/bazi-project-copy-materialization-requirements.v1.json`
- raw：`6168` bytes
- raw SHA-256：`c63a95514063fb64752d449f6ccb1c844544110e5ed9f1192de2ef7723efd13b`
- ledger ID：`hakimi.bazi.project-copy-materialization-requirements/1.0.0`
- ledger digest：`690320c5d9778bf61da5ca3e112aedc6f9e2760e27f628f4c992cb5b9a8af1cb`
- role：`fixed_zero_instance_materialization_contract_projection_template_only_not_current_capability`

版本感知候选为：

- path：`content/system-admission/bazi-project-copy-materialization-requirements.v1.1.0.json`
- schema：`1.1.0`
- record type：`bazi_project_copy_materialization_requirements_version_aware_candidate_v1_1`
- ledger ID：`hakimi.bazi.project-copy-materialization-requirements.version-aware-candidate/1.1.0`
- status：`candidate_only_version_aware_zero_instance_materialization_requirements_no_admission_effect`
- raw：`10887` bytes
- raw SHA-256：`7e98e6042cd1ccb00a2cff749f0000a989baacd683271edffc3158a80ee02b83`
- ledger digest：`d877ac00042d9e9635b0bf2cfe7b5f387f3aad82faa96c8a7214548299045d3f`
- active admission effect：`none`

候选 digest 只是固定 domain 下的完整性摘要，不是数字签名、作者身份、保管链或法律证明。

## 六项直接 basis：三项不变、三项重绑

候选恰有六项直接 basis。前三项不改变 parent 关系，只验证当前时点 raw identity 后复用历史投影：

1. `content/knowledge/manifest.v2.json`：`48` bytes；SHA-256 `92e4d2c92aeb7f1e7898014be429f0fab92885dd52c36aee8eb1523941c55122`。
2. `packages/contracts/src/index.ts`：`244747` bytes；SHA-256 `674a3fe1e2b3cd4fc99e481a758966a320ddc3c1113a9471eba190851d15e041`。
3. `packages/knowledge-core/src/index.ts`：`39595` bytes；SHA-256 `9e6420e8c6c5b3aafd892fdb3c5363af81ea9d9e24ebff6e11897300053524b0`。

后三项是历史三个 parent slot 的精确版本重绑：

1. source parent → `content/bazi-strength-source-binding-candidates.v1.6.0.json`：`50427` bytes；raw SHA-256 `62f6a68b99246a802bcf2055aa09a8ac8113df37a7ede1c55d8a2ea64deaea98`；ledger ID `hakimi.bazi.strength.source-binding-candidates/1.6.0`；ledger digest `6f008058b282613fd131344206b27fccb1d80596bb49789f0554acdd69c1e44c`。
2. rights parent → `content/bazi-strength-source-rights-candidates.v1.2.0.json`：`23947` bytes；raw SHA-256 `678632a8875672f58bb5c9f1ec4a299a4f0c6e750b8ff4a02bfe4df889e1243a`；ledger ID `hakimi.bazi.strength.source-rights-candidates/1.2.0`；ledger digest `300e092c4913885cff626221b8734786128639c7d6cfd7c2be7bea2acc6bbefc`。
3. readiness parent → `content/system-admission/bazi-binding-freeze-requirements.v1.7.0.json`：`32629` bytes；raw SHA-256 `7565a1c1ae51c09c78cf8f906e544bd8ee822020f76587237bc909ef1e6408a2`；ledger ID `hakimi.bazi.strength.binding-freeze-candidate-readiness/1.7.0`；ledger digest `afdebfa1fb7a797f02d136373eb9aef1afa26b38b755926f8930ea5de97316b8`。

因此本层计数固定为 `6 direct basis = 3 unchanged non-parent basis + 3 rebound parent slots`。它不是把六项 basis 都重新执行一次 projection inspection。

## 两个私有品牌与一项 supporting receipt

三个 parent slot 只对应两个经私有 WeakSet 验证的上游 capability：

1. source v1.6 与 rights v1.2 由同一个 versioned parent supersession capability 成对验证。
2. readiness v1.7 由 candidate readiness capability 验证。

supersession receipt 的固定 supporting tuple 为：`content/system-admission/bazi-dtt-versioned-parent-supersession.v1.json`；`9229` bytes；raw SHA-256 `aa9e62089990d8ba699c441114914173141e8228f027605b47f5c5e238fac0e3`；supersession ID `hakimi.bazi.dtt-versioned-parent-supersession/1.0.0`；digest `601b4e07b94e62b73812a7a26f1108a094272dca660a188bb660fa860f031264`。

该 receipt 只是 supersession capability 的 supporting artifact：它不是第 4 个 parent slot，不是第 3 个品牌，也不进入六项直接 basis 计数。plain object、clone、digest 或自行拼装的 tuple 都不能替代两个 loader 私有品牌。

## 历史投影、API 与零实例边界

本层只在以下条件同时成立后复用历史 materialization contract projection：历史 C-M1 raw 与 semantic identity 精确匹配；三项 non-parent basis 当前时点 raw identity 精确匹配；三个 parent slot 经两个私有品牌完成版本重绑。

`currentProjectionInspectionReexecutedByThisLayer=false`。本层没有重跑历史 producer／consumer inspection，没有声称嵌套 verifier 的 same-buffer 性质可传递，也没有建立跨文件原子快照或运行时执行证据。

物化 API 边界保持原样：

- materialization candidate schema：`1.0.0`
- materialization receipt schema：`1.0.0`
- SourceCarrier schema：`1.0.0`
- normalization profile：`hakimi.knowledge.utf8-text-normalization/1.0.0`
- `materializationApiIntegratedByThisCandidate=false`
- project result 不保存 absolute path、content 或 raw bytes

旧 C-M1 的 materialization candidate／receipt API 仍是独立历史实现；v1.1 候选没有替换、调用或激活它们。

当前库存继续全部为零：bundled manifest entry `0`、formal SourceCarrier `0`、formal SourceRights `0`、verified materialization `0`、ProjectCopyMaterialization `0`、redistributable source `0`。需要冻结的 binding 为 `12`，verified frozen binding 为 `0`。

## 六本账必须继续分开

### 工程证据

本层建立的只有固定文件路径、raw bytes、raw SHA-256、semantic digest、两个私有品牌、三项 parent 重绑、三项未变 basis 的时点 raw identity，以及历史投影复用条件。`endpointSnapshotOnly=true`；interval mutation 与 ABA 均未排除。

### 内容真值

没有新增材料正文、载体真实性、OCR／转录／校勘准确性或规范化之外的语义等价证据。`sourceBundleComplete=false`、`contentTruthEstablished=false`。

### 专家真值

没有新增专家身份、资质、独立性、意见或评审 bundle。`expertReviewBundleComplete=false`、`expertTruthEstablished=false`、`expertClaimsAuthorized=false`。

### 权利与法律结论

formal SourceRights 与 SourceCarrier 都是 `0`，没有建立作品层、版本层、载体层、复制或再分发许可。`rightsBundleComplete=false`、`rightsLegalConclusionEstablished=false`。

### 浏览器与运行时

本层没有执行浏览器、PWA／Service Worker、Chrome／Edge、应用运行时、真实 consumer 或公开主机验证。`browserRuntimeEvidenceEstablished=false`。

### 发布与公开授权

本层没有建立 Release Evidence、部署、回滚或公开发布证据。`releaseReady=false`、`publicDeploymentAuthorized=false`、`formalActivationAllowed=false`，且 formal admission promotion 保持 blocked。

## 固定默认治理与 epoch 红门

默认治理仍固定为 `legacy-v13 / targetSchema 13 / migrationId null`。v1.1 候选不提升 schema、不生成 migration，也不改变 active line。

`crossFileAtomicSnapshot=false`、`mutationEpochAvailableForSchema13=false`、`mutationEpochReceipt=null`、跨文件 interval mutation 未排除、ABA 未排除。这里的 `mutationEpochAvailableForSchema13` 明确限定 Schema 13，不能改写成无 schema 范围的 mutation-epoch 可用性结论。

## 无 policy 依赖

历史 C-M1 requirements artifact、lib 与 test 没有 policy weights 依赖；本次最小 transform 也没有新增 policy weights v1.1、engineering gap、PR10BC 或其他为串链而虚构的依赖。version-aware C-M1 只直接消费 supersession 与 readiness 两个私有品牌。

后续任何 consumer 若要依赖本候选，必须显式消费本候选 loader 的同根 WeakSet 品牌，并另行证明该依赖来自真实契约。不能因为相邻候选已经存在，就把它强行写进 C-M1 parent ledger。

## 机械验证与历史测试分账

窄 CLI：

```powershell
node scripts/verify-bazi-project-copy-materialization-version-aware-candidate.mjs
```

成功前缀固定为 `BAZI_PROJECT_COPY_MATERIALIZATION_VERSION_AWARE_CANDIDATE_MECHANICS_OK`。输出只含固定候选身份、默认治理、parent／brand／supporting receipt 计数、API schema、零实例计数与红门；不输出完整 ledger、上游对象或来源正文。失败输出也只给固定失败前缀与错误码。

当前验证分账：

- frozen artifact loader：已通过；artifact identity 为 `10887 / 7e98e6042cd1ccb00a2cff749f0000a989baacd683271edffc3158a80ee02b83 / d877ac00042d9e9635b0bf2cfe7b5f387f3aad82faa96c8a7214548299045d3f`。
- version-aware candidate lib identity：`31832 / 63ba3afe9fe3321fc6d73e48ab51f85ee02d078c569aaa524f7527a5e53d1f7b`。
- narrow CLI identity：`4278 / 63d113bebe1a76e9f20c97aaa347e375256219981adb71a5d7e011c74fce1ca5`。
- v1.1 独立 test 文件 identity：`45316 / 2c96e2de8a57cf7b6920ce24ae8a11dd1619aa0ead94b28842c2e52238d064a8`；定向结果为 `32/32`。
- 从 DTT versioned-parent supersession、readiness 1.7、PR10BC v1.1、engineering gap v1.1、policy weights v1.1 到 C-M1 v1.1 的六文件候选链结果为 `180/180`。
- 历史旧 C-M1 定向 tests：保持 `19/23`；其中 19 个 materialization candidate／receipt／preflight API 行为测试通过，4 个 requirements expectation 因历史 readiness parent alias／pin 漂移而失败。这不是 v1.1 候选对旧文件的原位修复，也不是 API 回归。

本切片的机械通过不等于内容真值、专家真值、权利法律结论、运行时通过、发布就绪或公开发布授权。
