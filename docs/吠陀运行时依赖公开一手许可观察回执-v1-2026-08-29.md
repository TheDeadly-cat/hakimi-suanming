# 吠陀运行时依赖公开一手许可观察回执 v1

- 日期：2026-08-29
- 体系：吠陀占星
- 角色：非产品、link-only 的公开端点读取证据
- 当前结论：已观察公开一手端点，不等于依赖已选、许可已审、法律结论成立或获准再分发

## 1. 本阶段结论

本轮开始把吠陀运行时提案中的许可问题从“只有待答问题”推进到“有真实公开读取回执、但仍未完成复核”。2026-08-29T06:07:50.954Z 至 06:08:01.354Z 对五个公开端点各执行两次立即读取，共记录十次 HTTP 200 响应。只保存 URL、HTTP 元数据、字节数和 SHA-256；没有把页面、PDF、许可证正文、源码或星历文件复制进仓库。

五个端点只覆盖五类依赖中的四类：`python_runtime`、`swiss_ephemeris_code`、`ephemeris_data_files` 与 `dedicated_worker` 有公开观察引用；`loopback_local_service` 仍无具体实现或许可证据。端点数、读取次数和依赖覆盖数不得互相替代。

## 2. 公开读取回执

| evidence ref | 公开端点与有限角色 | 两次读取结果 | 稳定性 |
| --- | --- | --- | --- |
| `hakimi.vedic.runtime-license-evidence/python-psf-legal-public-read/2026-08-29` | [Python Software Foundation legal overview](https://www.python.org/about/legal/)；只观察许可家族与不同 Python 实现可能有不同权利 | 两次均 33,368 bytes，SHA-256 `579c76490d8f53a260c5aaa436f8287d770063168220b5bc9f1bf41cc2da0957` | 立即二读相同；无版本 pin、ETag 或 Last-Modified |
| `hakimi.vedic.runtime-license-evidence/swiss-ephemeris-commit-license-public-read/2026-08-29` | [Swiss Ephemeris commit-pinned LICENSE](https://raw.githubusercontent.com/aloistr/swisseph/3fd0f956d73898b91cc4f67cf18b21af656d1342/LICENSE)；只观察 commit `3fd0f956d73898b91cc4f67cf18b21af656d1342` 的许可证文本载体 | 两次均 2,371 bytes，SHA-256 `b6345743292c516d07799dfad46f1a641e5270da2160cc4d6bd54594d6faa991` | 立即二读相同；commit 固定 |
| `hakimi.vedic.runtime-license-evidence/swiss-ephemeris-dynamic-overview-public-read/2026-08-29` | [Astrodienst Swiss Ephemeris overview](https://www.astro.com/swisseph/swephinfo_e.htm)；只观察发布者网页及其动态性 | 两次均 62,524 bytes，但 SHA-256 分别为 `0d08e91fe7c975d0156b38d56add580f8bcd662433fef589e82dcdb1cd4eb9c0` 与 `a5766678581fa5fd263fe217bcd11695d3b74144a7be216c0668a7ef545a2efe` | 立即二读不同，明确不能冒充 frozen source body |
| `hakimi.vedic.runtime-license-evidence/swiss-ephemeris-professional-contract-template-public-read/2026-08-29` | [Swiss Ephemeris Professional License contract template](https://www.astro.com/swisseph/secont_e.pdf)；只观察未签署合同模板 | 两次均 105,150 bytes，SHA-256 `0e0a80ab3efcc239ce5a0d8d0be05635281b2a6f298935229ebdfe5392ca043a`；Last-Modified `Fri, 19 Jun 2026 14:10:31 GMT` | 立即二读相同；不等于购买、付款、签署或获授权 |
| `hakimi.vedic.runtime-license-evidence/whatwg-workers-living-standard-public-read/2026-08-29` | [WHATWG Web Workers standard](https://html.spec.whatwg.org/multipage/workers.html)；只观察 Dedicated Worker 平台接口和消息边界 | 两次均 305,340 bytes，SHA-256 `f768875a62c65ef50b08c7e1126084148b3f1b9e74037165601b22399aaee429`；Last-Modified `Fri, 28 Aug 2026 06:43:45 GMT` | 立即二读相同；living standard 不是浏览器实现或待打包依赖许可证 |

SHA-256 只说明本轮读取到的响应字节，不是数字签名、作者身份、发布者身份、真实性或许可授予证明。未保存响应正文意味着该账是 link-only retrieval receipt，不是可离线重放的完整证据包。

## 3. 五类依赖分账

| 依赖 | 当前公开观察 | 仍然缺失 | 当前状态 |
| --- | --- | --- | --- |
| `python_runtime` | PSF legal overview | 精确实现、版本、架构、二进制来源、打包方式、incorporated software、SBOM、NOTICE、支持期与更新策略 | `undecided_not_selected`；review/legal/redistribution 均 false |
| `swiss_ephemeris_code` | commit-pinned LICENSE、动态 overview、Professional contract 模板 | 精确版本与 binding、AGPL 或 Professional 路线、修改项、合同主体与签署／付款、法律复核 | 同上 |
| `ephemeris_data_files` | Swiss LICENSE、overview、合同模板都指向同一候选产品家族 | 精确文件集合、每文件 digest／覆盖区间／来源、JPL 与派生数据权利链、逐文件再分发依据 | 同上 |
| `dedicated_worker` | WHATWG living standard | 实际 Worker 脚本、WASM／runtime、imports、浏览器版本矩阵、bundle SBOM 与第三方 notices | 同上 |
| `loopback_local_service` | 无 evidence ref | 服务语言/runtime、framework、packager、installer/updater、端口／bind／origin／auth、安全与全部传递依赖许可 | 同上；公开观察覆盖为 0 |

Astrodienst 的 [公开许可说明](https://www.astro.com/swisseph/swephinfo_e.htm)描述 AGPL 与 Professional 两条路线，并要求在分发或激活公共服务前选择；[Professional contract 模板](https://www.astro.com/swisseph/secont_e.pdf)同时列出代码和压缩数据文件。这里只登记供应商页面表达与响应身份，不判断 AGPL 兼容范围、合同效力、商业适用性或本项目应选择哪条路线。

PSF 页面只说明 Python 许可家族；没有选择精确 Python 发行版，因此不能据此回答具体二进制、嵌入包、第三方组件或安全支持期。WHATWG 页面只定义平台 API；不能据此推导 Edge／Chrome 的真实实现通过，也不能替代 Worker bundle 的逐组件许可审计。

## 4. 作品、版本、载体和法律判断继续分账

- 作品／项目层：只观察公开发布者或维护者端点的自述，没有独立完成权利主体真实性核验。
- 版本层：只有 Swiss Ephemeris LICENSE 使用 commit pin；Professional 模板只有 HTTP Last-Modified；Python 页面无精确 runtime 版本，WHATWG 是 living standard，Astrodienst overview 的两次正文摘要不同。
- 载体层：当前载体只是 HTTPS 响应，采取 `link_only_no_source_body_stored`；没有把网页、PDF、LICENSE、代码、数据文件或现代整理内容复制进仓库。
- 法律层：`reviewComplete=false`、`legalConclusionEstablished=false`、`redistributionAuthorized=false`；未签合同模板不产生合同权利，公开可下载也不自动等于允许公开再分发。
- 所有者层：尚未选择 runtime、许可路线、产品分发方式或部署方式；任何后续选择都必须单独记录，不能由本回执自动推导。

## 5. 当前仓内依赖事实

限定范围的只读盘点观察了 33 个 package manifest、160 条依赖声明、两份 lockfile 和 765 个 `apps`／`packages` scoped paths：未发现 Python／Pyodide、Swiss Ephemeris／pyswisseph、`.se1`／JPL 数据、吠陀 Worker 或吠陀 loopback service 的依赖声明、命名实现或数据包。仓内已有的通用 Worker、Vite `127.0.0.1`、西洋 Astronomy Engine 与跨体系 `vedic` 占位符都不是吠陀运行时实现，也不得继承为许可证据。

该观察只描述本次限定仓库范围；它不证明主机没有安装相关软件，不证明隐藏或未来依赖不存在，也不建立全仓语义不存在性。

## 6. 不得晋升的结论

本回执不建立：

- 任何 runtime option、依赖、版本、binding、数据集或服务选型；
- 许可兼容、合同生效、商业使用、复制、修改、公开服务或再分发的法律结论；
- 作品／版本／载体三层权利闭包；
- build、bundle、性能、浏览器、PWA、Service Worker、公开主机或运行时执行证据；
- 吠陀 input／fact／rule 正式准入、来源 Binding、专家真值、内容真值或高风险政策 gate；
- mutation epoch、跨文件原子快照、区间 mutation 或 ABA 排除；
- 发布就绪或公开发布授权。

项目默认发布治理仍固定为 `legacy-v13 / targetSchema 13 / migrationId null`；吠陀自身 `releaseIdentity/targetSchema/migrationId` 仍为 `null/null/null`。`publicDeploymentAuthorized=false`、`expertClaimsAuthorized=false`、`releaseReady=false`、`publicReleaseAuthorized=false`。

## 7. 后续才能回答的问题

下一步必须先由所有者选择候选 runtime 与分发方式，再按精确版本／commit／binary／data file 建立 SBOM、NOTICE、每文件 digest、来源载体、支持期、更新策略与私有法律意见。若选择 Professional 路线，已签署、付款和双方身份材料只能进入授权的私有保管链；若选择 AGPL 路线，项目范围、源码提供和兼容性必须由现实法律责任主体判断。任何一条路线都不能由生成模型自动批准。

## 8. 当前机械身份与单向接线

authoritative evidence child 的四份机械身份为：JSON 23,919 bytes／`ed51e68438bc3b00613986e082d6064ceea41e9f75dc9992613e1b91449105e1`，library 47,235 bytes／`735fafecf2695d0acbd874635e96d70096f54f8527c42a3f210442799bd643c4`，CLI 2,132 bytes／`7fd781d310c7a04b1f9fe509e2e780c036819cd5c9f12b17cbe9325c0a3cafd9`，test 15,966 bytes／`c9b5ee474f94c457259f01326e9b6278f135848ca765028db2c245bf00b08036`；domain-separated `evidenceDigest` 为 `cbd459847bd4f57450f8389f127aa76c424298511786811b17c3cb88a856c926`。

child 自身只绑定固定 ADR，`childBindsRuntimeProposal=false / childBindsParent=false / childBindsCentralRegistry=false`。更新后的 runtime proposal 以 `runtime_dependency_license_evidence_to_runtime_proposal_aggregator_only` 单向聚合该 child；原有 ADR→input→fact→rule 七节点线性链不变。父产品化账只冻结 runtime proposal 的更新投影并传递承接 child digest，不把 child 直接重读成同级父账材料，也不把它计为第 4 个产品工件、第 39 条来源 binding 或中央 registry 项。

矩阵证据引用数精确保持 `python/swisseph/data/worker/loopback = 1/3/3/1/0`。child 25/25、runtime proposal 53/53、parent 40/40 定向测试通过；全部 11 个吠陀 test files 在默认模式与显式 `--test-concurrency=1` 两种运行下均为 471/471、0 fail／skip。该绿色只证明固定机械闭包与 fail-closed 攻击边界，不改变本页列出的法律、专家、内容、浏览器、运行时和发布红线。
