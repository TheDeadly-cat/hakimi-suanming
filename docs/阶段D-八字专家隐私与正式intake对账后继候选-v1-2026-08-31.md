# 阶段 D：八字专家隐私与正式 intake 对账后继候选 v1

日期：2026-08-31  
状态：candidate-only；zero-instance；opaque-only；active admission effect = none

## 结论

本 tranche 新增 [八字专家隐私与正式 intake 对账后继候选](../content/system-admission/bazi-expert-privacy-formal-intake-reconciliation.v1.json)。它只解决一个机械契约歧义：历史 expert packet／intake 投影仍带有 8 个“public identity binding”字段，而当前 authority/privacy precheck 已把仓内材料收紧为 `opaque_refs_digests_enums_and_zero_instance_counts_only`，并明确禁止 `legal_or_display_name`、PII、原始 credential／dossier／opinion、正文／quote／截图／认证路径和人物简介。

本 child 不改写历史 packet、intake 或 authority precheck，不建立收集 API，不保存真人实例，也不修复 formal packet。当前仍为现实专家 `0/2`、Binding `0/12`、packet artifact locks `11 current + 1 bazi-core drift`；formal packet 首失败仍是 `INTAKE_GAP_BINDING_DRIFT`。

## 单向父品牌与中央隔离

本 child 只消费两个实际 loader 私有品牌及其冻结身份：

| 父件 | Raw identity | Semantic identity |
|---|---|---|
| expert intake v1.1 | `32,579 bytes / 7a7088a3fdc660ccca3ef0d9765e36421563c7feee1c3dfa6bc4d7169d0f91df` | `hakimi.bazi.expert-review-intake-gap.version-aware-candidate/1.1.0 / 5318b9543bad20c52e5129901e94aa2ca3d2dd68f342ba11ab9611f4f767d582` |
| authority/privacy precheck v1 | `12,974 bytes / ccfe28e4dfb8c1bd7a6764d6253cf818c37a712ea24225bd100092362cc1f2af` | `hakimi.bazi.expert-authority-material-precheck/1.0.0 / 0c68a77a135a6ad6205a9c0da3e37162a820ee2f273cb899783fe9a990526a80` |

历史 formal packet 只通过已验证 intake parent 的模板投影进入对账，不是本 child 的 direct parent；本 child 不 import 或调用 formal packet verifier。Bazi Manifest v2、manifest observers、四体系 Registry v2、formal lifecycle、D0 和隔离 review-cycle kernel 均未消费本 child，也没有新增 backlink、active/default adoption 或中央 integration。

## 历史 8 字段 disposition

八个旧字段全部保留为历史说明，但统一降格为 `historical_template_only_not_collection_or_persistence_authority`：

| 旧字段 | 当前规则 |
|---|---|
| `reviewerId` | 禁止作为真人标识；未来只能使用固定 reviewer seat 与不含真人结论的 opaque context ref |
| `displayNameOrControlledPseudonym` | 禁止 |
| `verificationMethod` | 禁止自由文本；只能登记受控 method code 词汇 |
| `credentialDigest` | 禁止作为仓内 person binding；未来只能由 private runtime 使用 opaque credential evidence ref |
| `verificationDate` | 禁止直接持久化；未来只允许 opaque first-seen receipt ref 词汇 |
| `reviewScope` | 禁止自由文本；当前只保留四个固定 question IDs |
| `evidenceType` | 禁止自由文本；只能登记受控 evidence-category code 词汇 |
| `verifiedBy` | 禁止 |

候选定义的 15 个 opaque 字段只是一组 non-executable future-private-runtime vocabulary。`currentInstances=[]`、`collectionAuthorized=false`、`persistedRealPersonInstancesAllowed=false`、`privateRuntimeEndpointInvoked=false`；它们不证明匿名化、身份、资质、scope、独立性、同意、authority、custody、first-seen 或真实性。尤其 `personDataPresenceAssessed=false / personDerivedDigestExcluded=false / safeToPublish=false`。

## Formal 与权威红门

- reviewer seats：`0/2`；identity／credential／scope／independence／verifier authority／opinion／seal／bundle 全部为 0；
- Binding：`0/12`；source／rights closure 均未完成；
- packet locks：`11/12` current，唯一 drift 为 `packages/bazi-core/src/index.ts`；
- formal packet：本 child 未调用 verifier、未修复、未重签；首失败固定为 `INTAKE_GAP_BINDING_DRIFT`；
- 内容真值、专家真值、权利法律结论、release readiness、公开 release／deployment 与 expert claims 全部为 false／`not established`；
- disagreement 仍不得多数表决、平均或由生成模型自动选赢家；本 child 没有意见实例可供 reconciliation。

## Runtime trust 校准

红队首先淘汰了会被 post-import 污染的 `WeakSet`、`Object.freeze`、JSON、数组 iterator／setter、descriptor 继承、全局 `String`、inherited `toJSON` 和 `-0` canonical collision；随后又证明任意 hidden preload 已在 CLI 模块执行前取得同进程代码权限时，可以同时伪造 verifier 与自检原语。后一类权限无法靠同进程事后检测建立可信启动证明。

因此最终 artifact、loader summary 与 CLI observation 同时固定：

- `hiddenPreloadExcluded=false`；
- `nodeRuntimeIdentityEstablished=false`；
- `loaderIdentityEstablished=false`；
- `runtimeLauncherIdentityEstablished=false`；
- `cliOutputTrustedAttestation=false`；
- `visibleLoaderGuardIsSecurityBoundary=false`；
- `mechanicalResultAssumesNoArbitraryPreEvaluationCodeExecution=true`。

CLI 成功前缀因此使用 `..._OBSERVATION_OK`，不是 trusted attestation。可见 `NODE_OPTIONS`／`--import` guard 只减少误用；它不证明隐藏预载、OS 启动链、Node binary、loader 或 launcher 身份。逐文件 held-handle endpoint snapshot 也不构成跨文件原子快照；Schema 13 继续没有 mutation epoch receipt，interval mutation、ABA 与 replay 均未排除。

## 冻结身份与机械验证

最终身份：

| 文件 | Bytes | SHA-256 |
|---|---:|---|
| reconciliation artifact | 10,260 | `f99612bd68adbe42145b0cc92030f44d54ee91eed574617690de8a4f109c9a17` |
| verifier library | 28,185 | `3148f739708c8bc77757fcb47d87086c668238626470ee3c31b91d8c03a789da` |
| observation CLI | 8,674 | `a33d5bd79fb9a4a05d6db83bf98514b525206f5195f3098e3ddfa2af2b281ba3` |
| adversarial tests | 22,389 | `e464e645311919270cce9409075589805d1cadc0256074b577c0346885c6ac89` |

artifact semantic digest 为 `cdf1a4d73a07b99a915e19f24672adbf6a4b42fb98754a0d679b32ec91eca3a2`。

最终专用测试默认与显式 `--test-isolation=none --test-concurrency=1` 均为 `30/30`；successor + intake v1.1 + authority precheck 三文件闭包两种模式均为 `92/92`。successor observation CLI 与两个父 CLI 均通过。Bazi Manifest v2 与四体系 Registry v2 既有组合默认／串行均为 `29/29`，两个 CLI 均通过；这只证明它们未被破坏，不代表它们消费本 child。release-governance CLI 通过，根 `package-lock.json` 仍为 `175,812 bytes / 40a02c7c3166f0c9bc03afa9c1d1f342fb7f54efe1abbf7cb3f5cf6ecb482a4d`。

必须分开记录的 expected-red：formal expert packet CLI 仍 exit 1，首失败为 `INTAKE_GAP_BINDING_DRIFT`；manifest observer v1.2 当前仍以 `RAW_IDENTITY_DRIFT` exit 1。任何历史绿色、isolated kernel 测试或本 child 绿色都不能替代这些红线。

两路独立只读终审在把任意 hidden preload 从“可检测”改为“明确未排除且不形成 attestation”之后，最终均未报告剩余 P1／P2。一条终审仍登记了测试覆盖 P3：没有逐一为 URL-shaped ID、全部 11 个零计数、临时 workspace 父 raw drift 传播与中央 consumer 反向引用分别建立专用用例；当前 whole-ledger exact projection、两个父 loader 自身 raw pin 与本轮只读 consumer 搜索会失败关闭或显示未接入，但不能把这些外推为每一类未来路径的永久自动覆盖。该结论只覆盖列明的机械契约和 PoC，不是安全沙箱或可信启动证明。

## 分账

| 账本 | 本 tranche 结论 |
|---|---|
| 工程证据 | 双私有品牌、raw／semantic pin、strict JSON、canonical projection、opaque-only 零实例与对抗回归可机械复核 |
| 浏览器／运行时证据 | 未建立；CLI 只是一条不受信任启动上下文下的 mechanical observation，不是 runtime attestation |
| 内容真值 | `not_established` |
| 专家真值 | `not_established`；现实专家 `0/2`、意见 `0/2` |
| 来源／权利法律判断 | `not_established`；Binding `0/12`、三层权利未闭合 |
| 发布就绪 | `false` |
| 专家宣称授权 | `expertClaimsAuthorized=false` |
| 公开部署／发布授权 | `publicDeploymentAuthorized=false / publicReleaseAuthorized=false` |

默认治理继续固定为 `legacy-v13 / targetSchema 13 / migrationId null`。本 tranche 未运行全仓 typecheck、默认 Web build、完整应用、浏览器／PWA／Service Worker、公开主机、Release Evidence、部署或回滚，也未进行专家外联、第三方后台访问、网络取证或私人资料收集。
