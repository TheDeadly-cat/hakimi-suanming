# 四体系 v1 原文恢复与历史测试

本批从 `27a9527931db97355f52da3f5e4b861107810364` 开始，恢复此前缺失的 20351 字节西洋文档，并把旧四体系 v1 登记测试绑定到它实际声明的历史输入。生产验证器、当前选择、旧登记和既有归档均未修改。

## 20351 字节原文的来源

目标仍是 `docs/西洋星盘契约草案与来源门-v0.1.md`，原 SHA-256 为 `518116de8187893ddbe04542431681f6d17969e41b337988d189af6d977d9984`。现用的同名较新文档没有被覆盖。

在项目旧会话 `019fe758-875a-7433-9d6d-78144cb02d2d` 的归档中找到完整文件读取和实际落盘的变更：

- 原日志第 1426 行为读取命令，第 1428 行为完整成功输出，未被截断。
- 第 1622 行是修改请求，第 1623 行是实际 `FileChange` 差异，第 1624 行确认修改成功。
- 从输出中取出西洋文档部分，只移除 PowerShell 字符串输出追加的最后一个 CRLF，得到 19902 字节；不归一化文件自身换行。
- 实际差异在原第 135 行处唯一匹配：新增内容并移除一处原行末 CR，净增 449 字节。应用该差异后恰为 20351 字节，SHA-256 与旧登记中的原值完全一致。正反重放也逐字节一致。

只解析归档数据，没有执行历史命令或按摘要补写段落。先前统一换行得到的另一份 20351 字节候选哈希不符，因此未采用；大小相同不能替代原字节身份。

五条原日志记录原样保全为 42955 字节的证据片段，SHA-256 为 `717f660bae39f33ce9d81dff45c87ac03322445283134236fa55cb7c580cb139`。另存的 Python 标准库复算脚本核对片段身份、真实 hunk 行号、唯一上下文、正反重放及目标摘要，并拒绝覆盖输出。其独立重放成功。完整会话及原始记录仍在本机，不随源码上传。

## 历史输入与执行代码

此前已取得本登记声明的 53 项输入中的 52 项，缺少的正是这份原文。此次补齐到新的独立目录，先用原测试文件的逐字节副本调用当前验证器，9/9 通过；旧的 52/53 目录、缺失错误和不完整归档继续保留。

仓库测试复用原有 35 项 Bazi v1.7 归档，只增加其余 18 项，避免复制整份共同输入：

| 归档 | 项数 | SHA-256 |
| --- | ---: | --- |
| `scripts/fixtures/bazi-v17-domain-original-inputs.zip` | 35 | `b00795e335c132350204baabb7e92519dd94f614c789e44211d4b65f9e4908f5` |
| `scripts/fixtures/four-system-v1-additional-original-inputs.zip` | 18 | `27c0e45907802bfd27bd7ecf691613598106bf5dac12a155daef04c8d221ebf3` |

新增归档为 125698 字节。测试先核对两个完整 ZIP 的摘要、条目数量、相对路径和不重叠性，再核对原文的 20351 字节与目标 SHA，最后在自有临时目录写出 53 项原字节。读取与验证代码始终从当前源码导入，归档源码只作为数据。

旧登记摘要保持 `a992d58ce2c98b5282ffafafb38e05c20c1d294e99b150674fdf01684f86097a`；其 Bazi manifest 摘要保持 `60711da42ebc71e1f23ec300dffd5ed44f418c9bcc83ee394bf106f7354dc932`。这里的历史输入上下文不是完整历史检出、安装或浏览器运行重建。

## 回归结果

原九个测试函数体逐字节不变；只把第一个标题明确改为历史 v1 登记，并调整输入准备。新增三项测试覆盖：实际源码目录仍拒绝旧登记；原文缺失和同长度篡改均拒绝；两个原 ZIP 的篡改、截断及空包均拒绝。当前选择和准入状态没有借用历史成功。

```powershell
node --test --test-reporter=tap scripts/verify-system-admission-registry.test.mjs
node scripts/run-node-test-group.mjs current-governance
```

定向文件 **12/12 通过**。完整 current-governance 为 **28 个文件、753 项：551 通过、202 失败**，无跳过、取消或缺失文件结果，整组仍退出 1。

与最近完整基线 `cd6630f` 的 750 项逐项比较（之后 `27a9527` 只增加同步文档），关闭原有八项失败，新增三项负例通过，没有其他结果变化。四条错误文本仅包含两个检出目录的绝对路径差异；原始差异保留，只有这些已知路径在比较中被替换为共同标记，未归一化错误码、正文或换行。其余错误文本一致。原 269 项累计关闭 **67 项**，剩 **202 项**。

执行日志、原文恢复片段、失败候选、逐项结果和后续源码/CI 验证分别保存在 `Z:/HakimiBaziBackups/LocalDelivery/2026-09-13/` 下的 `historical-session-recovery-v2`、`four-system-v1-input-audit-v1` 与 `system-registry-v1-history-v1`。远端 CI 只按其实际 head 和运行记录归属，不能把本机结果称为远端通过。

旧 Western README（SHA-256 `74d1faf827c0acf301a644af51d1435d3f6ba522b58af20f8f4002f09ade6aa2`）仍未恢复，其他历史验证尚未完成。真实来源/权利/专家材料也未因此增加；此次不改变生产参数、当前选择、5188/5189 安装或浏览器资料。
