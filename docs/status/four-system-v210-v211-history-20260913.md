# 四体系 v2.10 / v2.11 历史输入修复

源码基线为 `0ac140c98d0735f803e292e8f11dcae03cc1a569`，此前完整治理组为 777 项、713 通过、64 失败。本批原 v2.10 四项和 v2.11 两项在新工作区先完整复现为 0/6；加载器首先沿父链报告 `MANIFEST_IDENTITY_DRIFT`，CLI 的成功断言则收到现行入口的失败结果。

## 输入与适用范围

v2.10 在既有 754 项数据上补入 18 项至 **772 项**，包括 Western v3 选定路径清单、浏览器观察 v1 及其工具依据。v2.11 补入 23 项至 **795 项**；核对的显式依赖共 155 项，无冲突或缺失。这些范围由实际消费者决定，没有把选定路径清单扩大成完整领域或浏览器验收。

两版原摘要分别为 `768b4d46b90e6c33bf76910167b3423120e2dc3db97fbd3d83af97c53c0d217c` 和 `21d12ce28ba635f7e6ca8f79478421122705f12586266dc1866eb08c083cd2e9`。v2.10 新增数据均匹配 Git 原字节；v2.11 的 15 项匹配 Git，另 8 项 Playwright 代码或元数据匹配历史固定摘要、离线安装字节与锁定包版本。归档准备时曾错误要求依赖文件也必须受 Git 跟踪，该失败及更正均保留；已经生成的 v2.10 ZIP 未重写。

| 测试归档 | 字节 | 新增数据项 / ZIP 项 | SHA-256 |
| --- | ---: | ---: | --- |
| `scripts/fixtures/four-system-v210-additional-inputs.zip` | 90910 | 18 / 19 | `209251ac8ddb82435002b7d960f9485f695492b20e3edb05faf8cd2eec7662d4` |
| `scripts/fixtures/four-system-v211-additional-inputs.zip` | 923859 | 23 / 24 | `5394b872aa537143ef09130557c508eb3cdd1a07a9639ccbb8b667738366e11e` |

数据分别合计 351761 和 4549837 字节。额外 ZIP 项是新建的测试输入清单。解析先校验整个 ZIP 身份，再校验精确路径集合、唯一安全路径、逐项大小与摘要、总字节数。补入使用 `wx`，父输入不覆盖。归档代码只作数据读取，执行的是当前仓库加载器，未执行归档模块、旧安装、旧构建或浏览器。

## 测试修改与一次已修正回归

v2.10 原四个测试函数体逐字节不变；v2.11 原两个函数体仅把四处 `process.cwd()` 参数换为历史输入目录，原断言全部保留。新增六项测试覆盖当前入口不能继承历史成功、ZIP 损坏/截断/空集合，以及真实工具或依赖文件被同长度修改、删除时的拒绝。

v2.10 的原 CLI 函数由独立子进程通过固定静态导入调用，CLI 本体是当前源码的逐字节副本，测试桥接指向当前加载器。原 `argv`、`execArgv` 和环境检查仍由实际 CLI 函数判断；被拒绝的 loader 参数作为函数输入传入，不执行该 loader。真实源码 CLI 即使以历史目录为 cwd 仍失败。公共 helper 只增加实际需要的版本 10。

首轮关联 v2.9/v2.10/v2.11 **41/41 通过**，但第一次完整治理组出现额外边界回归：783 项中 **724 通过、59 失败**。既有边界规则拒绝测试新增的非字面量动态 `import()`，独立运行边界 CLI 也退出 1。修正为上述固定静态导入后，边界 CLI 退出 0，边界测试与 v2.10 两个完整文件 **95/95 通过**。原失败结果、原测试文件与补丁均保留；未放宽边界检查。

## 最终完整治理结果

再次运行 `node scripts/run-node-test-group.mjs current-governance`：完整 28 文件、783 项，**725 通过、58 失败**，无跳过、取消或缺失文件结果，约 432 秒，整组仍退出 1。最终结果 JSON 的 SHA-256 为 `b87da8fea3d4ced38935ac2b578c9891f77286245779e699196d97981bb253d6`。

与基线逐项比较，本批关闭原六项失败、新增六项通过，没有删除测试，其余结果状态不变。与第一次完整运行比较，仅上述边界测试从失败恢复通过。原 269 项累计关闭 **211 项，剩余 58 项**。41 项与 95 项检查有重叠，不相加为独立覆盖。

Windows 新检出与远端 CI 按最终提交另存结果。Quick CI 不包含完整历史治理组，不能用工程汇总代替本组结果。后续 v2.12—v2.14 已准备历史数据并独立加载通过，尚未接入测试，不计入本批关闭数量。

原 current 选择、生产代码、5188/5189 安装与专家资格状态未改动。历史浏览器记录、来源权利、领域真实性、工程通过和正式准入保持各自证据范围。

原日志及最终交付回执位于 `Z:/HakimiBaziBackups/LocalDelivery/2026-09-13/four-system-v210-v211-history-v1/`。输入发现分别保存在同级 `four-system-v210-input-diagnostic-v1/`、`four-system-v211-input-diagnostic-v1/`；后续准备位于 `four-system-v212-v214-prepared-inputs-v1/`。
