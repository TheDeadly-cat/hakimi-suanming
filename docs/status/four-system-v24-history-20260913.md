# 四体系 v2.4 历史输入与当前验证器

本批从 `428bb01882472e3d4541a903509d3fde6448c2a1` 开始，修复 v2.4 测试对历史输入与当前源码的混用。生产验证器、旧记录、当前选择、参数和现实准入均保持原样。

## 实际失败与恢复依据

基线完整治理组中，v2.4 文件的 19 项测试有 16 项失败。其原正例直接读取当前目录，首先遇到 v2.3/v2.2 父链中的 `MANIFEST_IDENTITY_DRIFT`。提供已验证的 v2.3 历史输入后，还必须补足 v2.4 新增的八字机器身份与零实例专家父记录，不能把整条链的失败归结为一个 Western 文件。

前期诊断保留了 620、625、649、652、653 项输入各自的失败：零实例专家子记录、v1.8 父记录、八字解释源码、实际使用的旧 readiness 文件、历史验证器源码依次缺失。没有往已记录的旧输入目录里补写文件来改变原失败现场。

`bazi-binding-freeze-requirements.v1.json` 在元数据中出现两种身份。实际父加载器要求 v1.6 的 **30655 字节 / `1db59e2f…809`**；v1.5 的 **26038 字节 / `662c91e6…201`** 是 intake-gap 中的历史比较元组。按实际消费代码选择前者，未合并或重写两份历史记录，也没有因为引用图中出现两个摘要就判定加载链不可能成立。

最后缺失的 `scripts/bazi-expert-review-packet-lib.mjs` 要求 **139453 字节**、SHA-256 `76046086e4d4b49cd7166b548f4b6999fb208db020ed9b06e1cdefa90c2cd669`。本地 Git 对象 `833442cfafee6612735135f7128d74de4b7f4d24` 完全匹配；现用文件为 144207 字节、`3259573a…23b`，未覆盖。当前 precheck 只读取并核对这份旧文件的原始身份，不从历史输入目录导入或执行它。

同时从冻结机器身份的 `orderedUniqueFileIdentities` 获取其完整 28 项组件身份，逐项核对现有输入和新增原字节。补齐后的 662 项输入由当前 v2.4 完整加载器验证通过，child digest 保持 `bc685bb8da2f4a285eda645ab883da002c526cc80d800f2406fe9e0386a04840`。

## 测试数据与代码范围

复用现有 v2.3 的 618 项输入，新增 44 项，共 **662 项**。新增 ZIP 为 `scripts/fixtures/four-system-v24-additional-inputs.zip`，**291844 字节**，SHA-256 `cc2edc9448ef388108141d0021ce586cc6b1e56a30017a8d194c9326b44f71a3`；包含 44 项数据和一份 `fixture-inputs.json`，数据总计 1321115 字节。现有 v2.2/v2.3 归档均未改变。

准备阶段核对整个 ZIP 的固定 SHA、清单身份、44 项唯一安全路径、每项大小和 SHA、总字节数与旧验证器原身份；新增文件采用 `wx` 写入，不能覆盖任何既有 v2.3 输入。全部归档内容仅作数据。临时目录清理继承父 helper 的实际父目录、目录前缀、dev/ino 与非链接目录检查。

验证器始终从当前源码导入。CLI 正例使用当前 CLI 的逐字节副本，邻接模块只转发到当前验证器 URL；公共测试 helper 仅将允许版本增加到 4。当前源码 CLI 即使从历史输入目录启动，也必须保持拒绝，不能借用历史根的成功。

## 原断言与验证结果

原 19 项测试全部保留：18 个函数体逐字节未改，另一个仅替换输入根参数；原 `fixture()` 中四个加载调用也仅调整输入根，原加载顺序和缓存行为不变。新增三项验证当前入口不继承历史成功、归档篡改/截断/空 ZIP 被拒绝，以及旧验证器同长度篡改、被现用文件替换或缺失时被真实递归加载器拒绝。

两个完整文件 v2.3/v2.4 首轮 **43/43 通过**（21 + 22），无取消或跳过，约 197 秒：

```powershell
node --test --test-reporter=tap scripts/verify-four-system-current-status-observation-child-v2-3.test.mjs scripts/verify-four-system-current-status-observation-child-v2-4.test.mjs
```

随后执行 `node scripts/run-node-test-group.mjs current-governance`，完整 28 个文件、762 项：**592 通过、170 失败**，无取消、跳过或缺失文件结果，约 294 秒，整组仍退出 1。

与 `428bb01` 的 759 项结果按文件、测试名和同名出现序号逐项比较，原 v2.4 的 16 项失败全部转为通过，新增三项通过；没有删除原测试，其他测试结果状态不变。原 269 项治理失败累计关闭 **99 项，剩 170 项**。提交后的 Windows/CI 按其实际源码身份另存回执，定向或工程 CI 通过均不代替该完整治理组仍有失败的事实。

上述机械验证不增加专家意见、权利或领域证据，也不验证本轮新的浏览器运行；5188/5189 安装未更换。

原始恢复、各次失败和完整输入记录保存在 `Z:/HakimiBaziBackups/LocalDelivery/2026-09-13/four-system-v24-input-diagnostic-v1/` 与 `four-system-v24-input-diagnostic-v2/`；本次测试与交付证据保存在 `four-system-v24-history-v1/`。后续版本需要分别验证其新增父链，不能直接继承本批通过。
