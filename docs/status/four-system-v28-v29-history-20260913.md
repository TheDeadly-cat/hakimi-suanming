# 四体系 v2.8 / v2.9 历史输入修复

源码基线为 `9a977f02db751862df4015d78d860ef0aae6af17`。最近完整治理记录来自其父提交 `1471219`：771 项中 669 通过、102 失败。其后只有焦点测试等待修正，治理加载器和本批原测试均未改变。

## 原因与输入身份

v2.8 原 25 项测试中有 19 项失败，并行初始化首先报告“Ziwei current old-schema projection 不符合固定漂移账”；v2.9 原 26 项中有 19 项失败，首先报告“历史 formal expert verifier helper 原始字节身份漂移”。单独调用这两版当前源码入口，都先沿旧父链返回 `MANIFEST_IDENTITY_DRIFT`。这些入口和失败顺序分别记录，未把它们合并成同一条原始异常。

v2.8 所需的 Ziwei v3 选定路径及上游绑定共核对 81 个唯一文件身份，其中 79 项已在 v2.7 的 748 项历史数据内；补入两份原 JSON 后共 **750 项输入**。这仍是选定路径范围，不是整个 Ziwei 工程目录完整性证明。

v2.9 在此基础上需要四份原记录：自身记录、Bazi machine successor v1.1、专家零实例子记录 v1.1，以及 createdAt 标签勘误，共 **754 项输入**。最初 753 项目录缺少勘误而失败，原目录和失败记录保留；补齐的勘误为 8821 字节，SHA-256 `cf387592c507660f3ec00f31b5b735c7e90eb6bf1a92aefbb361240a2518bb21`。其 `correctedCreatedAt` 仍为 `null`，没有编造更正时间、可信时间戳或真人专家意见。

两版完整加载器已分别核对原摘要：v2.8 为 `a3057d564024e6e5c52a01ab82b4bcc74cc863aea127b6556849b5dd7a2ee792`，v2.9 为 `0406537cf99b025644fb294068d9515c2c0a0ff4a0bf322edcbf39722447b6a1`。六份新增数据不与既有路径冲突，不覆盖父上下文。

## 测试归档与修改

| 新归档 | 字节 | 数据项 / ZIP 项 | SHA-256 |
| --- | ---: | ---: | --- |
| `scripts/fixtures/four-system-v28-additional-inputs.zip` | 18154 | 2 / 3 | `251f54d3f4d3167883aaf1ea31ead6dfbd3d864695bfc17ce82ef93084c66a3c` |
| `scripts/fixtures/four-system-v29-additional-inputs.zip` | 18106 | 4 / 5 | `81564dd8cae3ed456cf70583bc07d08b12fe1052d79ba87367f9a9fa7ca1a1b1` |

每份 ZIP 除原数据外包含新建的 `fixture-inputs.json`；数据分别合计 88023 和 70756 字节。解析先校验整个 ZIP 的固定身份，再校验清单、精确唯一安全路径、逐项长度和摘要及总长度。新增写入使用 `wx`，旧归档保持原字节。

原 51 个测试函数体逐字节不变。两处 `fixture()` 各只调整四个输入根，原并行初始化、缓存和断言保留。当前 CLI 使用原字节副本及指向实际当前加载器的测试桥接；真实源码 CLI 即使以历史目录为 cwd 也不能借用历史成功。公共 helper 明确增加版本 8、9。

新增六项测试分别检查当前入口、损坏/截断/空 ZIP，以及实际输入中的清单或勘误被同长度修改、删除时的拒绝。只把末尾 LF 改为空格仍会被原始字节或规范化校验拒绝，没有通过 JSON 语义相同来绕过固定身份。

## 验证范围

关联 v2.7/v2.8/v2.9 三个完整文件首轮 **90/90 通过**（33 + 28 + 29），无跳过或取消，约 78 秒。

随后运行 `node scripts/run-node-test-group.mjs current-governance`：完整 28 个文件、777 项，**713 通过、64 失败**，无跳过、取消或缺失文件结果，约 324 秒，整组仍退出 1。结果 JSON 的 SHA-256 为 `3a478d91bcc5a1b122fb5d72e2fa1df2799a4f4e87384e316c25afa145345787`。

与 `1471219` 的完整 771 项逐项比较，v2.8/v2.9 各关闭 19 项原有失败，合计 **38 项**；新增六项检查通过，没有删除测试，其余结果状态不变。原 269 项累计关闭 **205 项，剩 64 项**。Windows 新检出和远端 CI 仍按其实际提交身份另存证据，不能由本组结果推断正式准入。

归档源码只作为数据，加载器执行当前源码；本次历史输入核验没有执行旧构建或浏览器。原浏览器观察、工程身份、来源权利、领域真实性、专家资格和正式准入仍各有独立范围，current 索引和 5188/5189 安装未变。

本批日志、审阅和最终交付回执保存在 `Z:/HakimiBaziBackups/LocalDelivery/2026-09-13/four-system-v28-v29-history-v1/`；输入发现和早期失败保存在同级 `four-system-v28-input-diagnostic-v1/`、`four-system-v29-input-diagnostic-v1/`。Windows 新检出和远端 CI 按最终提交身份另存结果。
