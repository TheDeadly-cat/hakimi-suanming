# 四体系历史输入续作 · 2026-09-20

从 `9dff75f48457a31c2b8ec6ee5f38fab10c0e1fde` 续作，原完整 current-governance 为 28 文件、783 项、725 通过 / 58 失败。本批修复历史测试的输入适用范围，保留 v2.12—v2.14 及 observation registry v2 的 **53 个原测试函数体与全部冻结摘要**。没有修改生产加载器、current 选择或专家/发布资格。

## 已接入的输入

| 归档 | 数据项 | ZIP 字节 | SHA-256 |
| --- | ---: | ---: | --- |
| `four-system-v212-additional-inputs.zip` | 新增 12，累计 807 | 31940 | `16a0302b72f0b1839a6d36a647e8629061c83053d7a0a6b4536223f8a7f55977` |
| `four-system-v213-additional-inputs.zip` | 新增 31，累计 838 | 111038 | `0349b85f05512e449c6d72bfe73c3238fc60de291a54da486525dd22363dd06e` |
| `four-system-v214-additional-inputs.zip` | 新增 7，累计 845 | 30630 | `ed2885bc0115ce166c95bb8624aec61b51a70166f67fcd0147098aab27c76291` |
| `four-system-observation-registry-v2-original-inputs.zip` | 独立上下文 574 | 3016397 | `6948d3c4e592ea9a397d689e557db2d203eff83fce81220ebb533aa5b4dcfde3` |

前三份直接复用交接准备归档，数据字节及清单未重写。各 helper 先核对整个 ZIP，再检查精确条目集合、安全唯一路径、大小、逐项摘要及总字节数，以 `wx` 增补父输入。历史初始化移入 `before`，同时为三个加载 Promise 注册拒绝处理；原测试函数体不变。CLI 测试运行当前入口的原字节副本，并通过静态测试桥接加载当前库。公共 helper 仅明确增加所需版本 12、13、14。

observation registry v2 需要独立历史上下文。574 项共 12318499 字节：549 项与基线 Git 原字节相同，25 项可逐项追溯到已有固定历史 ZIP；完整路径/摘要/来源表保存在外部证据。Western 的七份旧实现或测试也分别与真实 Git blob 相符，包括 526 字节的 `rule-layer-bridge.ts`。Vedic 使用其当时的 downstream registry 与 Web 测试配置。现用同名文件均未被旧字节替换。

发现输入范围时使用读取捕获；随后独立、不带捕获地重放当前加载器及原 15 项测试。首份 554 项归档仅覆盖加载器，原测试为 14/15，缺少旧 CLI 的拒绝路径输入；扩为 574 项后原 15/15 通过，再接入仓库。两次归档及失败原文均保留。固定 registry digest 仍为 `fa521f30a40fd1c006e886c30f8da5295fbb82784db5671d59df2691117f3738`。

所有归档源码、历史依赖和锁只作输入数据；没有执行历史模块、安装历史锁或重跑历史浏览器。独立上下文不是通用于所有年代的资料集合。

## 验证与原失败保留

- 接入前，三个完整 v2.12—v2.14 文件复现为 **0/38**，包括原顶层 Promise 的未处理拒绝记录。
- 首轮关联五文件 **139/140**。唯一额外失败来自旧 main 切换后的 CRLF 检出残留：现行 v2.11 拒绝测试先遇到 `FROZEN_GOLDEN_DRIFT`。仅当移除 CRLF 后与现有 Git blob 完全相等时校正检出字节；四份实际源码改动原样保留。修正后该项 **1/1**，最终完整组也通过。索引对象列表校正前后完全相同。
- 第一次完整组 **792 项：767 通过、20 失败、5 跳过**。五项原本通过的符号链接测试因沙箱 `EPERM` 跳过，未计为通过。与基线逐项比较为关闭原 38 项、新增 9 项通过，无删除；五处跳过另列。
- observation registry v2 接入后完整文件 **18/18**，含原 15 项与三个新增拒绝测试。
- 经正规权限审批重跑完整治理组：**28 文件、795 项，788 通过、7 失败、0 跳过、0 取消、0 缺失文件结果**，约 747 秒，整组退出 1。

最终结果 JSON SHA-256 为 `f6c89957887fb1351a5494c42c2601f10b9eb8f40eca7a38fc7c26424adf7cd4`。按 file/name/同名出现次序对照基线：**51 项失败转为通过，新增 12 项通过，无删除、无其他结果状态变化**。原 269 项累计关闭 **262 项，剩余 7 项**。上述定向范围互相重叠，不相加为独立覆盖。

新增负例分别验证真实当前入口不能继承历史成功，ZIP 损坏、截断或空集合被拒绝，以及真实文件等长篡改、删除被拒绝。完整草稿边界文件 88/88 通过，没有放宽动态导入或生产隔离规则。

## 剩余事实回执七项：输入与消费者版本均需处理

真实源码上的首错仍是 `LEDGER_MISMATCH`，不是探索目录曾经缺少 comparison 文件的错误。独立诊断进一步恢复三份较早的 Bazi 输入：

| 文件 | 字节 | 原固定 SHA-256 | 恢复依据 |
| --- | ---: | --- | --- |
| `package.json` | 26719 | `79ed3e10c5e8b9da4a128d98b9f6c48fbf716d9f4432cef442ee27c355d68dd0` | 原会话完整读取记录 |
| `apps/web/bundled-knowledge-audit.ts` | 12538 | `13eb497173611862276fce6c16a9ff207778fa70e3a3eadd60296ae09a0503cb` | 原会话完整读取记录 |
| `scripts/bazi-binding-freeze-requirements-lib.mjs` | 34817 | `9483ca0895f1f8a5033ca55e53fad8dc762b786953e52f4f26d34541f87f2bd6` | 从 Git 原件反向重放两份实际历史补丁，结果精确匹配原摘要 |

这些字节仅进入独立诊断目录，没有覆盖现行源码，也未计作测试关闭。首个反向重放因上下文重复而未恢复成功；原失败保留，改为按补丁内的原始 hunk 顺序匹配后恢复精确摘要。

补齐该时期 Bazi 输入与 Western bridge 后，当前事实回执验证器进入 `REGISTRY_MISMATCH`。逐字段比较确认至少存在以下不能仅靠增补输入解决的合同差异：

- 旧回执绑定 Ziwei `v0.1.0.json`、digest `fe646d35…c79`；当前独立 manifest 消费者明确选择 `manifest.v2.json`、digest `f16f4e02…b34e`，组件集合也不同。
- 旧回执中的 Bazi expected preview 为 `8d1a11a1…769`；当前 D0 消费者固定预期 `be637b1e…954`。不能通过重签回执把两者视为同一观察。
- 旧来源 requirements、旧中央 artifact locks 与较新输入还存在独立时代差异；完整逐字段比较保留在诊断证据。

下一步应明确此回执的历史验证合同与对应原始输入，保留实际当前入口的拒绝。不能执行旧归档模块代替当前验证器，不能全局退回旧 manifest 选择，也不能将真实生产者重放结果转签到旧回执。本批七项继续原样失败，未伪造关闭。

## 产品与交付边界

5188 固定版本及 5189 已选候选均未变更；`4de42e9db980` 安装替换仍待明确选择。默认继续为 `legacy-v13 / targetSchema 13 / migrationId null`。本批历史测试不产生新安装包或浏览器验收，也不替代真实研究周期、另一台 Windows 验收、来源校勘/权利与合格独立专家原始意见。

普通开发分支同步及精确提交的 CI 回执另存。Quick CI 不运行完整 current-governance，远端工程汇总不能掩盖这里的七项失败，正式专家门也仍须独立报告。

新证据目录：`Z:/HakimiBaziBackups/LocalDelivery/2026-09-20/four-system-history-contexts-v1/`。包含原失败、两轮完整报告、逐项比较、归档来源、历史恢复、补丁和精确提交 CI 记录；旧交付目录未修改。
