# 受控 Git 初始基线：清单、门禁与提交证据（2026-08-10）

## 1. 审计结果

| 项目 | 结果 |
| --- | --- |
| 审计前未跟踪文件 | 约 2858 个（含 `tmp/` 中浏览器 profile、token 数据库、缓存与构建产物） |
| 加入忽略规则后 | 575 个源码/文档/设计文件待纳入 |
| 敏感文件名扫描（env/secret/token/private key/pem/pfx/p12/key/sqlite/db 等） | 0 命中 |
| ≥ 2 MiB 文件 | 0 个（最大为设计概念 PNG 约 1.29 MiB） |
| 生成物残留（dist/.vite/playwright/log 等） | 0 |

`.gitignore` 新增排除：

- `tmp/`（含 Edge profile、token 数据库、缓存、旧构建、日志）；
- `test-results/`、`playwright-report/`、`blob-report/`、`.playwright/`；
- `.env` 与 `.env.*`（保留 `.env.example`）；
- 原有 `node_modules/`、`dist/`、`coverage/`、`.vite/`、`*.log`、`.DS_Store`、`Thumbs.db`。

## 2. 纳入基线范围

- 根配置：`.gitignore`、`package.json`、`package-lock.json`、`tsconfig*.json`、`THIRD_PARTY_NOTICES.md`、`README.md`；
- `apps/`：Web 应用、E2E 与发布协议；
- `packages/`：八字、紫微、西洋契约与引擎、存储、备份、研究查询等包；
- `docs/`：产品、领域、工程、验收与交接文档；
- `design/`：原创视觉规格、概念图和 QA 截图；
- `content/`：随包知识 manifest（当前正文为 0）；
- `scripts/`：构建、审计、桌面入口脚本。

## 3. 轻量门禁（当前代码）

| 门禁 | 结果 |
| --- | --- |
| `npm run typecheck`（含草案隔离 precheck） | 通过 |
| `npm run test:system-contract-draft-boundaries` | 42/42 通过 |
| `npm run build` | 通过，构建身份 `legacy-v13 / hakimi-bazi-research / targetSchema 13` |

未运行：全量 Vitest、跨 Schema E2E、容量与长浏览器矩阵；这些属于后续发布门，不并入初始基线声明。

## 4. 初始提交

初始基线提交哈希：`6fdece411cdc85288437a138c4ebc4c355da463f`（`6fdece4`，master 根提交，575 个文件）。

提交内容仅为上表纳入范围；`tmp/` 等运行状态不进入版本库，也不被删除。
