# Schema 16 / Web v1 证据增量（2026-08-12）

> 性质：只记录 2026-08-12 在当前未提交工作区取得的新证据。它不覆盖 2026-08-10、2026-08-11 的历史记录，也不是发布、提交或 Schema 晋级授权。

## 1. 本轮起点

- 分支/HEAD：`master` / `85c7f96`。
- 运行测试前 `git status --short` 为 45 个条目：29 个已跟踪修改、16 个未跟踪条目；与新任务短交接的预期一致。
- 所有既有修改与未跟踪文件均按用户工作保留；本轮未执行 `git add`、`git commit`、`git reset`、清理或干净 worktree 切换。

## 2. 默认发布身份：本轮新证据

先审计当前源码的默认选择链：普通 `apps/web/vite.config.ts` 使用 `DEFAULT_VITE_RELEASE_DATABASE_DESCRIPTOR`，其非隔离 fixture 路径固定返回 `BRIDGE_RELEASE_DATABASE_DESCRIPTOR`。

定向验证：

```text
vitest run release-protocol.test.ts current-release.test.ts pwa-files.test.ts
3 files / 18 tests passed
```

随后使用普通 `apps/web/vite.config.ts` 和 `--configLoader runner` 构建到系统临时目录，没有覆盖仓库内 `dist/web`。从新产物独立解析 HTML meta 与 Service Worker 描述符：

```text
HTML: legacy-v13 / targetSchema 13
Service Worker: legacy-v13 / targetSchema 13
build hash: e69c67ad256c
```

结论：本轮当前源码的默认发布身份仍为 `legacy-v13 / targetSchema 13`；该结论不把 v16 candidate 提升为默认发布代。

## 3. `web-v1-flow:v13-to-v16`：本轮新证据

为避免重跑全部长矩阵，本轮只运行交接中明确缺少当前复验的单条连续 Web v1 流程，并按浏览器串行执行：

```text
Chrome: 1/1 passed, about 1.5 minutes
Microsoft Edge: 1/1 passed, about 1.4 minutes
Total: 2/2 passed
```

两次均使用隔离的 `vite.production-v13-to-v16.config.ts` candidate、端口 `4200` 和单 worker；候选构建成功，测试完成后 webServer 正常退出且端口释放。该 spec 当前覆盖同一批数据贯穿案例/修订、四盘对照、证据链、生命周期、事件、检索、离线匿名导出、full v1.2 十六分区清空与全新 context 恢复、二次导出摘要一致，并在源 context 与恢复 context 断言零意外外网请求及零 console 问题。

## 4. 历史证据与本轮证据分界

以下只保留为历史记录，本轮没有重跑：

- 2026-08-11 全量 Vitest 147 文件 / 1541 项；
- 2026-08-11 v13→v16 故障矩阵 Edge 13/13 + Chrome 13/13；
- 2026-08-11 Schema 16 clean 万条容量门；
- 2026-08-11 P2-05 重数据集、紫微 4218、西洋 4219、跨体系 4220 浏览器门。

本轮新增且可表述为当前证据的只有：默认发布身份定向门 18/18、隔离默认构建身份与 hash，以及 `web-v1-flow:v13-to-v16` Edge/Chrome 2/2。

## 5. 未关闭边界

- v16 仍是隔离 candidate，默认发布仍是 v13；
- 未运行全量 Vitest、完整 13/13×2 故障矩阵或其他长门；
- 未取得 Firefox、固定 Android Chrome/WebView、真实系统安装/分享/打印 UI 证据；
- 未取得 JPL Horizons 官方字节、专家真值、内容权利签字或共同工作台正式入口授权；
- 本轮工程通过不增加 360 金标 `verified` 数量，也不构成 Web v1、Android 或专家验收完成声明；
- 未绕过 mutation epoch，也未用 raw IndexedDB 伪造 v16 数据。

## 6. 后续内部增量：西洋基础内容审稿闭环

同日后续在隔离 4219 草案中新增 43 项西洋基础内容审稿模板与只读预检：10 天体、12 星座、
12 宫位、5 相位、4 轴点，绑定 31 来源与三个 SHA-256。该模板不包含个人星盘事实，也不覆盖
默认盘 73 张动态组合卡；自述身份、条件化方向提案和建议保留／退修／驳回均不自动写入规则或存储。

本增量新执行：定向 Vitest 2 文件 / 10 项、双包及全体系 TypeScript、95-module 4219 build、
应用内 Browser 主链，以及新增场景 Edge + Chrome 各 1/1、共 2/2。没有重跑或借用 v0.5 的
历史 8/8 故障矩阵。体系草案聚合门最终为边界 42/42、Vitest 16 文件 / 110 项通过；聚合首轮
顺带发现并修正一个紫微测试夹具在来源路径超过 15 项后产生 128 位假摘要的问题，运行时协议未变。
默认生产构建仍为 `legacy-v13 / targetSchema 13 / migrationId null`。

详细契约、模板摘要、截图和历史／本轮证据分界见
`西洋内容-v0.6-基础内容审稿反馈模板与只读预检-2026-08-12.md`。该工程闭环不建立科学有效性、
专家真值、正式吉凶、事件结果、JPL 官方字节、严格成功回执、正式入口或发布授权。

## 7. 后续内部增量：西洋当前盘动态候选审稿

同日继续在隔离 4219 草案中补齐 v0.6 明确留下的动态组合审稿缺口。v0.7 只从当前
`WesternContentProjection` 生成模板，移除姓名、UTC、地点、request 与表单原值；但精确落位、
宫位、相位和守护链仍构成可关联的敏感星盘指纹，因此不宣称匿名。下载由用户主动触发，程序不自动
上传或缓存；导入只在当前内存命盘上重建并核对事实、完整投影、有序候选、来源账和逐卡快照摘要。

默认盘实测为 73 卡，换 UTC 后为 70 卡，证明候选总数由实际相位数派生而不是硬编码。有效反馈只读
显示 1/72；重算后旧 73 卡文件在 70 卡新盘失败关闭，非法 UTC 再清空并禁用。43 项基础审稿始终
保持独立 0/43，动态决定不写规则、存储或正式入口。

本增量新执行：定向 Vitest 2 文件 / 12 项、体系草案边界 42/42、体系草案 Vitest 17 文件 / 117 项、
western 双 tsconfig、全体系草案 typecheck、根 typecheck、96-module 4219 build，以及新增动态流
Edge + Chrome 2/2 和原基础审稿相关回归 2/2。应用内 Browser 桌面与 390px 完成下载、有效导入、
换盘拒绝及非法输入清空，横向溢出与控制台问题均为 0。没有把未重跑的历史 8/8 或其他长门混入本轮。

普通生产 build 同期为 2051 modules，产物 HTML 与 `sw.js` 仍独立解析为
`legacy-v13 / targetSchema 13 / migrationId null`。详细账见
`西洋内容-v0.7-当前盘动态候选审稿反馈与只读预检-2026-08-12.md`。这些仍只是隔离草案工程证据，
不建立专家身份、签名、科学有效性、吉凶／事件结果、JPL 官方字节、Android、正式入口或发布授权。

## 8. 2026-08-13 后续内部增量：紫微当前盘三方四正四段式直读

在隔离 4218 草案中新增 `ziwei.palace_sanfang.four_part_synthesis_candidate/0.1`。它只从既有十二宫问题域、十四主星 168 条落宫候选、逐星／逐宫复核包、本命生年四化事实与三方四正几何派生 12 份当前盘候选，每份固定按“本宫主题—外部牵引—资源／压力观察—矛盾合成”显示四段。空宫不借对宫或三合主星补写，无四化目标保持空集合；主导主题、资源／压力方向、吉凶、事件和结果仍全部 `null`。

本增量新执行：adapter Browser 投影 Vitest 1 文件 / 22 项、adapter 与 workspace Browser 双 typecheck、草案隔离边界门、105-module 4218 build，以及新增场景 Edge + Chrome 各 1/1、共 2/2。两浏览器均覆盖切宫替换、真实空宫、不含四化的当前三方组、零 Revision／epoch／bytes 写入、390px 零横溢和零页面控制台问题。应用内 Browser 另行完成同一路径；既有库基线 `0 / 5 / 0 B` 保持不变。移动端首轮截图发现跳转链接遮挡首卡，修正后重新执行 2/2，最终截图已通过视觉复核。

当前源码的默认发布描述符再次读取为 `legacy-v13 / targetSchema 13 / migrationId null`，发布身份定向测试 3 文件 / 18 项通过；本增量没有重跑普通生产 build，也不借用 2026-08-12 的 2051-module 构建作为本轮新证据。紫微 Browser 源码图从 17 增至 18 个 TypeScript 文件，系统草案隔离门仍证明其不进入生产图。

详细合同、来源边界、截图摘要、历史／本轮证据分界与下一目标见 `紫微内容-v0.11-当前盘三方四正四段式直读候选与证据-2026-08-13.md`。该阶段确定的下一内容项——核心十二星基础候选与 `12 × 12 = 144` 条落宫中性候选——现由下方第 9 节单独记录；v0.11 当时没有提前取得它的实现或测试证据。传统分组不得直接等价为个人吉凶，也不得按类别或数量评分。本轮仍不建立专家真值、正式吉凶、Android、JPL 官方字节、正式入口、提交或 v15/v16 默认代。

## 9. 2026-08-13 后续内部增量：核心十二辅煞星与十二宫 144 条中性候选

隔离 4218 草案新增 `ziwei.core_minor_star.neutral_candidate/0.1` 与 `ziwei.core_minor_star_all_palaces.neutral_candidate/0.1`。它只冻结左辅、右弼、文昌、文曲、天魁、天钺、擎羊、陀罗、火星、铃星、地空、地劫十二个精确星键，形成 12 条基础候选和 `12 × 12 = 144` 条落宫候选。传统 `supporting_six / challenging_six` 只作分组元数据；地空与古籍“天空地劫”的命名冲突显式登记，古典定位不充当地空精确语义支持。禄存、天马、天空、其他 minor 与全部 auxiliary 的两个候选字段保持 `null`，不按类别广播内容。

本增量新执行：adapter 投影 Vitest 1 文件 / **25 项**，adapter 两个 tsconfig 与 workspace Browser app typecheck，系统草案隔离门，以及 **106-module** 4218 build 全部通过。默认发布身份定向门 3 文件 / **18 项**仍为 `legacy-v13 / targetSchema 13 / migrationId null`。Edge、Chrome 的新增核心十二星场景各 1/1、共 **2/2**，v0.11 四段式定向回归另为 **2/2**；两浏览器均覆盖命宫陀罗、四个 auxiliary、禄存／天马空候选、连续切宫、完整存储快照前后相等、`revisionCount 0 / mutationEpoch 0 / totalBytes 0 B`、390px 零横溢和零页面控制台问题。应用内 Browser 后端不可用，未把它计入本轮证据；旧 4218 长故障矩阵、整仓全量与普通生产 build没有重跑。

详细合同、来源等级、截图哈希、历史／本轮证据分界与下一目标见 `紫微内容-v0.12-核心十二辅煞星基础与十二宫144条候选证据-2026-08-13.md`。所有候选仍固定 `goodBadOrientation / eventOutcome / result:null`，专家真值、直接结果与评分均关闭；本增量不建立正式吉凶、Android、JPL 官方字节、正式入口、提交或 v15/v16 默认代。

## 10. 2026-08-13 后续内部增量：八字旺衰政策单一事实源与当前盘命中复核包

八字内容 v0.17 新增可序列化 `strength-policy.ts`，把当前执行中的月令 4、透干 2、首位藏干 2、其余藏干 1，以及五档阈值、十神分组／方向、六个敏感性场景和四个方法审稿问题收敛为单一政策账。旧 v0.14 的“4/2/1”只是历史文字漂移；本轮没有把纠偏解释为算法新真值或专家认可。

新增 `hakimi.bazi.current_chart_hit_review/0.1.0` 当前盘复核包：只包含 4 个旺衰方法项、当前盘实际透干／藏干十神出现项，以及用户显式打开神煞研究后实际命中的落柱项。文件与当前内存事实、政策、敏感性、出现项、来源、有序 ID 和完整候选快照做域分隔 SHA-256 绑定；错盘、篡改、跨 Revision 或迟到异步结果失败关闭。具名审稿仍是自我声明身份，不是签名或专家认证；所有正式吉凶、事件、结果、自动整合与存储写入保持关闭。

本增量新执行：v0.17 定向 Vitest 6 文件 / **57 项**、默认身份定向 Vitest 3 文件 / **19 项**、根 typecheck 与草案隔离边界通过；应用内 Browser 在新鲜 4215 origin 完成桌面和 390px 检查；独立 Edge + Chrome 各 1/1、共 **2/2**。双浏览器覆盖 R2 有效导入、篡改清空、恢复有效、切 R1 清空、R2 文件跨盘拒绝、完整 IndexedDB/local/session/cache 快照前后相等、URL 与 2 条 Revision 不变、390px 零横溢和控制台零问题。E2E 构建为 2053 modules、build `dfb13e14bb74`，HTML 与 Service Worker 仍为 `legacy-v13 / targetSchema 13`。

这些是本轮新证据；v0.14/v0.16、整仓 147 文件、Schema 16 故障矩阵、Android 与其他体系长门没有重跑，不并入上述计数。完整合同、来源使用边界、截图摘要和下一目标见 `八字内容-v0.17-旺衰政策单一事实源与当前盘命中复核包-2026-08-13.md`。本轮没有提升默认代、绕过 mutation epoch、自动采用人工反馈或建立命理／科学真值。

## 11. 2026-08-13 后续内部增量：八字 v0.18 来源—主张账与旺衰证据叙事

本节与第 10 节分账。第 10 节的 57 项、19 项、Edge／Chrome 2/2 和 build `dfb13e14bb74` 均为 v0.17 历史证据；下述 v0.18 数字来自本轮新执行门禁，不回填或覆盖 v0.17。

当前源码已经形成以下可只读审计的 v0.18 事实：

- `hakimi.bazi.strength_claim_registry/0.1.0` 登记 8 个来源、12 个精确 binding 与 12 条 claim；
- 三个 pinned Wikisource revision 分别为《滴天髓阐微》`oldid=2600158`、《三命通会》卷五 `oldid=2706483`、《三命通会》四库本卷十 `oldid=761703`；它们只支持传统语境，不支持工程参数；
- 《渊海子平》`oldid=2593607` 因页面不完整、来源未核保持 `source_warning_unresolved`；《子平真诠评注》CText 页面 revision 与原文／评注层未冻结，二者对应 claim 都是 withheld；
- `hakimi.bazi.strength_evidence_narrative/0.1.0` 按月令、透干、首位藏干、其余藏干逐项消费当前因素，公开月主气 `4 + 2` 双计、支持／需求汇总、五档区间和六个工程扰动；
- 当前盘复核协议升级为 `hakimi.bazi.current_chart_hit_review/0.2.0`，新增 narrative、claim registry、有序 evidence IDs 与 narrative IDs 摘要绑定；
- Web 新增只读“旺衰为什么落在这一档”账本。边界仍为 review inheritance、network、mutation、expert truth、scientific validity、formal activation 全部 `false`，吉凶／用神／格局／事件／结果全部 `null`。

4/2/2/1、0.25/0.43/0.57/0.75 和六情景只属于本仓工程政策；外部古籍只能说明传统问题域。固定 `oldid` 不等于与影印本逐字校勘，历史作品公版候选也不等于数字转录层可任意再分发。Wikisource 复用须另核页面许可、署名和相同方式共享要求；CText 当前只链接和人工定位。

本轮 v0.18 定向证据：

| 门 | 当前状态 |
| --- | --- |
| v0.18 定向 Vitest | **4 files / 27 tests PASS** |
| 默认发布身份定向 Vitest | **3 files / 23 tests PASS**；`legacy-v13 / targetSchema 13 / migrationId null` |
| 根 typecheck、草案隔离边界 | `npm.cmd run typecheck` **PASS**；草案隔离 verifier **PASS** |
| E2E 构建 | **2058 modules**；build `bc2c28f15407`；HTML／Service Worker 为 legacy-v13/schema13 |
| 应用内 Browser | 最新壳桌面 + 390×844；12 claims / 12 unique bindings；月主气 `4 + 2 = 6`；四个共享 SHA 一致；overflow 0、overlay 0、console warning/error 0；legacy-v13/schema13 |
| Edge／Chrome | 各 1/1，共 **2/2 PASS**；测试 30.2 秒，总命令 34.3 秒；完整 IndexedDB/local/session/cache 前后相等；跨 Revision 失效关闭；`.last-run` 为 `status: passed` |

Edge 与 Chrome 同状态截图字节一致：桌面 SHA-256 `6fa6c663af55a14ae7c809f7f01c3bc35639fa2cdec551d1906e473c349ab276`，390×844 SHA-256 `e1318b5808decaa04f7cd0908946a16318168e500656b784f21ee24339bf72d9`。

本节只声明上述定向收口；没有重跑整仓 147 文件 / 1541 项、Schema 16 完整故障矩阵、Android、Firefox 或其他体系长门。完整字段、locator、版权边界、旧 69 项队列仍为空 locator 的债务，以及既有“偏有利／需警惕”不能冒充专家吉凶的边界，见 `八字内容-v0.18-来源主张账与旺衰证据叙事-2026-08-13.md`。本增量不改变默认 legacy-v13，不绕过 mutation epoch，也不建立提交、Android、专家、正式入口或 v15/v16 晋级授权。下一 P2 是隔离紫微 v0.13 核心十二辅煞 × 三方四正 occurrence 动态审稿包。

## 12. 2026-08-14 后续内部增量：紫微 v0.13 整盘核心辅煞三方四正动态审稿包（定向收口）

本节与第 9 节 v0.12 严格分账。第 9 节的 25 项、相关 typecheck、106-module build、默认身份 18 项以及 Edge／Chrome 2/2 都是 v0.12 历史证据，不能作为 v0.13 通过结果。

当前源码已声明 `ziwei.core_minor_star.sanfang_occurrence_review/0.1` 与 `hakimi.ziwei.core_minor_star_sanfang_review_feedback/0.1.0`：一份当前整盘包固定覆盖 12 个目标组；标准盘的十二个核心辅煞事实各进入 `self / opposite(+6) / trine(+4) / trine(-4)` 四组，形成 48 个有序 occurrence。空本宫不借对宫或三合星。五来源登记只复用 v0.12 的两条星曜、两条宫位来源与既有三方几何来源，没有新增星义语义来源；几何只支持关系位置。

切宫只过滤显示，不改变整盘包；编辑输入、重算、重开、换盘或失败会令旧动态包失效。模板不含直接身份或原始输入，但派生四宫、星曜、亮度与四化仍属敏感事实；SHA-256 不是加密、签名、匿名化或身份认证。预检保持零网络、零存储、零 mutation epoch 写入，不继承决定到 v0.12 静态 12／144 候选或旧 v0.10 静态 48 项反馈。formal activation、scoring、自动整合和直接结果均为 `false`，`goodBadOrientation / eventOutcome / result` 均为 `null`。

v0.13 本轮新执行 adapter 定向 Vitest **2 files / 30 tests PASS**、三个相关 typecheck、草案隔离 boundary verifier 与默认身份定向门 **3 files / 18 tests PASS**；默认描述符仍为 `legacy-v13 / targetSchema 13 / migrationId null`。4218 隔离 build **108 modules transformed**，源码图为 **22 paths = 21 TypeScript + 1 JSON**。应用内 Browser 在桌面与 `390 × 844` 完成 12 reviews／48 occurrences、702,355-byte 模板实际生成与导入、切宫保持、篡改及跨盘失败关闭，页面保持 `0 Revision / epoch 5 / 0 B`、横向溢出 0 与 console 0。

独立 `core-minor-sanfang-review-v013.spec.ts` 配合 `playwright.v013.config.ts` 在 Edge、Chrome 各 **1/1、共 2/2 PASS**，最终 **7.0s**，`.last-run` 为 `status: passed`。因沙箱拦截 Windows `taskkill`，正式门在获准的沙箱外执行；产品断言相同，覆盖 12／48／5 sources、有效未填包导入、切宫保持、篡改／跨盘拒绝、静态 48 隔离、完整 IndexedDB／localStorage／sessionStorage／Cache Storage 快照相等、`0 Revision / epoch 0 / 0 B`、390px 横向溢出 0 与 console 0。Edge／Chrome 同状态截图字节一致并经人工复核：桌面 215,243 bytes，SHA-256 `22f01c0195675d8a7c72c3471745ccf4603fc99c01e597d839bf925df778ee97`；移动端 235,486 bytes，SHA-256 `cf2d0e9b7791cfce32abf96febcb5ef73ea8cb3ab78be2117aa924535a8ebe67`。完整合同见 `紫微内容-v0.13-核心十二辅煞三方四正命中复核包与证据-2026-08-14.md`。

本增量仍只在 4218 隔离草案内工作；默认发布身份必须保持 `legacy-v13 / targetSchema 13 / migrationId null`，不绕过 mutation epoch，不取得 v15/v16 晋级、Android、JPL 官方字节、正式入口、提交、专家真值或科学有效性授权。
