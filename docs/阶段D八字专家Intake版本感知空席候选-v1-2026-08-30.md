# 阶段 D：八字专家 Intake 版本感知空席候选 v1

日期：2026-08-30  
状态：版本感知零实例候选；不接入正式专家门、默认发布线或运行时

## 结论先行

本 tranche 只为既有八字专家 intake 空席账建立一个版本感知、单向、失败关闭的工程候选。它在不覆盖历史 packet／intake gap 的前提下，机械核对当前版本感知父链与 packet artifact-lock 观察；没有创建、导入、收集、验证或虚构任何现实专家身份、资质、意见、签名、独立性材料或私有档案。

默认治理继续固定为 `legacy-v13 / targetSchema 13 / migrationId null`。候选的 `activeAdmissionEffect=none`；现实专家仍为 `0/2`，两席均为空席，专家 gate、内容真值、专家真值、权利法律结论、浏览器／运行时证据、发布就绪、专家宣称授权与公开部署授权全部保持红态。

## 机器身份

冻结身份如下：

- candidate ID：`hakimi.bazi.expert-review-intake-gap.version-aware-candidate/1.1.0`；
- status：`candidate_only_version_aware_vacant_intake_not_collection_ready_artifact_drift`；
- candidate semantic digest：`5318b9543bad20c52e5129901e94aa2ca3d2dd68f342ba11ab9611f4f767d582`；
- persisted artifact：`content/system-admission/bazi-expert-review-intake-gap.v1.1.0.json`；
- raw identity：`32579 bytes / 7a7088a3fdc660ccca3ef0d9765e36421563c7feee1c3dfa6bc4d7169d0f91df`。

这些摘要只建立确定字节与确定机械投影之间的一致性，不是数字签名、first-seen、custody、现实身份真实性、专家意见真实性或 owner 重签授权。

## 版本感知重绑与空席账

候选只允许以下窄机械结论：

- 三个历史 direct parent slot 已按当前版本感知身份重绑：两个 source／rights artifact-lock slot，加上旧 readiness `1.5.0` 到当前候选 readiness `1.7.0` 的一个 ledger-binding slot；
- 两个上游 capability 必须同时通过各自模块私有 `WeakSet` 品牌，克隆对象或调用方投影不能替代；
- 一个 DTT versioned-parent supersession supporting receipt 仅作为版本化 source／rights 父账关系依据，不产生正式 admission effect；
- 历史 packet 的 12 个 artifact lock 仍精确分为 `11 current + 1 drift`，不得把单一漂移掩盖为全绿；
- reviewer seat 固定 `required=2 / vacant=2 / realReviewerInstances=0 / verified=0`；
- feedback／opinion collection 与 release-closure review 均不可开始，候选不得反向写入或晋级旧 formal 链。

`11 current + 1 drift` 是本候选对 packet artifact lock 的当前端点字节观察，不是旧 formal verifier 的运行结果，也不是对 12 条旺衰来源 binding 的冻结数量；旺衰 binding 仍为 `0/12`。同样，两个空席只是结构合同，不是两位现实专家。

## 历史 formal 链与中央红门

旧 formal packet 定向测试保留其真实 point-in-time 结果：

```text
node --test scripts/verify-bazi-expert-review-packet.test.mjs
历史当前结果：44/47；3 个失败均先遇到 INTAKE_GAP_BINDING_DRIFT
```

这 3 个失败是独立实际测试分账，不能并入本候选的绿色测试数，也不能由本候选旁路。本候选没有调用或证明旧 formal verifier 的 runtime，也没有建立 formal first-failure 结论；旧 packet、旧 intake gap 与旧 verifier 均未修改或重签。

保存的八字 `single-chart-report@1.7.0` manifest 当前仍因组件漂移而失败关闭；其当前只读 expected-manifest preview 已变化。旧 D0 决策账也继续因 `CURRENT_EXPECTED_MANIFEST_CHANGED` 保持红态，owner decisions 仍不存在。本候选不覆盖保存 manifest、不刷新 D0、不重建四体系 registry，也不接入跨体系 receipt。

## CLI 窄输出与隐私边界

CLI 从自身模块位置推导固定项目根，不接受 caller `cwd` 作为 authority；拒绝额外位置参数、可见的 `NODE_OPTIONS` 与 loader／require 注入型 `execArgv`。成功输出只包含候选身份、固定默认治理、`3 rebind / 2 brands / 1 supporting receipt`、`12 locks = 11 current + 1 drift`、`2 vacant / 0 real` 以及 collection／authority／release 红门。

CLI 不输出完整 candidate、完整父账、上游 ledger、来源正文、意见正文、PII、仓外 opaque context 或绝对路径。失败只输出固定失败前缀与受限错误码，不回显 stack、cause、路径或敏感输入。

## 证据分账

| 账本 | 本 tranche 的精确结论 |
|---|---|
| 工程证据 | 版本感知父账 identity、私有品牌、固定 raw identity、候选空席投影与失败关闭边界可被机械复核 |
| 浏览器／运行时证据 | 未建立；未运行产品浏览器、PWA、Service Worker、正式仓储、公开主机或部署 |
| 内容真值 | `not_established`；本候选不判断八字规则或断语是否正确 |
| 专家真值 | `not_established`；现实专家 `0/2`、意见 `0/2`、独立性未核验 |
| 来源／权利法律判断 | `not_established`；不证明作品层或载体层许可、可再分发性或法律结论 |
| 发布就绪 | `false` |
| 专家宣称授权 | `expertClaimsAuthorized=false` |
| 公开部署授权 | `publicDeploymentAuthorized=false` |

端点观察不建立跨文件原子快照、Schema 13 mutation epoch、区间 mutation 排除或 ABA 排除。本候选的机械通过不得外推为历史 formal verifier 已恢复、完整应用已验证、专家审定已完成或公开发布已授权。

## 定向验证

当前已完成：

```text
node --check scripts/verify-bazi-expert-review-intake-gap-version-aware-candidate.mjs
结果：exit 0

node scripts/verify-bazi-expert-review-intake-gap-version-aware-candidate.mjs
结果：exit 0；输出固定 BAZI_EXPERT_REVIEW_INTAKE_GAP_VERSION_AWARE_CANDIDATE_MECHANICS_OK 前缀与窄红账

附加位置参数
结果：exit 1；CLI_ARGUMENTS_FORBIDDEN

NODE_OPTIONS=--no-warnings
结果：exit 1；VISIBLE_NODE_LOADER_OPTIONS_FORBIDDEN
```

同层定向测试 `27/27` 通过、skip 0；覆盖冻结 raw／semantic identity、三槽重绑、双私有品牌、单 supporting receipt、12 个 lock 的 `11+1` 分账、两席空位、克隆／自重签／真值与授权晋级拒绝、来源／权利／readiness／receipt／历史模板漂移、严格 JSON／UTF-8、链接与固定路径、窄 CLI、LF 和旧／默认／运行时隔离。连同阶段 C 的六文件候选链，显式七文件组合结果为 `207/207`。历史 `44/47` 继续单列为 expected-red，不计入上述绿色通过数。未执行全仓 typecheck、默认 Web build、浏览器、PWA／Service Worker、真实主机、部署或回滚验证。

独立 post-import pollution 反例还揭示一项继承型 P2 硬化缺口：完整 loader 经 supersession 父链最终会进入 `scripts/bazi-dtt-month-command-public-evidence-lib.mjs`，该上游仍使用 live `JSON.parse` 与部分 live 语言内建。污染发生时当前链会以 `JSON_BYTES_INVALID` 安全失败并且拿不到候选私有品牌，因此没有接受绕过；但本切片没有跨范围修改该历史上游。该项应登记为上游继承限制，不能误写成当前 intake 候选自身的新 P1/P2，也不能把 fail-closed 解释为 post-import intrinsic hardening 已完成。

## 后续准入条件

现实 intake 仍需另行取得用户授权，并在受保护的仓外流程中分别建立现实身份、资质范围、核验者 authority、两席彼此独立性、原始意见真实性、first-seen 与 custody。分歧必须并列保存，不得多数表决、平均或由生成模型自动选赢家。完成这些外部条件之前，本候选只能保持零实例、无准入效果和全部红门。
