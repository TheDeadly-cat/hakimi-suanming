# 阶段 F：紫微专家提升失败关闭与身份漂移追加记账（2026-09-01）

## 结论

本阶段转入紫微独立体系，只收口一个此前已识别的 fail-open 缺口：两个本地自述 `reviewerId` 不再能够把紫微规则或 provenance 提升为现实专家态。

本阶段没有取得现实专家身份、资质、scope fit、彼此独立性或原始意见，也没有取得任何外部机器验真的专家收据。因此：

- `expert_reviewed_rule` 继续保留在 draft 枚举中，但在外部机器验真的专家身份、资质、独立性与意见收据接入前，schema 一律拒绝；
- `expert_double_reviewed` 同样作为保留提升态一律拒绝；
- 本地 `double_reviewed` 仍可作为结构性 review metadata，但不能证明现实专家身份、资质、独立性、意见或专家真值；
- `bindingFrozenVerified = 0/27`；
- `independentExpertReviewsVerified = 0/2`；
- `externalVerifiedExpertReceiptsVerified = 0/2`；
- `expertClaimsAuthorized = false`。

这次工程源码变化按设计使旧紫微 manifest 和旧 same-artifact browser child 不再代表当前源码。历史 JSON 原始字节没有被修改；变化的是它们相对于当前源码的 currentness，而不是历史 artifact 被篡改。

## 契约修正

修改范围：

- `packages/ziwei-doushu-contracts-draft/src/index.ts`
- `packages/ziwei-doushu-contracts-draft/src/index.test.ts`

失败关闭边界：

1. `ziweiFixtureEvidenceDraftSchema` 在 child schema 层直接拒绝 `expert_reviewed_rule`；独立消费该 schema 也不能绕过。
2. `ziweiFactProvenanceDraftSchema` 在 child schema 层直接拒绝 `expert_double_reviewed`；独立消费同样失败关闭。
3. 完整 fixture 的既有纵深约束仍保留：专家 claim scope 只能是 `rule_profile`，reviewer ID 必须至少两个且互不相同。
4. 上述 reviewer ID 约束只校验结构，不再被解释为现实身份或独立性证明。
5. 完整 `verifyZiweiNatalFixtureDraft` 对两个自述专家态都返回 `success=false / reason=schema_invalid`；重新计算 fixture 摘要不能绕过 schema gate。

两个保留值仍存在于 TypeScript 推导联合类型中，因此静态对象字面量仍可能编译；安全边界是所有实际入口必须持续 `parse`／`verify`。当前已知 Node adapter、Browser、Workspace 与 Fortel 验收路径都执行完整 verifier，后述 shallow projection helper 则被单独列为接入前 P3 硬化项。

当前契约源码身份：

- bytes：`46181`
- SHA-256：`2a9555dba509873e6c436dcdc9121fcd90e943d880984581fbff865feca15685`

当前契约测试源码身份：

- bytes：`30647`
- SHA-256：`5cd0a7eef6c6522c5b6cf641d0340fe4e2618d02706e8e73cddcee85251ab74e`

## 单一源码、五个 component digest 的身份漂移

旧紫微 manifest v2 把同一个 `index.ts` 同时绑定到五个 component。当前变化因此形成一次根源码身份变化向五个 component digest 的 fan-out；不得描述成五次彼此独立的领域语义变更。

| component | persisted digest | current candidate digest |
| --- | --- | --- |
| `execution_rules` | `501f2b2a97a3986f78e9a9d73581410381c560ff5599be9629ea70e332ce6c17` | `2f1800d9878ae45cbc7591058b11345c0e31f66c97a987998e767b67cd7fc05d` |
| `interpretation_rules` | `7579a19bde8b936de2682e0d38e87b0d1e4d9a9ce1742f82f9efb9b0b29bddff` | `35aa0b900592397e753d8e693a3dbd522b610c07630c3285c7804da680a44c12` |
| `input_policy` | `dff5a8c23adfdf3a87940f6e382aade2a1afe922cff13fd70750eb84b634c958` | `10e7c444f9c04a24c4afedfffb73cabe8b049ed3512779c7310512ca824cbf83` |
| `fact_contract` | `394eb11e7cc2b8b21fad894ea09408b087a1551d7043d4f5d51d0b92f798f32e` | `0ad26a351c9f4774314e8a8d735fa8646e5a79ea4a81d656e61a4e3d490de386` |
| `high_risk_policy` | `c2982c9987fd896deb5d2d39f1d24d192d6a877eb911ad4fd8550b0dace5d24d` | `fd694af774917da59a4a2575bc2f31d660c17f18bf3f6b64ee185d9f656a8bd9` |

`source_bundle`、`rights_bundle`、`expert_review_bundle`、`report_contract` 四个 component digest 在这次窄投影中保持不变。

旧 manifest：

- path：`content/domain-release/ziwei-doushu.engineering-draft.v0.1.0.manifest.v2.json`
- raw bytes：`17968`
- raw SHA-256：`f886e95815109d670b65628db403a2fb86e69e5af4088466de1330a15695a867`
- persisted manifest digest：`f16f4e023248d4362fe42b28e34fd054a3451980d437ca9c83b6bdfb053fb34e`
- current candidate digest：`24ddffd0caebbce4f800154ad70fb24e441e67b3580785b798044b0393d70b66`

current candidate digest 没有被持久化为新 manifest，也没有 owner rebind／re-sign 授权。旧 manifest 继续原样保存，其当前 loader 以 `MANIFEST_MISMATCH` 失败关闭。

## 紫微追加漂移收据

新增 candidate-only、零准入效力的 append-only 收据：

- path：`content/system-admission/ziwei-expert-promotion-boundary-identity-drift-receipt-candidate.v1.0.0.json`
- candidateId：`hakimi.ziwei.expert-promotion-boundary.identity-drift-receipt-candidate/1.0.0`
- raw bytes：`7905`
- raw SHA-256：`c6728e943d713cf63be2b1d7e01d5170cf2a2b7b308e332abbbc53ff1ab37348`
- receipt digest：`09d86df6f23d4c1cbc0a76df359dd0738f793de5bdf84e4784b0620a3a6f05bb`
- `activeAdmissionEffect = none`

实现：

- `scripts/ziwei-expert-promotion-boundary-identity-drift-receipt-candidate-lib.mjs`
- `scripts/verify-ziwei-expert-promotion-boundary-identity-drift-receipt-candidate.mjs`
- `scripts/verify-ziwei-expert-promotion-boundary-identity-drift-receipt-candidate.test.mjs`

该收据精确绑定：

- 旧 manifest 的固定 raw／semantic identity；
- 当前契约源码和 focused test 源码身份；
- 单一源码到五个 component digest 的 fan-out；
- 旧紫微 browser child 的固定 raw identity、childId、observationDigest 和旧 source entry；
- `MANIFEST_MISMATCH` 与 `SOURCE_GRAPH_MISMATCH` 两条相互独立的陈旧关系；
- `uniqueBlockerClaimed = false`；
- 未 rebind、未 re-sign、未覆写 browser child、未做当前浏览器重跑；
- 全部专家、内容、权利、准入、发布与公开授权账继续为 false。

旧 browser child 继续原样保存：

- path：`content/system-admission/ziwei-same-artifact-browser-observation-child.v1.0.0.json`
- raw bytes：`37337`
- raw SHA-256：`7cd6365d6f6ad18f0460418e5b745da0f0e68761cea361886729e55563e4db16`
- observationDigest：`792bba4b3c5307fe3bc08aed849eaeb092787eb9e171e09a9e82b3be4dd71a1b`

其当前 verifier 以 `SOURCE_GRAPH_MISMATCH` 失败关闭。本阶段没有浏览器重跑，因此不得把历史 browser child 提升成当前浏览器或产品运行时证据。

## 四体系 current-status v2.2 successor

没有覆盖 v2.1，也没有把 v2.2 写成中央 registry。新增：

- path：`content/system-admission/four-system-current-status-observation-child.v2.2.0.json`
- childId：`hakimi.system-admission/four-system-current-status-observation-child/2.2.0`
- raw bytes：`18625`
- raw SHA-256：`1a81a03b682613c9221407cb2dfa9598728e63f683632d877abdb0524baade54`
- childDigest：`b0d0340eae1190560831be2901afe9924e6c1a6434f5a7596d8be6aacb28d941`
- `activeAdmissionEffect = none`
- `persistedAsCentralRegistry = false`

实现：

- `scripts/four-system-current-status-observation-child-v2-2-lib.mjs`
- `scripts/verify-four-system-current-status-observation-child-v2-2.mjs`
- `scripts/verify-four-system-current-status-observation-child-v2-2.test.mjs`

v2.2 把固定 v2.1 作为历史 predecessor，不调用其已经失败关闭的 current loader；只校验 v2.1 的固定 raw identity、canonical materialization 和 `childDigest`。v2.1 原始字节没有变化，但由于其紫微 endpoint 已陈旧，current loader 继续非零退出。本 child 明确 `uniqueBlockerClaimed=false`，不把任何 Promise race 的首错冒充唯一 blocker。

当前逐体系分账：

| 体系 | 当前机械 endpoint | 当前完整 domain manifest | 当前 engineering manifest | 当前浏览器证据 | binding | 现实独立专家 | 正式准入 |
| --- | --- | ---: | ---: | --- | ---: | ---: | ---: |
| 八字 | report component drift receipt candidate | 否 | 否 | 本 child 未评估 | 0/12 | 0/2 | 否 |
| 紫微 | expert-promotion identity drift receipt candidate | 否 | 否 | 历史 child source graph 陈旧；当前证据不存在 | 0/27 | 0/2 | 否 |
| 西洋 | version-aware observation child v1.1 | 否 | 否 | 本 child 未评估 | 0/28 | 0/2 | 否 |
| 吠陀 | v1.2 observation + engineering manifest v1 + isolated fact browser child | 否 | 是，仅工程 manifest | 旧有隔离 civil-time fact-only Edge/Chrome 10/10；非 Web/PWA/产品 runtime | 0/38 | 0/2 | 否 |

四体系汇总继续为：current endpoint 4/4，当前完整 domain manifest 不是 4/4，正式准入 0/4，准入门 0/32。紫微 receipt 的机械通过只证明这份红灯收据当前，不证明紫微 manifest、浏览器、内容或专家结论当前通过。

## 机械验证

本阶段定向结果：

- 紫微契约、Node adapter、browser artifact、cross-system engineering projection、Fortel differential、workspace 与 browser persistence：7 files / 67 tests，全通过；
- 紫微追加漂移收据：15/15；
- 四体系 current-status v2.2：20/20；
- 合计：9 test files / 102 tests，全通过；
- scoped strict TypeScript：6 个配置中 5 个通过；
- 通过：contracts、adapter browser preview、Fortel、workspace、workspace browser app；
- 未通过：`packages/ziwei-iztro-adapter-draft/tsconfig.json`，当前错误位于 `src/high-risk-expression-egress-view.test.ts:597`，类型联合允许 `selectedTradition`，而局部 `rawByField` 只包含 natal 的 `selectedSchool` 等键。该问题不在本次专家提升 gate 的生产路径中，本阶段未顺手修改或隐藏它。

当前陈旧链路复核：

- 旧紫微 manifest loader：`MANIFEST_MISMATCH`；
- 旧紫微 browser child CLI：`SOURCE_GRAPH_MISMATCH`；
- 历史 current-status v2.1 CLI：非零退出，`UNEXPECTED_ERROR`，其真实上游根因是未包装的紫微 manifest mismatch；
- 历史 registry v2 CLI：继续失败关闭，没有被 v2.2 静默重绑。

测试与 CLI 还覆盖：固定 raw／semantic identity、唯一 canonical LF materialization、builder 不授予私有品牌、loader 固定路径后才授予私有品牌、clone 不继承品牌、authority／gate／manifest／browser／comparison／epoch／lineage／central-registry／re-sign 的重摘要提权均拒绝、accessor／proxy／alias／cycle／own `__proto__`、post-import brand primitives 与关键 primordial 污染。

独立机械审计曾复现两类 P2：一是旧共享 canonical serializer 和稀疏期望键数组会受 post-import 数字索引 setter 污染，二是捕获 `Promise.all` 却使用可变全局 `Promise` receiver 与数组解构，可能跳过真实 current-input await。修复没有修改旧共享 serializer，也没有扩大历史 artifact：两份新库改用本地 passive canonical capture，数组和 key-list 逐索引用 captured `Object.defineProperty` 建立 own data property；receipt fan-out 数组改为 `appendOwn`；两库 current inputs 改为逐项 direct `await`，v2.2 endpoint 逐项读取，cross-system false fields 改为索引循环；全局 `String` 不再参与 property key 生成。setter／global `String`／替换全局 `Promise` 回归通过，冻结 JSON raw／semantic identity 不变。修后独立复核未再发现 P1/P2。

这些是工程机械证据，不是专家真值、内容权威、权利法律判断、完整应用或浏览器验收、发布就绪或公开发布授权。

独立消费者审计没有发现当前已知生产／验收入口可绕过两个保留专家态（P1/P2 均无）。另有一项未提升为当前生产漏洞的 P3：`cross-system-engineering-fact-projection.ts` 的 exported source helper 接受 `unknown` 后只做浅层工程身份检查，没有调用 full fixture verifier。当前只发现测试调用，相关 draft packages 仍为 `exports: {}` 且 `productionImport: forbidden`；将来接入前必须改为消费 verifier 私有品牌或内部执行完整 verifier，并补两个专家态负例。本阶段没有修改该 helper，以免把第二个工程源码变化混入已冻结的单一根源码漂移收据。

## 固定治理与未完成事项

项目默认治理继续固定：

- `legacy-v13`
- `targetSchema = 13`
- `migrationId = null`
- mutation epoch boundary required
- `mutationEpochAvailableForSchema13 = false`
- `mutationEpochReceipt = null`
- `crossFileAtomicSnapshot = false`
- `intervalMutationExcludedAcrossFiles = false`
- `abaExcluded = false`

紫微自身仍为独立产品边界：`releaseIdentity = null`、`targetSchema = null`、`migrationId = null`，不继承八字权威。

仍未完成：

1. 外部机器可验证的现实专家 identity／qualification／scope／independence／opinion receipt schema 与至少两份真实实例；
2. 27 条 binding 的可靠底本、版本、来源正文、exact quote 与作品／版本／载体三层权利依据；
3. 当前源码的 owner-authorized manifest successor；本阶段没有 rebind 或 re-sign；
4. 当前源码对应的同一构建产物 Chrome／Edge 重跑与新的 append-only browser child；
5. 完整应用、正式仓储边界、PWA／Service Worker、公开主机、Release Evidence、部署与回滚确认。
6. 任何接入 `cross-system-engineering-fact-projection.ts` 之前，先把其 shallow helper 收紧到完整 verifier／私有品牌边界。

专家分歧仍不得多数表决、平均或由生成模型自动选赢家；跨体系 scoring、weighting、auto merge、概念等价推断与权威继承继续禁止。

## 本阶段未做

- 未读取或修改 `apps/web/src/lib/local-user-data-cleanup.ts`；
- 未运行全仓 typecheck 或默认 Web build；
- 未覆盖、重签、删除、清理、暂存或提交任何历史 artifact；
- 未执行 Git 操作；
- 未访问第三方后台，未复制第三方 prompt、知识库、断语或资产；
- 未联系现实专家，未取得同意、私人 dossier 或原始意见；
- 未作权利或法律结论；
- 未执行当前紫微浏览器重跑、完整应用、PWA／Service Worker、公开主机、部署或回滚验证。

截至本阶段：`releaseReady=false`、`publicDeploymentAuthorized=false`、`publicReleaseAuthorized=false`、`expertClaimsAuthorized=false`。
