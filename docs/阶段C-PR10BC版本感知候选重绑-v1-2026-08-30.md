# 阶段 C：PR10BC 版本感知候选重绑 v1（2026-08-30）

## 结论

本切片新增一条独立的 PR10BC v1.1 候选 capability，把已经冻结的 DTT 成对 supersession、C-L3 v2 与 candidate readiness 1.7 接入九项 PR10B／PR10C scope projection。它只证明固定 raw／semantic tuple 下的候选父身份和相邻候选 context 已按允许的唯一投影重绑；不建立 active admission、正式 parent、Binding freeze、规则表真值、内容真值、专家真值、权利法律结论、浏览器证据、发布就绪或公开发布授权。

历史 PR10BC v1 没有修改、放宽或重签。旧 loader、旧 engineering value-subject gap、policy weights、C-M1、默认脚本和中央 lifecycle 都没有导入新 capability。

## 历史账与独立候选身份

历史 PR10BC v1 继续固定为：

- path：`content/system-admission/bazi-pr10bc-scope-reconciliation.v1.json`
- raw：`31668` bytes
- raw SHA-256：`0aa6dda66b5d3b47a69d1decb73220ae5811af7d779516858aeb32ef18a1abd6`
- ledger ID：`hakimi.bazi.pr10bc.scope-reconciliation/1.0.0`
- ledger digest：`5e742a8a92a8ca4ebf2337f5eb0866be6c962e53801e3d259de4571b0cd65116`

它目前仍因同路径 readiness basis 已变化而不能由旧 current builder 重现；当前旧 CLI 为 `LEDGER_MISMATCH`，旧测试为 `10/11`。v1.1 因此没有原位“修好”旧账，而是把该历史账显式标为 `fixed_projection_template_only_not_current_capability`。

新增工件：`content/system-admission/bazi-pr10bc-scope-reconciliation.v1.1.0.json`

- `schemaVersion=1.1.0`
- `recordType=bazi_pr10bc_version_aware_candidate_scope_reconciliation_v1_1`
- ledger ID：`hakimi.bazi.pr10bc.version-aware-candidate-scope-reconciliation/1.1.0`
- status：`candidate_scope_reconciliation_versioned_parent_rebound_no_binding_created`
- raw：`38161` bytes
- raw SHA-256：`edb367448f857972e50c735e86acc5252605a0b4d27a2f85c996fcd67d0ed2cc`
- ledger digest：`24851de28a762c426e2630c9a52666786274c3c5477b9fc0463f9129e43d20d1`

## 固定父账与六项 basis

专用 loader 从同一个 primitive `workspaceRoot` 内部加载 supersession 与 candidate readiness，并要求二者各自的私有 WeakSet 品牌。它同时逐字段锁定 source／rights ledger、DTT source／rights candidate、C-L3、receipt 与 readiness 的 raw／semantic tuple，不接受 caller JSON、caller path、caller dependency result 或可注入 options。

六项当前 basis 精确为：

1. candidate readiness 1.7：`32629` bytes；raw SHA-256 `7565a1c1ae51c09c78cf8f906e544bd8ee822020f76587237bc909ef1e6408a2`；digest `afdebfa1fb7a797f02d136373eb9aef1afa26b38b755926f8930ea5de97316b8`。
2. engineering binding candidates v1：`21712` bytes；raw SHA-256 `95154d55d9e368351f5a69a1fa2ff63a51ba946d33a800dbd1f2460d9ac97b2f`。
3. source parent 1.6：`50427` bytes；raw SHA-256 `62f6a68b99246a802bcf2055aa09a8ac8113df37a7ede1c55d8a2ea64deaea98`；ledger digest `6f008058b282613fd131344206b27fccb1d80596bb49789f0554acdd69c1e44c`。
4. rights parent 1.2：`23947` bytes；raw SHA-256 `678632a8875672f58bb5c9f1ec4a299a4f0c6e750b8ff4a02bfe4df889e1243a`；ledger digest `300e092c4913885cff626221b8734786128639c7d6cfd7c2be7bea2acc6bbefc`。
5. C-L3 v2：`8881` bytes；raw SHA-256 `e2656a19955db0ce908df88aa696cd40847e7536af58c79548ac1fbce9a7464b`；digest `2a47fc6fe281a4362c36dfc67c2a6dee195b5b069065ffc0e86ac1afcd7aa8a0`。
6. paired supersession receipt：`9229` bytes；raw SHA-256 `aa9e62089990d8ba699c441114914173141e8228f027605b47f5c5e238fac0e3`；digest `601b4e07b94e62b73812a7a26f1108a094272dca660a188bb660fa860f031264`。

历史 PR10BC raw／semantic identity 作为独立 lineage template 固定；它不混入六项“当前候选 basis”，也不被当作当前 capability。

## 唯一允许的 scope 投影变化

九个 concept 的顺序、topic、mapping、工程观察、non-equivalence、quote candidate ID、relation boundary、12 条 binding inventory 及 `7/4/1` 分账保持不变。旧字段 `adjacentHistoricalCandidates` 在新候选账中改为 `adjacentCandidateContexts`，避免把 superseding DTT v2 误写成历史 candidate。

精确三处 DTT 相邻引用发生：

- `dtt-chanwei-wikisource-r2600158-candidate-v1`
- → `dtt-chanwei-wikisource-r2600158-candidate-v2`

三处分别位于 `support.season_production`、`support.season_control` 与 `support.rooting_tables`。每处保存 `supersedesCandidateId=v1`，并标为 `superseding_candidate_input_only_not_active_formal_parent`。这里是三次 reference rebind，不是三个父身份；唯一 superseding source candidate parent 数为 `1`。其余六个非 DTT 相邻 context 逐字段保持历史语义，只增加 candidate-context 元数据。

这次重绑没有创建平行 binding，没有把相邻文献候选提升为规则表或正式来源依据，也没有改变 mapping authority。

## 完整性与权威边界

新 loader 使用独立的第三个 WeakSet 品牌；clone、自重封 plain object、test-only builder 结果和历史账都不能取得该品牌。它复用 supersession 层的固定路径、held-handle、有界读取、端点／目录链检查与 duplicate-key rejecting strict JSON parser，并重新核对历史 PR10BC 及六项 scoped production artifact 的 raw identity。

证据措辞明确分账：历史 scoped inspection projection 只在当前 raw identity 精确相等后复用；上游品牌只证明各自验证器的窄 capability。本层不声称嵌套 verifier 的 same-buffer 性质可传递。因此继续固定：

- `endpointSnapshotOnly=true`
- `nestedVerifierSameBufferTransitivityClaimed=false`
- `crossFileAtomicSnapshot=false`
- `mutationEpochAvailableForSchema13=false`
- `mutationEpochReceipt=null`
- `intervalMutationExcluded=false`
- `abaExcluded=false`

候选／active／formal 三账继续分开：candidate notice projection reconciliation 为 `1`，candidate discrepancy block 为 `0`；active notice discrepancy block 仍为 `1`；formal admission promotion blocked 为 `true`，`activeAdmissionEffect=none`。Binding 仍为 `0/12`，distribution 为 `link_only`，formal rights／carrier records 为 `0/0`。

## 机械验证

入口固定身份：

- `scripts/bazi-pr10bc-version-aware-candidate-lib.mjs`：`25805` bytes；SHA-256 `545a52fc70d5710a7de88a4d60d37a9780e43a18165fea986b2c83aa7d478b24`
- `scripts/verify-bazi-pr10bc-version-aware-candidate.mjs`：`2511` bytes；SHA-256 `cfef97f4c1ad28871ef0274b3ab38c1a0eebf3ca992581a2f2f9946f9969d834`
- `scripts/verify-bazi-pr10bc-version-aware-candidate.test.mjs`：`26952` bytes；SHA-256 `5fa4642f1b7f98886ea8eaa4d060c97c6e6899917d37fed5a59d2b727ca92b17`

结果：

- 新 PR10BC v1.1 定向 tests：`25/25`
- supersession、version-aware readiness 与新 PR10BC 合并：`93/93`
- CLI exit `0`，前缀为 `BAZI_PR10BC_VERSION_AWARE_CANDIDATE_MECHANICS_OK`

攻击覆盖包括：raw-only whitespace drift、BOM、非法 UTF-8、literal／escaped duplicate key、hardlink、old/new source-rights 混配、readiness 与 scoped artifact 漂移、历史 PR10BC raw 漂移、部分 DTT 引用回填、clone／自重封／test-only brand 冒充、authority／Binding／epoch／atomicity／ABA 正向伪造、post-import WeakSet／Object.freeze／fs clean-clone／Hash／FileHandle poisoning、合法 decoy 与额外 caller path／dependency 注入、全部直接 raw-bound 文本路径的 LF checkout 闭包，以及旧 verifier／专家／私有材料／cross-system／默认 consumer 隔离。

## 仍为红的旧下游与下一步

当前四个旧下游没有重签，合并测试保持 `52/67`：

- 旧 PR10BC：CLI `LEDGER_MISMATCH`；tests `10/11`
- engineering value-subject gap：上游 `LEDGER_MISMATCH`；tests `9/11`
- policy weights：`PARENT_LEDGER_DRIFT`；tests `14/22`
- C-M1：`BASIS_ARTIFACT_IDENTITY_MISMATCH`；tests `19/23`

下一 C 切片应新增、而不是原位改写 engineering value-subject gap 的 version-aware candidate，并内部要求本 PR10BC v1.1 私有品牌；之后再按 policy weights → C-M1 顺序推进。任何一层都不能凭 shape、digest 或历史测试把旧 active/default、中央闭包或发布治理洗绿。

本切片没有执行 Git 操作、全仓 typecheck、默认 Web build、完整浏览器／PWA、公开主机、Release Evidence、部署或回滚验证；受限 `apps/web/src/lib/local-user-data-cleanup.ts` 未读取、未修改。默认治理继续固定 `legacy-v13 / targetSchema 13 / migrationId null`，`publicDeploymentAuthorized=false`，`expertClaimsAuthorized=false`。
