# 其他体系独立工程增量：紫微、西洋、吠陀（2026-08-31）

## 总结

本轮响应优先级变更，只推进其他体系的独立、私有、失败关闭工程增量；没有重复八字 C 或 D，也没有把八字 v1.7 的工程证据、规则、专家结论或权利状态外推到紫微、西洋或吠陀。

项目默认发布治理继续为 `legacy-v13 / targetSchema 13 / migrationId null`。三个增量都没有进入 `apps/web` 正式入口，没有更新 `content/system-admission/four-system-admission.v1.json` 或 domain release manifest，也没有签发正式准入、发布就绪、公开发布或专家 claims authority。

## 紫微：版本化高风险表达 egress 候选

入口：`packages/ziwei-iztro-adapter-draft/src/browser-preview/high-risk-expression-egress-policy.ts`

- policy ID：`hakimi.ziwei.high-risk-expression-egress-policy/0.1.0`；
- canonical SHA-256：`f2be6ba54f97522ceadc776a4db1f599c33d48dcfe7ba77a2a740aec1123b715`；
- 固定 8 类高风险表达、16 个逻辑 egress surface，并绑定当前 source bytes/SHA-256；
- 未登记 surface 失败关闭；
- `result / eventOutcome / goodBadOrientation` 必须为 `null`；scoring、直接结果、确定性个人结果均固定为 false；
- 风险命中只能返回中性降级；receipt 不包含出生数据、候选原文、来源正文或个人派生摘要；
- request、receipt、decision 使用私有品牌和深冻结，拒绝 clone、Proxy、getter 与伪回执；
- 明示 `semanticCoverageComplete=false`、`candidateCallSitesWiredToGate=false`，完整 callgraph/sink 闭包和 pre-import intrinsic 完整性均未建立。

这只是隔离的版本化词法防御候选。有限词表不能覆盖同义改写，surface 仍由调用者声明；在上述 policy source-binding child 建立时，它尚未接入已有候选调用链。本轮 Z-EGRESS-01 的部分接线增量见下节，但完整 `candidateCallSitesWiredToGate`、callgraph 与 semantic coverage 仍保持 false，不能称生产统一 egress 或语义安全门。

### Z-EGRESS-01：14/16 非反馈显示面机械接门

本批只接紫微 4218 隔离 Browser Workspace 当前可达的非反馈正文显示面：13 个来源 surface 先分别执行 policy，再由 `ziwei.projection.browser-display` 执行第二次显示投影检查，合计 14 个机械接门 surface。两个文件反馈／预检 surface 继续明确排除：

- `ziwei.candidate.core-minor-star.sanfang-feedback-preflight`；
- `ziwei.candidate.natal-transformation.palace-feedback-preflight`。

`createZiweiHighRiskEgressDecisionMap` 为当前 projection 预计算同 realm 私有品牌、深冻结的内存 decision map。key 固定绑定 source surface、文本角色、source identity 与字段；另以私有 `WeakMap` 同时绑定创建该 map 的 projection 对象身份和每个 entry 的 exact raw leaf。查询必须同时匹配 map 品牌、当前 projection 对象、key 与当前原文，缺项、伪 map、错 projection 或错 raw leaf 都抛错，不回落显示原文。独立复核曾复现“不同 projection、相同 key/text 可复用”的声明缺口，现已加入 projection 对象身份负测并失败关闭。

计算与重开两条路径都在创建 projection 后异步生成 map；Worker 返回后与 map 完成后分别重查 `viewEpoch`，旧异步结果不能进入当前显示。这里的 `viewEpoch` 只保护视图替换，不是 IndexedDB storage mutation epoch；本批没有修改或证明保存、恢复、清空的 mutation epoch。

`reviewPrompt`、`reviewQuestions`、`readingOrderStatement`、`scopeNote`、`absenceBoundary`、`emptyMainStarBoundary`、`counterweight`、`balancePrompt` 被分类为 `review_guardrail` 并保留原样，避免词法规则误杀风险警示本身。当前测试 projection 机械采集 506 个 source leaves；这不是自由文本同义改写覆盖、完整 DOM/ARIA/dataset/download sink 闭包、内容真值或语义安全证明。

当前工程身份锚点：

- policy canonical SHA-256 仍为 `f2be6ba54f97522ceadc776a4db1f599c33d48dcfe7ba77a2a740aec1123b715`，source-binding child digest 仍为 `aff40f1d87c6575c9254877545e787b645a444090c18bcc976ae9c61deb8b0ac`；policy 源码仍为 `34053 bytes / 7b10bb23fe0194b0dc564eec895d9dc1bb99658ad5fcc710f8edd14d1fdd8616`；
- view helper 为 `25934 bytes / 557179bc65776f2a652ef76eb1b7432c2fb327d66312fe499a681203f1c26c42`；workspace calculation bridge 为 `1849 bytes / e0a95f077037b0c240f06004707c662248b3a72495c1f94538d3e48c81a2f5dd`；4218 `main.ts` 为 `143024 bytes / 93de9cb133a6d63dd02c6096409c8dbef44d71eb2d351e622d28fb0676b8ec7d`；
- adapter browser artifact 为 `23724 bytes / 14f75f337fe12e126bf2cb9e315e6106e6137973f349fa151b85b51e0ed56e1c`；Vite config 为 `14314 bytes / e2a034fdb78fb8f00f6c034e0fa2bc9b4e452c1fcc12fda71282657775d7a138`，其精确 Browser source graph 为 24 paths（23 TS + 1 JSON）；
- build-notice evidence child 当前原始身份为 `12871 bytes / cb8ab23f89531941fde358a452ecab41d508112eabec925fb56c76d508c20610`，semantic evidence digest 为 `d0618f4162d508dc24cc27609454813108f90ba77630976284036873d7bfe075`。

本批机械验证分账：

- adapter browser-preview 与 workspace browser-app 两个 strict TypeScript 配置通过；定向 Vitest `4 files / 80 tests` 通过，其中包含伪品牌、错 projection、错原文、缺 key、pass-through 与 neutralized 负／正例；
- policy verifier `19/19` 与 CLI 通过，但 CLI 继续报告 `0/8 admission gates`、`candidateCallSitesWiredToGate=false`、`semanticCoverageComplete=false`、`publicReleaseAuthorized=false`；
- isolated build-notice verifier `24/24` 与 CLI 通过，仍明确 `noticeObligationSatisfied=false`、rights legal conclusion、formal admission、release readiness 与 public deployment 均未建立；
- 直接的 4218 隔离 Vite build 通过，`110 modules transformed`。正式 `npm run build:ziwei-browser-workspace` 仍在 prebuild 的全局 boundary gate 退出 1，因此不能记为正式脚本构建通过；
- boundary 新增 source-graph omission 与 direct-bridge 定向负测 `2/2` 通过；完整 suite 为 `74/75`，CLI 仍因受限文件仅记录未检查、既有 Bazi helper 未注册依赖和三个既有动态 import 红项退出 1，但没有新增 Ziwei graph/bridge 错误；
- `check:independent-domain-release-manifests` 继续以 `MANIFEST_MISMATCH` 失败关闭；本批没有重签紫微 domain manifest。

应用内 Browser 在 `http://127.0.0.1:4218/` 加载标题“紫微本地研究档案 · 隔离草案”，只点击一次“生成完整工程工件”，得到“排盘完成并通过工程核对；尚未写入本地档案。”和“核对通过 · 尚未保存”。随后遍历十二宫，DOM 保持非空、日志为 0；十二宫的中性占位 `fallbackCount` 都为 0，说明本次默认盘没有可见风险命中，**不能**写成 neutral fallback 的浏览器验证。neutralized 行为只由定向单测建立。截图为 JPEG `1265 × 712 / 107392 bytes / e6e09c73a962baa80e924545fcf59c8242c913ef814458f78bcdfa5a302347a0`。本轮没有点击保存、下载、导入、清空或 IndexedDB 六项 probe，因此没有新增 storage、Revision、恢复、清空或 mutation epoch 浏览器证据；验收后已关闭标签页并停止 4218 端口。

上述 SHA-256 只绑定当前字节与结构，不是签名，也不证明作者、现实来源、作品／版本／载体许可、内容真值、专家真值或发布授权。紫微来源仍为 `0/27` frozen、现实独立专家仍为 `0/2`；两个反馈/file surface、完整 sink/callgraph/semantic closure、跨浏览器、PWA/Service Worker、公开主机、Release Evidence、部署与回滚均未建立。项目默认治理仍为 `legacy-v13 / targetSchema 13 / migrationId null`；紫微自身 `releaseIdentity / targetSchema / migrationId` 仍为 null，`publicDeploymentAuthorized / publicReleaseAuthorized / expertClaimsAuthorized` 仍为 false。

### Z-EGRESS-02：两条反馈 DOM 叙事投影机械接门

本批只关闭 Z-EGRESS-01 明确留下的两个**已导入、已通过各自结构预检的人类审稿叙事到 DOM** 的调用缺口：

- 当前盘核心十二辅煞三方四正反馈：`selectedTradition`、`decisionReason`、`applicabilityConditions`、`counterexamples` 与非空 `revisionRequest`；
- 四化十二宫反馈：`selectedSchool`、`decisionReason`、`applicabilityConditions`、`counterexamples` 与非空 `revisionRequest`。

每个 leaf 先在固定 feedback-preflight surface 求值，再把第一层 `displayText` 作为唯一输入交给 `ziwei.projection.browser-display`。两类反馈各用独立的同 realm 私有品牌、深冻结 decision map；核心反馈 map 绑定 exact projection、exact preflight 与 exact raw leaf，四化反馈 map 绑定 exact preflight 与 exact raw leaf。品牌、owner、key、字段或原文任一不匹配都失败关闭，不从 JSON 原文回落显示。它们没有并入 Z-EGRESS-01 的 14-surface projection map，也不改变后者的品牌、生命周期或 `feedbackSurfacesIncluded:false` 边界。

两条导入路径都在 `arrayBuffer()` 后及异步 decision view 返回后复核最新 token；当前盘路径还复核 scope、`viewEpoch` 与 projection 对象身份。所有叙事 decision entry 和所有补充来源 URL 都先在 live DOM 外完成求值，再组装 detached `DocumentFragment` 一次提交；任何 URL 或叙事字段失败都先清空旧预览，不留下半渲染卡片。URL 不冒充 expression policy 覆盖，而由独立导航 gate 严格限制为无 trim 差异、无控制／Bidi 字符、无账号密码、具非空 hostname 的绝对 HTTPS canonical URL，链接固定 `target="_blank"` 与 `rel="noreferrer noopener"`。导入文件名和 reviewer 原始显示名不再进入 DOM，只显示固定的“所选 JSON 文件”和“已提供自述身份（未核验）”。

因此，当前 16 个注册 surface ID 在这些**枚举过的指定链路**中都至少有一个受测机械调用点：13 个来源 surface、2 个 feedback-preflight surface、1 个 browser-display surface。该说法不等于 `16/16 sink closure`，更不等于全部字段或全部导出面安全。原始审稿模板与下载文件、reviewer 归属元数据、导入文件元数据、URL 文本本身、固定结构枚举、来源绑定字段、guardrail／counterweight、其他 DOM／ARIA／dataset／download sink、完整 callgraph 和自由文本语义覆盖继续明确排除；policy CLI 仍保持 `candidateCallSitesWiredToGate=false` 与 `semanticCoverageComplete=false`。

当前工程身份锚点：

- feedback/projection view helper：`48019 bytes / 926f932d45b22b0fc6e4b0b6a1c92f4b6ddb579603973bc74492bfff4790050c`；定向测试：`31679 bytes / 41c59332d7792d90ed87230e78bfb9c3a7a2531fb02ae81121fb3d87fd9d7aef`；
- adapter browser client：`6927 bytes / e492acafe1e6b312e8c00dddd0e1f4d9bc5076bb66dd7269b44096927f59e2fd`；生成的 Browser source identity：`489 bytes / bc0c5072f0f0cfc0b009e40f4f4d899560ede0a84438a480d514debff319989e`；
- workspace calculation bridge：`2752 bytes / 8f4662bc26ca34596751eafe10ca474ff3041f87593f8d743058bad9197e52ef`；4218 `main.ts`：`148531 bytes / 5dfd2f4003aea118666f135a17d87844c0eff97e5e283bfc7ccbb29319d6610a`；
- 双浏览器回归 spec：`93050 bytes / 8c0f75b3a7cd8a68de384a061e67694229062fcf6f23e3fdf9b59097782f44f1`；policy canonical 与 source-binding child digest 仍分别为 `f2be6ba54f97522ceadc776a4db1f599c33d48dcfe7ba77a2a740aec1123b715`、`aff40f1d87c6575c9254877545e787b645a444090c18bcc976ae9c61deb8b0ac`，本批没有重签 child。

本批机械验证分账：

- feedback view、policy 与两条 feedback contract 的定向 Vitest：`4 files / 58 tests`；adapter browser-preview 与 workspace browser-app 两个 strict TypeScript 配置均通过；
- Edge 与 Chrome 各运行两条相关 Playwright 场景，共 `4/4`：覆盖通用 reviewer 文案、原 reviewer／文件名不回显、安全 canonical HTTPS 链接、`noopener noreferrer`、高风险原文不显示而中性替代、带账号信息 URL 整批拒绝、旧预览清空和零存储变化；这不是其余浏览器、完整 4218 故障矩阵或跨浏览器发布验收；
- 使用项目锁定的 `apps/web/node_modules/vite` `7.3.6` 直接隔离构建通过，`110 modules transformed`。一次误用根目录 Vite `8.2.0` 的临时构建在真实浏览器以 `STAR_REGISTRY_SIZE_MISMATCH` 失败关闭；该构建已判定为测试工具链不匹配，不计入通过证据；
- 应用内 Browser 在锁定构建的新标签页完成真实排盘，并分别对两条反馈链导入 safe、确定性死亡叙事和带账号信息 URL 三类合成样本。safe 叙事精确显示；`此人必然死亡。` 原文不进入 DOM而显示固定中性替代；credential URL 返回“补充来源 URL 必须是无账号信息的绝对 HTTPS 地址”，旧卡片为 0／隐藏。可见计数全程保持 `本地档案 0 / 资料代次 5 / 本机占用 0 B`，console warning/error 为 0；这些是 DOM 可见状态，不冒充未执行的 IndexedDB／Web Storage／Cache 六项 probe；
- isolated build-notice CLI、policy CLI 与 release-governance CLI 均通过各自机械检查；它们同时继续报告 notice/legal/formal admission 未满足、policy `0/8 admission gates`、默认 `legacy-v13 / targetSchema 13 / migrationId null`、`publicDeploymentAuthorized=false`、`expertClaimsAuthorized=false`；
- 完整 boundary 的本批源图核对仍为 `74/75` 且只含既有红项；独立 domain manifest 仍为 `MANIFEST_MISMATCH`。没有绕过或重签任何红门。

本批没有点击保存、恢复、清空或六项存储 probe，也没有把 raw 模板／下载面纳入 egress。两次显式模板下载只为生成真实浏览器导入样本；下载及合成反馈不含现实专家意见。以上结果只建立两条指定 DOM 投影的工程证据，不建立紫微内容真值、现实专家身份／资质／独立性、作品／版本／载体权利结论、完整发布就绪或公开授权。

## 西洋：民用时输入适配器草案

入口：`packages/western-civil-time-input-adapter-draft/src/index.ts`

- 只消费西洋独立 birth-input 草案与项目现有 `@hakimi/tzdb-core`；
- 严格处理 IANA zone、unique、DST gap、overlap reject/earlier/later；
- 回执绑定精确 tzdb snapshot ID、IANA 版本、data SHA-256、resolver/adapter 版本与 UTC/offset round-trip；
- 禁止 host `Intl`/host timezone fallback、网络、持久化、geocoding 和 mutation；
- 不输出 UT1、TT、TDB、EOP、RAMC、星历、星盘或解释性内容；
- `targetSchema / migrationId / releaseIdentity` 保持 null，全部 authority 为 false。

该回执含出生输入及其派生摘要，所以 `safeToLog / safeToPersist / safeToPublish=false`。它是 Node 测试草案，不是浏览器、PWA、正式 time-scale 或 astronomical evidence。

## 吠陀：临时 mutation-epoch 运行实验

入口：`packages/vedic-mutation-epoch-runtime-experiment-draft/src/runtime.ts`

- 只使用随机 `ephemeral-mutation-experiment` IndexedDB 名称，工程观察不包含原始数据库名；
- runtime snapshot、mutation candidate 与 restore snapshot 使用私有同 realm 品牌；
- operation ID、nonce 与 idempotency key 由 runtime 内部随机生成，调用者不能注入原文；
- 四个 consumed-digest 数组在 canonical/hash 前按精确自有 data descriptor 捕获，拒绝 holes、额外键、symbols、accessors、非法／重复摘要和超限长度；
- 模块求值时捕获 `Reflect.apply` 与 `WeakMap.prototype.get/set`，但当前只证明 post-import `WeakMap#get` 污染不能伪造品牌；模块必须在受信 realm 中导入，pre-import 与其他 primordials 完整性未建立；
- 最终 readwrite transaction 对完整 canonical base 做精确比较，不信任存储对象自报 digest；该比较只保护本次提交点，不认证所有 writer，也不阻止后写；
- A→B→A 作为前向 epoch，restore replay 与 stale generation 失败关闭；
- post-put abort 与 labelled synthetic quota-like post-put abort 后不消费 epoch；后者不是实际 `QuotaExceededError` 或容量测量；
- 明示没有 external monotonic anchor 或独占／认证的 same-origin store ownership；不能排除完整旧自洽 envelope／数据库回灌，也不能排除 live same-origin raw writer；
- 工程观察冻结但不带私有品牌或真实性认证，公开可重算摘要不是签发证明，不能作为产品 receipt；
- 临时库删除为 best-effort，blocked 或崩溃时可能残留。

它仍不是产品 namespace/schema/backend、真实备份恢复或 rollback 实现，也不满足吠陀 parent ledger 的正式 re-review requirement。

## 本轮机械验证账

- 紫微 + 西洋：`8 files / 108 tests` 通过。
- 吠陀：`1 file / 10 tests` 通过。
- 合计：`9 files / 118 tests`，分属两个 Vitest 配置，不冒充全仓回归。
- strict TypeScript：紫微 base、紫微 browser-preview、西洋、吠陀共 4 个独立配置通过。
- 紫微、西洋 isolated build/license verifier 均通过机械检查；二者输出同时明确 legal clearance、rights conclusion、formal admission、release readiness 与 public deployment 均未建立。
- 吠陀隔离 Vite build 通过：6 modules。
- 离线 lockfile dry-run 通过。
- 应用内浏览器已加载吠陀隔离页，标题、初始 DOM 与 error/warn 日志通过核对；真实按钮流尚未登记通过，因此没有建立临时数据库创建、六项 probe 结果或 cleanup 的浏览器证据。
- 全局 `check:system-contract-draft-boundaries` 仍退出 1；输出不再包含本轮新增文件，只包含禁读文件未检查及既有 Bazi/Vedic 脚本红项。因此本轮只记“新增边界未引入新报告项”，不记全局 gate 通过。
- `fake-indexeddb` 已从包级 allowlist 移除；下游 registry 以 `fake-indexeddb-test-only-v1` 将其机械限制在 `*.test.*`。运行时代码导入、移除该检查或恢复包级 allowlist 均失败关闭；新增定向边界测试 `3/3` 通过。通用 boundary suite 仍因既有全局红项为 `72/73`，不得写成全局 gate 通过。

### 2026-08-31 下游 registry 重锁

- `scripts/system-contract-downstream-draft-registry.json` 当前精确身份为 `1808 bytes / c17503797376c5800364f8976b8c11e5b42479a637206de8b09ae77374f1cc59`；
- 吠陀 input-admission kernel 已同步该下游身份及 test-only policy，CLI point-in-time observation 通过；
- kernel verifier tests `20/20`、package Vitest `1 file / 24 tests`、package strict TypeScript 均通过；
- 这些结果只关闭依赖边界与观察身份漂移，不建立吠陀输入正式准入、持久化运行时、内容／专家／权利真值、Release Evidence 或公开授权。

## 2026-08-31 三体系独立 observation child

本批只新增 child 与独立 verifier，不重签 `four-system-admission.v1.json`，不改紫微／西洋 domain manifest，不覆盖吠陀 v1.1，也不产生 parent backlink。

### 紫微高风险 policy source-binding child

- `content/system-admission/ziwei-high-risk-expression-policy-draft.v0.1.0.json`；
- child digest：`aff40f1d87c6575c9254877545e787b645a444090c18bcc976ae9c61deb8b0ac`；
- 绑定 policy 源码 `34053 bytes / 7b10bb23fe0194b0dc564eec895d9dc1bb99658ad5fcc710f8edd14d1fdd8616`、policy canonical digest、8 类与 16 surfaces；
- `releaseIdentity / targetSchema / migrationId` 均为 null；`candidateCallSitesWiredToGate`、callgraph integrity、semantic coverage、pre-import integrity 和全部 authority 均为 false；
- 这证明的是当前源码身份，不证明 16 个 surface 的实现文件、真实调用点或输出 sink 已接门。

### 西洋独立版本观察 child

- `content/system-admission/western-independent-productization-version-aware-observation-candidate.v1.0.0.json`；
- candidate digest：`6f71eb3e0f8745c82b383e6e05bf9130d6249530f4ad1d872620480796b1deeb`；
- 绑定当前独立 manifest、28 条 requirements-only 来源账、Astronomy Engine notice child、19 个工程／测试 endpoint 和 2 个 tzdb descriptors；
- 中央 registry 的西洋 6 项身份比较为 `3 match / 3 mismatch`，因此明确 `staleForWestern=true`、`centralRegistryUsableForWesternAdmission=false`、`formalPromotionBlocked=true`；
- 西洋自身 `releaseIdentity / targetSchema / migrationId` 均为 null；`legacy-v13 / schema 13` 只作为项目背景，不向西洋继承。

### 吠陀 v1.2 version-aware observation child

- `content/system-admission/vedic-independent-productization-version-aware-observation-child.v1.2.0.json`；
- candidate digest：`462498245b533db031ed25a1a8808e4e77dc2ea92f17154013a04d487ebdb171`；
- 保留旧 v1.1，并单向绑定 parent、v1.1、source-rights、两席零实例专家计划、高风险、input structural、readiness、transition、storage、当前 input kernel 和 mutation experiment；
- 固定 `0/8` admission、`0/38` binding、`0/2` expert；kernel 仅为 point-in-time non-atomic source observation，formal parent integration、runtime、persistence 与成功 receipt 均为 false；
- mutation experiment 的测试／构建描述不被当作本 observation 自己执行的证据，browser button、产品 mutation gate、产品 receipt 与 external monotonic anchor 仍为 false。

### 本批定向验证

- 新 child verifier tests：紫微 `19/19`、西洋 `8/8`、吠陀 v1.2 `13/13`；
- 既有回归：紫微 policy `1 file / 36 tests`、吠陀 mutation experiment `1 file / 10 tests`；
- 吠陀 kernel：CLI 通过、verifier `20/20`、package `1 file / 24 tests`、strict TypeScript 通过；
- 西洋独立来源 requirements verifier 与独立 domain manifest verifier 通过，继续报告 `28/0`、专家 `0/2` 与全部正式 authority false；
- `fake-indexeddb` file-scoped 边界新增 `3/3` 通过；通用 boundary suite 仍为 `72/73`，当前 CLI 仍只列既有红项，没有新增紫微、西洋或吠陀 v1.2 路径；
- 上述测试按各自配置分账，不相加冒充全仓回归、完整浏览器验收或 Release Evidence。

## 仍为红的独立账

- 内容真值、预测准确性与其他体系权威：未建立。
- 来源正文、exact quote、版本／作品／载体三层权利：未建立。
- 现实专家身份、资质、独立性和至少两份并列意见：未建立。
- PWA、Service Worker、Chrome/Edge 跨浏览器、正式仓储、公开主机、Release Evidence、部署与回滚：未建立。
- `publicDeploymentAuthorized=false`、`publicReleaseAuthorized=false`、`expertClaimsAuthorized=false`。

## 2026-09-01 西洋 civil-time→UTC fact-only 隔离浏览器观察 child

### 工程链与范围

[独立机械验证说明](./阶段E-西洋civil-time至fact-only浏览器链机械验证-v1-2026-09-01.md) 与 [candidate child](../content/system-admission/western-civil-time-fact-browser-observation-candidate.v1.0.0.json) 新增一条 declared civil wall time→UTC 的事实层浏览器链。它只输出 UTC instant、offset、gap/overlap decision、tzdb identity 与 round-trip evidence；不接入 Astronomy Engine、4219 规则、宫位、相位、解释、跨体系比较、正式存储或默认 Web。文档 `v1` 不代表产品 v1，协议仍为 `0.1-draft`。

西洋自身继续为 `releaseIdentity / targetSchema / migrationId = null / null / null`；项目背景仍是 `legacy-v13 / targetSchema 13 / migrationId null`，但不向西洋继承。Worker 是同一构建内的受信计算组件，主线程不独立重算 tzdb 语义；SHA-256 不是签名，UI generation 只抑制 stale view，不是 mutation epoch。

### 实际 Browser 观察

最终 build 仅在 Codex In-app Browser 的 `127.0.0.1` 受控预览中观察。四组 synthetic 场景覆盖 Shanghai unique、历史秒级 offset、New York overlap reject 与 earlier；reject 清空旧结果且不产生部分事实。请求 `390x844` 时 `clientWidth=scrollWidth=375`，无横向溢出；表单 named controls 0、按钮 `type=button`、CSP `form-action 'none'`，console warning/error 0。Chrome、Edge、PWA、Service Worker、公开主机与完整应用均未验证。

### 隐私、storage 与生产 reachability

surface 能处理个人派生数据，因此 `safeToLog / safeToPersist / safeToPublish=false`。runtime storage probe 在只读 browser sandbox 中不可用；当前只建立 authored-source storage/cookie/cache/SW API 定向扫描 0 命中。专用 verifier 扫描 372 个可读 `apps/web` 源文件，未观察到反向引用，但受限文件继续明确跳过，故 `incompleteProductionReachabilityAudit=true / wholeRepositoryProductionUnreachableEstablished=false`。

### 机械验证与冻结身份

child raw／digest 为 `c23cef0c53f433e4996c78769c4f9ffe023309e8ef136414e67fc35d7e0932b2`／`e898edd1e91aa69193157bdbfc5ea24610a828ce16588388cd9b7cb5b6331b81`，绑定 25 个 artifacts 与 11 个构建产物。两次 GUID 临时目录构建逐字节一致；Vite `7.3.6` 来自 `apps/web` 已安装工具链，只是构建工具借用，不是 runtime/product import，也不证明工具链独立。隔离 `28` tests、专用 verifier `11` tests、canonical contract `7` tests 与两条 strict TypeScript 切片通过；中央 boundary tests 为 `74/75`，唯一失败由当前工作区既有 expected-red 在 alias 注入前触发，不是本链回归，也没有绕过红门。

### 仍为红的七账

| 账 | 结论 |
| --- | --- |
| 工程证据 | 隔离源码、Worker 协议、定向测试、静态边界与最终 build 已机械观察 |
| 浏览器／运行时证据 | 仅 Codex In-app Browser 受控 loopback；非 Chrome/Edge、非完整应用、非生产 |
| 内容真值 | `false` |
| 专家真值 | `0/2`，`false` |
| 权利法律判断 | `not_established` |
| 发布就绪 | `false` |
| 公开部署／公开发布授权 | `false / false` |

西洋 Binding 继续 `0/28`、正式来源正文与 exact quote 为 0、三层权利未建立、admission `0/8`。八字 v1.7、紫微与吠陀权威均不得外推到此链。
