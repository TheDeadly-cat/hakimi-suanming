# Schema16 / Web v1 新任务短交接（2026-08-12）

> 性质：用于替代无法继续压缩的超大 Codex 对话上下文；它是工作导航，不替代项目证据。  
> 权威状态仍以当前源码、`项目正式总结与后续工作交接-2026-08-11.md` 和 `待提交变更与证据总账-2026-08-11.md` 为准。

## 1. 为什么新开任务

- 旧任务：`哈基米算命：Schema16 发布闭环与 Web v1 后续`
- 旧任务 ID：`019fe758-875a-7433-9d6d-78144cb02d2d`
- 旧任务完整历史仍保留，未删除、未截断。
- 旧任务在自动/手动历史压缩前被远端接口拒绝：`input[14].content` 的数组形状触发 `array_above_max_length`（HTTP 400）。继续向旧任务发送消息只会新增失败轮次，不能形成可用压缩摘要。
- 因此后续工作必须从一个不继承旧对话历史的新项目任务开始，并通过本文件和项目内证据恢复上下文。

## 2. 当前工作区事实基线

- 项目目录：`C:\Users\Administrator\Documents\哈基米算命`
- Git：`master`，HEAD `85c7f96`
- 2026-08-12 交接前状态：44 个工作区条目，其中 29 个已跟踪修改、15 个未跟踪条目。
- 这些修改和未跟踪文件均属于现有项目工作；不得清理、覆盖、重置或擅自整仓提交。
- 新任务必须直接使用当前本地项目目录；不要创建只基于 HEAD 的干净 worktree，否则会看不到当前 44 项未提交工作。

## 3. 最近一次已记录证据

以下是 2026-08-11 证据总账中的记录，接手者必须按日期和证据层级表述，不得自动当作今天重新验证：

- 全量 Vitest：147 文件 / 1541 项通过（`--maxWorkers=2`）。
- typecheck 与体系草案边界检查通过。
- 默认构建仍为 `legacy-v13 / targetSchema 13`，build hash `e69c67ad256c`；不得把 v15/v16 提升为默认发布代。
- v13→v16 自然升级矩阵：Edge 13/13 + Chrome 13/13 = 26/26。
- P2-05 重数据集浏览器门：Edge + Chrome 合计 8/8。
- 紫微 4218：Edge + Chrome 合计 20/20。
- 西洋 4219：Edge + Chrome 合计 8/8，JPL 官方字节仍缺失，严格回执保持 `issued:false`。
- 跨体系 4220：Edge + Chrome 合计 6/6；仍是隔离只读预览，不是正式主入口。
- `web-v1-flow:v13-to-v16` 只有 2026-08-10 的 Edge/Chrome 2/2 历史证据；高负载环境下的后续复跑未完成，不能宣称当前新通过。

## 4. 未关闭边界

- 未获得提交授权，不执行 `git add`、`git commit`、`git reset` 或整仓整理。
- Android 固定真机/模拟器、P2-05 Android 验收门和 Capacitor 壳仍缺外部设备条件。
- JPL Horizons 官方响应字节仍受网络条件阻断；不得用合成字节冒充官方证据。
- 360 金标仍为 candidate=60 / verified=0；工程测试不能冒充专家真值或内容权利证明。
- 第二体系尚未获准进入正式壳；不得擅自显示正式体系切换器。
- 不允许用绕过 mutation epoch 的 raw IndexedDB 写入伪造 v16 数据。

## 5. 新任务启动顺序

1. 先读本文件、`项目正式总结与后续工作交接-2026-08-11.md`、`待提交变更与证据总账-2026-08-11.md`。
2. 运行只读的 `git status --short`，确认当前 44 项基线是否发生变化；已有变化一律视为用户工作。
3. 以当前源码为事实基线，先核对默认发布身份仍为 `legacy-v13`，再选择下一项工作。
4. 优先处理不依赖外部授权/设备的 Web v1 收口项；如果机器资源允许，可重新验证 `web-v1-flow:v13-to-v16`，并把“历史证据”和“本轮证据”分开记录。
5. 若下一步需要提交、Android 设备、JPL 官方字节、专家复核或正式入口授权，明确列出所需输入并停止在对应边界，不得自行扩权。

## 6. 新任务运行设置

- Provider：OpenAI
- 模型：`gpt-5.6-sol`
- 推理强度：`max`
- 工作方式：当前本地项目目录，不继承旧任务对话历史。
