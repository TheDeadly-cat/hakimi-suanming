# 待提交变更与证据总账（2026-08-11）

> 性质：提交前的盘点，不是提交动作；未经明确授权不会执行任何 `git add/commit/reset`。  
> 前置：[项目正式总结与后续工作交接-2026-08-11.md](./项目正式总结与后续工作交接-2026-08-11.md)

## 1. 一句话状态

跨 Schema 收敛、P2-05 浏览器重数据集/取消/资源探针、紫微 4218、西洋 4219 与跨体系 4220 隔离预览的证据均已形成并通过双浏览器复验；普通构建仍为 `legacy-v13 / targetSchema 13`。所有变更尚未提交，等待用户授权；Android 与专家真值仍依赖外部条件。

## 2. 建议提交分组（仅建议，未执行）

### A. DeepSeek BYOK 只读研究助手（此前已完成并验证，本日未改动）

- `apps/web/src/lib/deepseek-assistant.ts` + `.test.ts`
- `apps/web/src/components/deepseek-assistant-panel.tsx` + `.test.tsx`
- `apps/web/src/pages/chart-page.tsx`、`apps/web/src/pages/help-page.tsx` + `.test.tsx`
- `apps/web/src/styles.css`
- `packages/platform/src/android-boundary.test.ts`
- `docs/产品方向更新-v0.2-专业研究版.md`、`docs/项目完善总纲与实施指引-v0.1.md`

### B. 跨 Schema v13→v16 自然升级收敛修复

- `apps/web/src/lib/release-database-coordinator.ts`（源冻结有界重试、同代迁移在途等待、失败态诚实标记）
- `apps/web/src/main.tsx`（影子启动预算 300 秒）
- `apps/web/e2e/service-worker-cross-schema-v13-v16.spec.ts`（新增场景 13 与 14/14 矩阵）
- `apps/web/src/lib/release-database-coordinator.test.ts`

### C. P2-05 浏览器重数据集/取消/资源探针

- `packages/storage/src/heavy-dataset-evidence.fixture.ts`（共享重数据集 + 正向组合夹具）
- `packages/storage/src/heavy-dataset-evidence.test.ts`
- `apps/web/e2e/p2-05-heavy-browser-gate.spec.ts`（4 测试 × 2 浏览器）
- `apps/web/playwright.p2-05-heavy.config.ts`
- `apps/web/src/lib/batch-cancellation-contract.test.ts`
- `package.json`（`test:e2e:p2-05-heavy-browser-gate`）

### D. 紫微 4218 浏览器证据

- `packages/ziwei-workspace-artifact-draft/e2e/workspace-browser-gate.spec.ts`（7 测试 × 2 浏览器）
- `packages/ziwei-workspace-artifact-draft/README.md`

### E. 西洋 4219 故障矩阵

- `packages/western-astrology-rules-preview-draft/e2e/rules-preview-browser-gate.spec.ts`（4 测试 × 2 浏览器）
- `packages/western-astrology-rules-preview-draft/README.md`

### F. 跨体系 4220 只读并列预览（新文件）

- `packages/cross-system-comparison-draft/browser-app/index.html`
- `packages/cross-system-comparison-draft/src/browser-app/main.ts`
- `packages/cross-system-comparison-draft/vite.browser-preview.config.mjs`
- `packages/cross-system-comparison-draft/playwright.preview.config.ts`
- `packages/cross-system-comparison-draft/e2e/cross-system-preview-browser-gate.spec.ts`
- `packages/cross-system-comparison-draft/README.md`
- `scripts/system-contract-draft-registry.json`（browserPreview 隔离图）
- `package.json`（4220 构建/预览/E2E 脚本）

### G. 文档口径同步

- `README.md`、`docs/v1功能验收矩阵-v0.1.md`、`docs/P2-05原子完整性审计协议-v0.1.md`、`docs/P2-05案例库容量基线-2026-08-04.md`、`docs/跨Schema数据库与ServiceWorker发布协议-v0.1.md`、`docs/紫微斗数契约草案与来源门-v0.1.md`、`docs/西洋星盘契约草案与来源门-v0.1.md`、`docs/多术数融合扩展边界-v0.1.md`、`docs/项目正式总结与后续工作交接-2026-08-11.md`

### H. 构建脚本（受限环境可复现）

- `apps/web/package.json`：默认 build/preview、e2e build/preview、v13→v16 候选 build/preview 全部改用 Vite 7 `--configLoader runner`（只改变配置加载方式，不改变发布身份；本机受限环境拒绝 esbuild 配置打包读取父目录）

### I. 八字内容 v0.18 来源—主张账与旺衰证据叙事（2026-08-13 后继增量）

- `packages/bazi-interpretation/src/strength-claim-registry.ts`：8 个来源、12 个精确 binding、12 条 claim；分开作品层、数字载体层、locator 核验与工程参数来源；
- `packages/bazi-interpretation/src/strength-evidence-narrative.ts` + `.test.ts`：当前盘逐项因素、月主气双计、分档与六情景叙事；
- `packages/bazi-interpretation/src/current-chart-hit-review.ts` + `.test.ts`：当前盘复核协议升级为 v0.2.0，并绑定 narrative／claim registry 摘要；
- `packages/bazi-interpretation/src/index.ts`：导出 v0.18 合同；
- `apps/web/src/components/bazi-strength-evidence-ledger.tsx`：只读来源账与“旺衰为什么落在这一档”；
- `apps/web/src/components/bazi-interpretation-panel.tsx` + `.test.tsx`、`apps/web/src/styles.css`：接入当前 Revision 与展示；
- `README.md`、`docs/八字内容-v0.18-来源主张账与旺衰证据叙事-2026-08-13.md` 及本轮增量文档：来源、权利、历史／新证据和下一 P2 同步。

该分组当前只作范围盘点，仍未授权暂存或提交。

## 3. 当前证据账（均为本机当前代码实测）

| 门 | 证据 |
| --- | --- |
| 全量 Vitest | **147 文件 / 1541 项**通过（`--maxWorkers=2`，约 212 秒） |
| typecheck / 边界检查 | `tsc --noEmit` 通过；`check:system-contract-draft-boundaries` 通过 |
| 默认构建 | `legacy-v13 / targetSchema 13`，build hash `e69c67ad256c` |
| v13→v16 自然升级矩阵 | Edge 13/13 + Chrome 13/13 = **26/26**（含冻结重试、组合条件负向/正向命中、CSV 预检取消） |
| v13→v16 clean-start 容量 | Edge 1.300 s、Chrome 1.213 s（`epoch=verifiedEpoch=1`、`cache_hit`） |
| P2-05 重数据集浏览器门 | Edge + Chrome 各 4/4 = **8/8**（恢复、单/多关键词/负向/组合条件、10k 导出取消、5k CSV 预检取消、资源探针） |
| 紫微 4218 | Edge + Chrome 各 10/10 = **20/20**（完整套件双浏览器复跑确认；全链路、8 条备份/恢复、陈旧 epoch、计算 Worker 故障、损坏备份、内容冲突恢复、保存事务中止、设备配额不足、三方并发陈旧写入） |
| 西洋 4219 | Edge + Chrome 各 4/4 = **8/8**（快乐路径 + 零持久化、Worker 崩溃、畸形回执、表单失败关闭） |
| 跨体系 4220 | Edge + Chrome 各 3/3 = **6/6**（并列通过 + 零持久化、摘要失配、边界提权） |
| web-v1-flow:v13-to-v16 | 2026-08-10 Edge/Chrome 2/2 历史证据；本机高负载下当前复跑未完成，未宣称新通过 |

## 4. 仍未关闭（外部依赖）

- **提交授权**：以上全部变更均未提交；`tmp/pending-deepseek-byok-commit.md` 与 `tmp/deepseek-byok-pending.zip` 仍在。
- **Android**：需要固定真机/模拟器；P2-05 固定 Android 验收门与 Capacitor 壳未开始。
- **西洋官方星历**：JPL Horizons 官方字节仍被网络层阻断，严格回执保持 `issued:false`。
- **专家真值与内容权利**：360 金标 candidate=60 / verified=0；紫微、西洋、随包内容需要现实复核人与合法来源。
- **共同工作台正式入口**：需第二体系获准进入正式壳后才显示体系切换器。

## 5. 约束

- 不清理、不覆盖、不重置、不擅自整仓提交；
- 不把 v15/v16 提升为默认发布代；
- 不以绕过 mutation epoch 的 raw IndexedDB 写入伪造 v16 数据；
- 不把工程复现、哈希一致或浏览器通过表述为专家真值；
- 所有“通过”均对应当前代码的单测或真实浏览器证据。

## 6. 2026-08-13 未提交后继增量（不并入第 3 节旧账）

- 新增 `packages/bazi-interpretation/src/strength-policy.ts`，让解释器、敏感性、十神方向与审稿问题统一引用 4/2/2/1 政策；旧 4/2/1 只是文字漂移纠偏，不是专家真值。
- 新增 `current-chart-hit-review.ts` 及测试，形成当前盘最小事实、实际十神／神煞命中、来源和完整候选快照绑定的 2 MiB 只读复核包。
- `apps/web` 新增独立当前盘面板：显式准备神煞、导出、只读预检、跨 Revision epoch/token 失效与错盘失败关闭；不写任何浏览器存储。
- 新增独立 Edge／Chrome 配置与用例；根脚本为 `test:e2e:bazi-current-chart-review`。
- 当前新证据：v0.17 定向 Vitest 6 文件 / **57 项**，默认身份定向 Vitest 3 文件 / **19 项**，typecheck 与草案隔离门通过，Edge + Chrome **2/2**；E2E 2053 modules、build `dfb13e14bb74`，HTML/SW 仍为 `legacy-v13 / targetSchema 13`。
- 新增 `术数内容新目标与来源路线图-2026-08-13.md`，分开登记八字、紫微、西洋的竞品 IA、传统文本、工程来源、许可风险与下一最小内部目标；它不构成内容授权或专家验收。
- 本轮未重跑第 3 节整仓 1541 项、Schema 16 26/26、Android 或其他体系长门；不得把历史计数并入上述新证据。
- 所有后继修改仍未暂存、未提交；详细合同与来源边界见 `八字内容-v0.17-旺衰政策单一事实源与当前盘命中复核包-2026-08-13.md`。

## 7. 2026-08-13 八字 v0.18 未提交后继增量（与第 6 节 v0.17 分账）

- 新增 `hakimi.bazi.strength_claim_registry/0.1.0`：8 sources / 12 bindings / 12 claims；数值主张只能绑定内部工程定义，pinned 古籍只作传统语境，待核 locator 不能进入 enabled claim。
- pinned 来源为《滴天髓阐微》`oldid=2600158`、《三命通会》卷五 `oldid=2706483`、《三命通会》四库本卷十 `oldid=761703`；《渊海子平》`oldid=2593607` 明确保持来源警告，《子平真诠评注》CText revision 未冻结。
- 新增 `hakimi.bazi.strength_evidence_narrative/0.1.0`：按月令→透干→首藏→余藏逐项展开，公开当前工程 `4 + 2` 月主气双计、五档阈值和六情景敏感性；未知时辰不序列化任何被关闭的时柱值。
- 当前盘复核协议升级到 `hakimi.bazi.current_chart_hit_review/0.2.0`，把 narrative、claim registry 与有序 ID 摘要绑定进 packet；Web 新增只读来源账。
- 作品公版候选不等于数字转录层公版；Wikisource 复用需另核适用许可与署名／相同方式共享，CText 当前仅链接和人工 locator。`contentSha256:null` 和 pinned oldid 均不能冒充影印校勘。
- 4/2/2/1、四个阈值与六情景只属工程政策；外部古籍不支撑项目参数。旧 69 项审稿目录仍未全部获得同等级 locator，既有“偏有利／需警惕”也不能冒充专家吉凶。
- v0.17 的 57 项、19 项、双浏览器 2/2 与 build `dfb13e14bb74` 全部只保留为历史证据；v0.18 新执行定向 Vitest **4 files / 27 tests PASS**，默认发布身份定向 **3 files / 23 tests PASS**，`npm.cmd run typecheck` 与草案隔离 verifier 均 **PASS**。
- v0.18 E2E 构建为 **2058 modules**、build `bc2c28f15407`，产物仍为 legacy-v13/schema13。应用内 Browser 最新壳桌面与 390×844 均显示 12 claims / 12 unique bindings、月主气 `4 + 2 = 6`，四共享 SHA 一致，overflow 0、overlay 0、console warning/error 0。
- Edge + Chrome 各 1/1、共 **2/2 PASS**；测试 30.2 秒、总命令 34.3 秒；完整 IndexedDB/local/session/cache 前后相等，跨 Revision 失效关闭，`.last-run` 为 `status: passed`。两浏览器截图字节一致：桌面 `6fa6c663af55a14ae7c809f7f01c3bc35639fa2cdec551d1906e473c349ab276`，390×844 `e1318b5808decaa04f7cd0908946a16318168e500656b784f21ee24339bf72d9`。
- 本轮没有重跑整仓 147 文件 / 1541 项、Schema 16 完整矩阵、Android、Firefox 或其他体系长门；v0.18 只能称为定向收口。
- v0.18 不改变 `legacy-v13 / targetSchema 13 / migrationId null` 发布合同，不绕过 mutation epoch，不建立专家真值、科学有效性、Android、正式入口或提交授权。
- 下一内部内容目标为隔离紫微 v0.13：核心十二辅煞 × 三方四正 occurrence 动态审稿包。

## 8. 2026-08-14 紫微 v0.13 未提交后继增量（定向证据已收口）

- 新增整盘投影合同 `ziwei.core_minor_star.sanfang_occurrence_review/0.1`：12 个目标 review；标准盘十二个核心辅煞事实各进入本宫、对宫 `+6`、三合 `+4`、三合 `-4` 四组，共 48 个 occurrence；空本宫不借星。
- 新增动态反馈合同 `hakimi.ziwei.core_minor_star_sanfang_review_feedback/0.1.0`／模板 `0.13.0`：绑定当前规则、事实、完整候选快照、12 组、有序 occurrence 与五来源登记。
- 五来源只复用核心星曜 2、宫位 2、几何 1；没有新增星义语义来源。传统分组、星数、亮度、四化和 occurrence 数量均不得加总评分。
- Web 范围是当前整盘；切宫只过滤显示。编辑、重算、重开、换盘、失败或异步旧结果会令旧动态包失效。v0.13 面板与旧 v0.10 静态 48 项四化反馈分离，决定不回写 v0.12 静态 12／144 候选。
- 导出排除直接身份与原始输入，但四宫关系、星曜、亮度、四化是敏感派生事实；SHA-256 不是加密、签名或匿名化。网络、存储、mutation、formal、scoring、自动整合与直接结果继续关闭，结果字段继续为 `null`。
- v0.12 的 25 项、106-module build、默认身份 18 项与双浏览器 2/2 只保留为历史证据。本轮 v0.13 新执行 adapter 定向 Vitest **2 files / 30 tests PASS**、三个 typecheck、隔离 boundary verifier、默认身份 **3 files / 18 tests PASS**、4218 build **108 modules transformed**；源码图为 **22 paths = 21 TypeScript + 1 JSON**。
- 应用内 Browser 完成桌面与 `390 × 844` 定向验收：12 reviews／48 occurrences、702,355-byte 模板生成与导入、切宫保持、篡改与跨盘拒绝、旧静态 48 隔离，页面保持 `0 Revision / epoch 5 / 0 B`、横向溢出 0、console 0。
- 独立 v0.13 spec／config 在 Edge、Chrome 各 **1/1、共 2/2 PASS**，最终 **7.0s**，`.last-run` 为 `status: passed`；完整浏览器存储快照前后相等，E2E 页面保持 `0 Revision / epoch 0 / 0 B`。因沙箱拦截 Windows `taskkill`，正式门在获准的沙箱外执行，产品断言未改变。两浏览器截图字节一致且人工复核无截断或错误着色：桌面 SHA-256 `22f01c0195675d8a7c72c3471745ccf4603fc99c01e597d839bf925df778ee97`，移动端 SHA-256 `cf2d0e9b7791cfce32abf96febcb5ef73ea8cb3ab78be2117aa924535a8ebe67`。
- 默认发布合同保持 `legacy-v13 / targetSchema 13 / migrationId null`；不提升 v15/v16，不绕过 mutation epoch，不建立 Android、JPL、正式入口、提交、专家或科学授权。
- 新增详细账 `docs/紫微内容-v0.13-核心十二辅煞三方四正命中复核包与证据-2026-08-14.md`；以上修改仍未暂存、未提交。
