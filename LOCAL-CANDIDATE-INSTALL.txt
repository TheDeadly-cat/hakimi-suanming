哈基米本地研究候选版 4de42e9db980 / 安装包修订 2（独立入口 5189）

应用源码：d3ff1bdcc64d0917dcc604f753ac896eb62a9c1e。
此包保存已完成 Edge/Chrome 16 条启动检查、PWA 和连续用户流程验证的同一份产物及原始回执。
它是本地工程候选。真实专家准入、来源校勘和整体 CI 尚未全部完成。

需要已安装 Node.js 24.16.0。安装和启动不会运行 npm、重新构建或自动下载运行时。
先完整解压到新文件夹，在 PowerShell 中运行：
  powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\install-local-candidate.ps1
默认安装在当前用户 USERPROFILE\HakimiBaziWorkbenchCandidates\packages\4de42e9db980-package-v2，
建立名称含 Candidate (5189) 的独立快捷方式。已有不同包或快捷方式会被保留并拒绝覆盖。
不建立快捷方式：增加 -NoShortcut。
自选全新安装根目录：增加 -InstallRoot "D:\HakimiCandidate"。

启动：双击 Candidate (5189) 快捷方式，或运行：
  powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\launch-local-candidate.ps1 -PackageRoot "已解压包的完整路径"
入口固定为 http://127.0.0.1:5189/。端口被其他服务占用时停止，既不换端口也不终止该服务。
日常 c15ef 版本继续使用 5188。本候选不会替换日常安装或自动清理、迁移浏览器数据。
两个入口的数据空间独立。第一次打开候选时案例库为空是正常的。
在候选中新建的案例留在 5189；需要保留时使用应用里的完整备份导出，并妥善保存 ZIP。
安装副本不会内置任何用户案例、古籍原件、专家原件、浏览器档案或账户凭据。

当前验证范围：同一 Windows 主机上的两个真实浏览器；另一台干净 Windows 和本人真实研究周期尚待验收。
原始日志和截图保存在维护者 Z 盘备份。包内原始回执只证明其明确列出的测试与产物，不能证明正式发布许可。

已有 5189 候选安装的本机：先用 -NoShortcut 安装到新目录。现有快捷方式和监听服务不会被替换。
本包修复首次控制器确认前允许输入、接管刷新后输入丢失的问题。切换已有 5189 入口前需另行明确决定，并先保存当前研究资料。
第一份 9057371baf85 候选及 c15ef 日常包继续保留原始身份。

修订 2 保留原 137 个应用文件、产物锁及浏览器回执，应用没有重新构建。
只更新安装工具：探测已占用端口时按锁定文件大小流式读取，超量立即取消。
修订 1 的 ZIP、安装目录和原始验证记录继续保留，不能覆盖为修订 2。
